# Plan 06-03: Baseline-Preservation Guard (INTEG-01, Rule #3)

**Goal:** Prove stock baseline remains buildable/runnable from same tree — never destroyed.

**Deliverables:**
- Mid-phase `cmake -B build-stock -DGGML_CUDA_ENABLE_CUSTOM_GFX1100=OFF` rebuild + `run_op_gate.py` 21k PASS + `llama-bench -c 8192` sanity
- `baseline/binaries/v0.2.0-bb4caa75/` fingerprint unchanged, `guard.py` RSS + `preflight.py` VRAM cap still green
- **Env version gate re-run** after any driver/ROCm change (silent-pairing-breakage pitfall); verify frozen WSL snapshot reference intact before scheduling the final matrix

**Verification:** Stock rebuild log + op-gate JSON with 0 errors written to a NEW timestamped file (never overwrite history), `sha256sum -c baseline/...` unchanged.
