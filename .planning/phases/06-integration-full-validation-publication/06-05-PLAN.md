# Plan 06-05: Thermos Review Remediation (pre-publication gates)

**Goal:** Fix all HIGH/blocker findings from the 2026-08-25 thermos double-review before any publication claim ships. Split: (a) docs/repo hygiene — no GPU needed; (b) kernel guards + verification — needs one `test_gemm_compare` re-run.

## A. Repo & docs (no GPU, ~30min)

1. **`.gitignore` staging trap:** ignore `kernels/fixtures/*` with explicit `!kernels/fixtures/manifest*.json`; add `/test_wmma*`, `*.bc`, `*.hipi`, `*.hipfb`, `*.out.resolution.txt`. Delete stray root `test_wmma-*` artifacts. Adopt `scratch/` convention for future probes.
2. **Fixture determinism:** replace salted `hash(name)` in `tools/dump_matmul_fixtures.py` with stable sha256-derived seed; per-fixture `default_rng(SeedSequence([seed, stable_hash]))`. Note: existing manifests not byte-reproducible until regenerated — regenerate or annotate manifest.
3. **Doc drift:**
   - README phase counter → **5 of 6 (83%)**, Phase 5 complete.
   - Label comparator "**naive scalar HIP baseline**" consistently in README/QWEN-GRAPH/STATE headlines (never imply beating real llama.cpp MMVQ/MMQ until 06-02 true-stock comparison).
   - `KERNEL-BENCH-DIFF.md`: fix "18/18"→"11/11"; soften "no literal 32/64" claim; make it the canonical results doc — STATE/ROADMAP/PUBLICATION reference it instead of restating tables.
4. **model_gate.json history rule:** keep the FAIL artifact; STATE.md records FAIL+cause hypothesis+follow-up; going forward every gate run writes a new timestamped file — never overwrite a historical PASS record.
5. **Dead code deletion:** `dequant_w_iq4xs()` placeholder, `dequant_row_cpu()` pass-stub, unreachable `elif`, chained assignment, commented `__shfl_xor` path (move rationale to failed-variants log), probe-transcript comments in `extract_weight_for_shape`.
6. **Doc inconsistencies:** `KERNEL-BENCH-DIFF.md` "18/18"→"11/11"; soften "no literal 32/64" claim (`BLOCK_N=64` literals exist in WMMA path); `TENSOR_CANDIDATES` — remove/annotate non-Qwen leftovers (`blk.0.ssm_out.weight`, `blk.3.attn_output.weight`) that silently trigger synthetic fallback; rename fixture manifests to an unambiguous pair (`manifest_dequant.json` / `manifest_matmul.json`, update `test_fixture.py`).
7. **Provenance honesty (L-3):** `benchmarks/tools/run_kernel_bench.py` fallback manifest hardcodes gpu/rocm/commit when fingerprinting throws — mark those fields `"unknown"` or fail loudly instead of fabricating provenance.
8. **CMake cleanup (F6):** delete permanently-true `EXISTS(...)` conditional targets in `kernels/matmul_iq4xs/CMakeLists.txt`; collapse the 6× repeated include/link stanza into one INTERFACE library (`matmul_common_iface`).
9. **ROADMAP Merge Map clarify:** Phase 10 row → note "light graph-aware report landed as `docs/QWEN-GRAPH.md`; deep tensor dump = v2".

## B. Kernel guards + verification (GPU, ~1h)

10. **WMMA coverage gap (H-1):** add gate-passing test case (`K=5120, N=1024, M=512`) asserting vs CPU oracle; zero-init `B_lds` or clamp `br %= 16` (lanes ≥16 read uninitialized LDS rows today). Re-run `test_gemm_compare` — M=512 prefill numbers stay **provisional** until this passes.
11. **Barrier divergence (M-1):** remove early `return` before `__syncthreads()` in GEMV (compute-with-mask) or tighten host gate; same for WMMA partial-tile exits (gate `%16` vs comment `%64/%32` mismatch).
12. **Misaligned loads (M-2):** replace `reinterpret_cast<const uint4*>` type-punning with aligned-safe load (two `uint2` / memcpy-style) or document alignment guarantee.
13. **Fallback logic (L-2):** set `wmma_ok=false` on launch error so tiled fallback actually engages; simplify launcher flow while touching it (single `launch_tiled` helper, drop verbatim duplication).
14. **Shape guards (L-1/NIT):** host-side assert `K % QK_K == 0`; error (not clamp) on grid > INT32_MAX.
15. **Dedupe harness:** extract `kernels/common/matmul_test_util.h` (`Metrics compute_metrics`, `gen_iq4xs_weights`) consumed by all test/bench binaries — collapses four divergent weight distributions into one; unify `Metrics` vs `CompareMetrics` naming.

## Verification

- [ ] `git check-ignore kernels/fixtures/matmul_attn_q_5120x5120_M1.npz` → ignored
- [ ] Regenerated fixtures reproducible across two runs (same sha256)
- [ ] `test_gemm_compare` includes WMMA-gate-passing case → PASS cosine ≥0.999
- [ ] `test_gemv_compare` + `matmul_test_baseline` still PASS after barrier/load fixes
- [ ] grep: no "beat stock HIP" headline without "naive scalar baseline" qualifier
- [ ] All M=512 claims marked provisional-or-verified per outcome of #6

**Time:** ~90min total; B items need one supervised GPU session (step-up: CPU-only rebuild first).
