# Plan 06-01: Empty-Flag Plumbing Proof (INTEG-01)

**Goal:** Prove ON/OFF switch wiring is sound before winner logic lands — OFF must be bit-identical to stock `bb4caa75`.

**Deliverables:**
- `ggml/src/ggml-cuda/CMakeLists.txt` `option(GGML_CUDA_ENABLE_CUSTOM_GFX1100 OFF)` + `ggml/src/ggml-cuda/custom_gfx1100/empty.cuh` (stub `hipErrorNotSupported`)
- `build-stock` (OFF) and `build-custom-empty` (ON+stub) both compile `--offload-arch=gfx1100 -mwavefrontsize32`, `test-backend-ops` 0 errors on both.

**Verification:** stock vs empty-flag binaries + op-gate identical; stock path not regressed. OFF config must **compile clean** — the provisional patch's unguarded `can_handle` calls currently break OFF (thermos H-4); this plan proves the wiring before winner logic lands.
