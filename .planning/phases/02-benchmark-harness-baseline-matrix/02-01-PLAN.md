---
phase: 2
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/tests/pytest.ini
  - benchmarks/tests/fixtures/gen_llabench_jsonl.py
  - benchmarks/tests/test_llabench_wrapper.py
  - benchmarks/tests/test_repro_gate.py
  - benchmarks/RUNBOOK.md
  - benchmarks/prompts/short_code_01.txt
  - benchmarks/prompts/short_prose_01.txt
  - benchmarks/prompts/short_prose_02.txt
  - benchmarks/prompts/long_code_01.txt
  - benchmarks/prompts/long_prose_01.txt
  - benchmarks/prompts/long_prose_02.txt
  - benchmarks/lib/llabench.py
  - benchmarks/bin/run_prompts.py
autonomous: true
requirements: [BENCH-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Wrapper drives the pinned llama-bench with fully explicit cell definitions — every published row is a deliberate -p C (pure prefill) or -pg C,128 (decode-at-context) cell; no upstream-default cell ever enters output"
    - "Each context tier invocation yields exactly 2 x len(fa_seq) rows with correct n_prompt/n_gen/flash_attn fields, asserted mechanically before any row is accepted"
    - "Repeats r=5 with warmup enabled produce per-row samples arrays; a re-run variance gate computes pct deviation and passes/fails at 5%"
    - "Six deterministic prompt files exist with recorded sha256s and a greedy Layer-2 runner executes one end-to-end on the pinned llama-cli"
  artifacts:
    - path: "benchmarks/lib/llabench.py"
      provides: "argv construction, invocation, jsonl parsing, cell-integrity assertion, variance gate"
    - path: "benchmarks/bin/run_prompts.py"
      provides: "Layer-2 greedy real-prompt runner (D2-05 layer 2)"
    - path: "benchmarks/prompts/"
      provides: "D2-08 deterministic corpus, 6 files, short/long x code/prose"
    - path: "benchmarks/tests/pytest.ini"
      provides: "Wave 0 test scaffold anchor"
    - path: "benchmarks/RUNBOOK.md"
      provides: "protocol skeleton (threshold sections filled at calibration)"
  key_links:
    - from: "benchmarks/lib/llabench.py"
      to: "/root/llama.cpp/build-ci/bin/llama-bench"
      via: "subprocess argv with LD_LIBRARY_PATH pointing at build-ci/bin"
      pattern: "build-ci/bin"
    - from: "benchmarks/bin/run_prompts.py"
      to: "benchmarks/prompts/*.txt"
      via: "llama-cli -f PROMPT_FILE greedy single-turn invocation"
      pattern: "--single-turn"
---

<objective>
Build the canonical-matrix engine of the two-layer harness (D2-05): a llama-bench wrapper that makes cheating-by-default impossible (BENCH-01), plus the Layer-2 real-prompt runner and its deterministic corpus. Also lays down the Wave-0 test scaffold the rest of the phase runs against.

Purpose: BENCH-01's reproducibility lives or dies on constraint enforcement — at pin bb4caa75 llama-bench injects default cells unless the wrapper overrides every vector explicitly.
Output: benchmarks/lib/llabench.py, benchmarks/bin/run_prompts.py, benchmarks/prompts/ (6 files), Wave-0 test scaffold, RUNBOOK skeleton.
</objective>

<execution_context>
@C:/Users/arhan/.pi/agent/gsd-core/workflows/execute-plan.md
@C:/Users/arhan/.pi/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-benchmark-harness-baseline-matrix/02-CONTEXT.md
@.planning/phases/02-benchmark-harness-baseline-matrix/02-RESEARCH.md
@.planning/phases/02-benchmark-harness-baseline-matrix/02-PATTERNS.md
@benchmarks/environment/versions.txt
@benchmarks/environment/llamacpp-pin.txt

Environment facts (verified): guest work via `wsl.exe -d Ubuntu-24.04 -u root`; repo visible at /mnt/e/Projects/qwen_3.8_27b_optimizations; pinned binaries at /root/llama.cpp/build-ci/bin (byte-identical to baseline/binaries/v0.2.0-bb4caa75, set LD_LIBRARY_PATH=/root/llama.cpp/build-ci/bin as loader insurance); model at /root/models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf; guest python3 = 3.12.3. llama-bench at pin: NO ctx flag exists (ctx = p+n+d); cooldown flag is --delay (never -D); defaults inject a 512-token prompt vector and an empty-context 128-token gen vector unless overridden; -o jsonl flushes per row and carries samples_ns/samples_ts arrays.
</context>

## Artifacts this phase produces (this plan)
New files: benchmarks/lib/llabench.py · benchmarks/bin/run_prompts.py · benchmarks/prompts/{short_code_01,short_prose_01,short_prose_02,long_code_01,long_prose_02,long_prose_01}.txt · benchmarks/tests/pytest.ini · benchmarks/tests/fixtures/gen_llabench_jsonl.py · benchmarks/tests/test_llabench_wrapper.py · benchmarks/tests/test_repro_gate.py · benchmarks/RUNBOOK.md (skeleton)
Commands introduced: `python3 -m pytest benchmarks/tests/...` (guest), `python3 benchmarks/bin/run_prompts.py ...` (guest)

<tasks>

<task type="tracer">
  <name>Task 1: Wave-0 scaffold + tracer — wrapper proves real-cell contract end-to-end</name>
  <precondition>Pinned binaries exist at /root/llama.cpp/build-ci/bin/llama-bench and the model exists at /root/models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf (Phase-1 artifacts)</precondition>
  <files>benchmarks/lib/llabench.py, benchmarks/tests/pytest.ini, benchmarks/tests/fixtures/gen_llabench_jsonl.py, benchmarks/tests/test_llabench_wrapper.py, benchmarks/RUNBOOK.md</files>
  <read_first>02-RESEARCH.md sections "Pattern 1", "Exact JSONL row schema", "Common Pitfalls" 1-2; benchmarks/environment/llamacpp-pin.txt</read_first>
  <action>
    GPU: brief (~5 min — one model load + tiny cells).
    Install pytest guest-wide so `python3 -m pytest` works exactly as VALIDATION.md specifies: apt-get install -y python3-pytest (if already present, skip). Create benchmarks/tests/pytest.ini with testpaths=tests-relative config so plain `python3 -m pytest benchmarks/tests/ -x -q` discovers from repo root; create fixtures/gen_llabench_jsonl.py generating synthetic llama-bench jsonl rows carrying the full verified field list (build_commit … avg_ns/stddev_ns/avg_ts/stddev_ts plus samples_ns/samples_ts arrays) with controllable n_prompt/n_gen/flash_attn/sample values.
    Create benchmarks/lib/llabench.py implementing EXACTLY these public contracts (later plans import them): constants PIN_COMMIT, BIN_PATH=/root/llama.cpp/build-ci/bin/llama-bench, MODEL_PATH=/root/models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf, TIERS=(4096,8192,16384,32768); build_argv(prompt_tokens:int, gen_pairs:list[tuple[int,int]], fa_seq:tuple[str,...]=("off","on"), repeats:int=5, delay_s:int=30, threads:int|None=None, bin_path=BIN_PATH, model=MODEL_PATH) -> list[str] emitting in order: -m, then -p prompt_tokens, then -n 0 (explicitly zeroes the default gen vector so the banned empty-context cell cannot be enumerated), then -pg with every pair joined as p,g, then -fa off,on per fa_seq, -r repeats, --no-warmup=0 (warmup stays ON per D2-07), --delay delay_s (NEVER the -D short form — unrecognized at pin), -ngl 99 -sm none -t threads (default: physical cores detected once via lscpu), -o jsonl -oe jsonl -v --progress. run_invocation(argv, stdout_path, stderr_path) -> int spawning via subprocess with env LD_LIBRARY_PATH=<bin dir>, returning exit code (fail-fast: caller decides, no retry loops anywhere in this module). parse_rows(path) -> list[dict]. assert_cell_integrity(rows, expected_cells:list[dict]) raising MatrixContaminationError when: row count != expected, OR any row exhibits a default-contamination signature (a generation row whose prompt length equals the upstream default 512, or a generation row with empty-context prompt paired with the default 128 gen), OR flash_attn/type fields deviate. variance_pct(mean_a, mean_b) -> float and repro_ok(mean_a, mean_b, max_pct=5.0) -> bool.
    Tracer proof: run ONE real smoke invocation through run_invocation on the pinned binary — build_argv(prompt_tokens=1024, gen_pairs=[(1024,8)], fa_seq=("off",), repeats=1, delay_s=0) with stdout_path=benchmarks/tests/.tracer_smoke.jsonl (exactly the sentinel path the task's verify step parses) and stderr to benchmarks/tests/.tracer_smoke.err — parse rows, assert_cell_integrity against exactly [{type pp,prompt 1024},{type tg,prompt 1024,gen 8}] at flash_attn off. This proves argv construction, tool contract, and parsing against reality, not fixtures. Write RUNBOOK.md skeleton: title, scope line, dated-amendment-style placeholder sections (§session-protocol, §thresholds TBD-calibration, §telemetry-modes, §thermal-policy, §operational-ceiling note reserving HWiNFO shared-memory duty-cycle limit for the calibration fill).
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_llabench_wrapper.py -x -q && python3 - <<PY
import sys; sys.path.insert(0,"benchmarks/lib")
import llabench
rows = llabench.parse_rows("benchmarks/tests/.tracer_smoke.jsonl")
assert len(rows)==2, rows
print("TRACER_OK", rows[0]["avg_ts"], rows[1]["avg_ts"])
PY'</automated>
  </verify>
  <acceptance_criteria>
    - python3 -m pytest benchmarks/tests/test_llabench_wrapper.py exits 0 in guest
    - tracer smoke produced exactly 2 rows from a real pinned-binary invocation, both at flash_attn 0, with prompt lengths 1024 and no third row
    - pytest.ini exists and `python3 -m pytest benchmarks/tests/ --collect-only -q` discovers the new test module
    - RUNBOOK.md exists with all five placeholder sections present
  </acceptance_criteria>
  <done>Wrapper module proven against the real pinned tool end-to-end; scaffold + RUNBOOK skeleton committed</done>
</task>

<task type="auto">
  <name>Task 2: Full profile engine — tier enumeration, integrity gate, reproducibility gate (per D2-06/D2-07/D2-19)</name>
  <files>benchmarks/lib/llabench.py, benchmarks/tests/test_llabench_wrapper.py, benchmarks/tests/test_repro_gate.py, benchmarks/tests/fixtures/gen_llabench_jsonl.py</files>
  <read_first>benchmarks/lib/llabench.py (own Task-1 output); 02-RESEARCH.md "Internal enumeration order" paragraph; 02-CONTEXT.md decisions D2-06, D2-07, D2-19</read_first>
  <action>
    Extend llabench.py with tier-level orchestration: enumerate_tiers(tiers=TIERS, fa_seq=("off","on")) -> list[TierPlan] yielding one TierPlan per context tier in ascending order (D2-19 project-lifetime cell order: ctx ascending, fa off then on within tier), where TierPlan carries argv (one llama-bench INVOCATION PER TIER — blast radius of a mid-tier OOM is capped at that tier's 4 cells because allocation failure loses remaining cells of that invocation only) plus expected_cells derived as {(pp, tier), (tg, tier, 128)} x fa_seq. Add assert_tier_rows(rows, plan) wrapping assert_cell_integrity with the plan, and scan_banned_signatures(rows_iterable) -> list[str] returning human-readable descriptions of any contaminating row for publish-time auditing. Unit tests in test_llabench_wrapper.py using fixture-generated rows: (a) correct 4-row tier parses clean; (b) injected default-vector rows raise MatrixContaminationError; (c) wrong row count raises; (d) fa order off-before-on validated; (e) argv snapshot test — golden assertion that generated argv contains -n 0, uses --delay spelling, orders -p before -pg, and never contains a bare -D token. Create test_repro_gate.py: variance_pct correctness on synthetic mean pairs (identical=0.0, ±4.9% passes, ±5.1% fails via repro_ok max_pct=5.0), plus a fixture-driven case comparing per-tier means assembled from two synthetic sessions. All tests pure fixtures — no GPU.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_llabench_wrapper.py benchmarks/tests/test_repro_gate.py -q'</automated>
  </verify>
  <acceptance_criteria>
    - Both test modules pass in guest with zero GPU contact
    - Golden argv assertions cover: explicit zero-gen override present, cooldown flag spelled --delay, per-tier single-invocation batching, fa sequence off-then-on
    - repro_ok returns False for deviation above 5% and True at or below it, demonstrated by named test cases
  </acceptance_criteria>
  <done>Matrix engine enforces D2-06 cell semantics, D2-19 ordering, D2-07 repeats/warmup/variance mechanically</done>
</task>

<task type="auto">
  <name>Task 3: Prompt corpus + Layer-2 greedy runner (per D2-08, D2-05 layer 2)</name>
  <precondition>Pinned llama-cli exists at /root/llama.cpp/build-ci/bin/llama-cli (Phase-1 artifact)</precondition>
  <files>benchmarks/prompts/short_code_01.txt, benchmarks/prompts/short_prose_01.txt, benchmarks/prompts/short_prose_02.txt, benchmarks/prompts/long_code_01.txt, benchmarks/prompts/long_prose_01.txt, benchmarks/prompts/long_prose_02.txt, benchmarks/bin/run_prompts.py, benchmarks/tests/test_llabench_wrapper.py</files>
  <read_first>02-CONTEXT.md decision D2-08; 02-RESEARCH.md "Greedy prompt-runner invocation (Layer 2)" + Pitfall 8; 02-PATTERNS.md S4 headless spawn shape</read_first>
  <action>
    GPU: brief (~5 min — one model load + shortest-prompt smoke).
    Author the six deterministic corpus files (content within planner discretion per D2-08): ASCII-only fixed text, short files roughly 200-800 chars (fits any tier), long files roughly 8k-16k chars (2-4k tokens, fits the 4096 tier upward), two code + four prose covering short/long x code/prose with short_prose_02/long_prose_02 as near-duplicates-of-shape variants for Phase-3 shape pairing. No trailing-whitespace churn; content frozen once committed.
    Create benchmarks/bin/run_prompts.py: CLI --corpus-dir --tier --bin --model --out-dir --gen 128; for each corpus file spawns ONE headless llama-cli invocation per the S4 shape: setsid detachment, PID written to OUT_DIR/run/current.pid, arguments -m MODEL -c TIER --temp 0 --single-turn --simple-io --load-mode none (the modern load-mode spelling — the deprecated mmap spellings warn at this pin) -ngl 99 -f CORPUS_FILE -n GEN, stdout to logs/<stem>.out and stderr to logs/<stem>.err inside a fresh run directory laid out like the result-store layout (manifest.json placeholder, rows.jsonl, logs/, telemetry/, so Layer-2 runs land in the same store discipline as Layer-1); records one enriched row per prompt run carrying prompt filename, its sha256 (corpus drift detection, S2 discipline), tier, gen count, wall time, and exit code; appends via flush+fsync per row. On non-zero exit: record the failure row verbatim and continue to next prompt (publish failures honestly, S6) — no retries.
    Smoke: run the shortest prompt at tier 2048 once; assert output non-empty and row recorded; on success leave a non-empty dotfile sentinel benchmarks/prompts/.smoke_out_check recording the smoke run-id and output path (the exact sentinel path the task's verify step tests with test -s). Extend test_llabench_wrapper.py (or add corpus checks into it) with a corpus-integrity test: the six frozen filenames exist and the count is taken ONLY over *.txt entries (glob the frozen name list, never a bare directory listing) so dotfile sentinels such as .smoke_out_check can never perturb the frozen-six check; sha256s stable across a re-read; long files exceed 8k chars and short files stay under 1k.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && ls benchmarks/prompts/*.txt | wc -l | grep -x 6 && python3 -m pytest benchmarks/tests/test_llabench_wrapper.py -q && test -s benchmarks/prompts/.smoke_out_check && echo PROMPT_SMOKE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly six corpus files at the frozen paths; integrity test green with the count restricted to *.txt so dotfile sentinels cannot affect it
    - Real greedy smoke run completed exit 0 with non-empty decoded output captured under a run directory, and its enriched row landed flushed in that run's rows.jsonl
    - run_prompts.py uses --temp 0, --single-turn, --simple-io, --load-mode none, setsid, and a PID file on every invocation
  </acceptance_criteria>
  <done>BENCH-01's harness surface complete: Layer-1 matrix engine + Layer-2 runner + deterministic corpus, all machine-readable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> llama-bench/llama-cli | argv crossing into pinned binaries; tier/gen values are integers from internal constants |
| corpus files -> prompt runner | file contents become prompt payloads (local, owner-authored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering/Elevation | llabench.build_argv -> subprocess | medium | mitigate | argv built programmatically from integer constants and internal enums; no shell string interpolation anywhere in the module; paths are module constants |
| T-02-01-02 | Repudiation | matrix provenance | low | accept | rows carry build_commit/tool identity from the tool itself; full fingerprint binding lands in plan 02-02's manifest — residual risk noted, closed downstream |
| T-02-01-SC | Tampering | dependency supply chain (pytest install) | low | accept | stdlib-only harness code; sole install is distro pytest from Ubuntu repos (apt, signed); no pip/npm/cargo registry packages introduced |
</threat_model>

<verification>
- Guest quick suite green: python3 -m pytest benchmarks/tests/ -x -q
- Real-binary tracer evidence: 2-row smoke + shortest-prompt greedy smoke both archived under run directories
- Wrapper golden-argv assertions prove explicit-cell enforcement (D2-06), --delay spelling, per-tier batching, D2-19 order
</verification>

<success_criteria>
A different executor could run the full 16-cell HIP matrix tomorrow using llabench.py alone, and the published output could contain no cell the wrapper did not deliberately define.
</success_criteria>

<output>
Create `.planning/phases/02-benchmark-harness-baseline-matrix/02-01-SUMMARY.md` when done
</output>
