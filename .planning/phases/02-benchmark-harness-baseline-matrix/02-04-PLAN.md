---
phase: 2
plan: 02-04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - benchmarks/bin/run_session.py
  - benchmarks/bin/calibrate.py
  - benchmarks/bin/publish_matrix.py
  - benchmarks/tests/test_matrix_assembly.py
  - benchmarks/tests/smoke_matrix.sh
  - benchmarks/RUNBOOK.md
autonomous: true
requirements: [BENCH-01, BENCH-03, BENCH-04]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One command runs a guarded, fingerprinted, checksummed benchmark session end-to-end: manifest -> pre-flight -> setsid'd invocation with PID file -> live guard -> enriched fsynced rows -> CHECKSUMS.sha256 -> owner toasts (D2-15/D2-16)"
    - "Calibration has empirically derived RSS/spill thresholds from healthy profiles, mapped this card's HWiNFO sensor labels, rehearsed the thermal kill at a safe threshold, and executed the ONE supervised near-OOM run (D2-13/D2-14) with results written into RUNBOOK.md"
    - "The published stock HIP baseline matrix spans pp/tg x ctx {4k,8k,16k,32k} x fa {off,on} in the fixed D2-19 order with mean ± stdev per cell from r=5 repeats, every row naming backend HIP"
    - "A re-run of one tier reproduces within the ±5% gate (BENCH-01 success criterion evidence), and any FAILED cell is published with reason verbatim rather than hidden (S6)"
  artifacts:
    - path: "benchmarks/bin/run_session.py"
      provides: "session orchestrator wiring all wave-1 libraries"
    - path: "benchmarks/bin/calibrate.py"
      provides: "label map, threshold derivation, kill rehearsal, supervised near-OOM"
    - path: "benchmarks/bin/publish_matrix.py"
      provides: "D2-19-ordered aggregate publisher excluding-but-publishing FAILED cells"
    - path: "benchmarks/config/thresholds.json"
      provides: "empirical guard thresholds (written by calibration)"
    - path: "benchmarks/results/BASELINE-MATRIX.md"
      provides: "published stock HIP baseline matrix"
  key_links:
    - from: "benchmarks/bin/run_session.py"
      to: "benchmarks/lib/{llabench,fingerprint,guard,store,preflight,toast}.py + host daemons"
      via: "imports of the locked public contracts from plans 02-01/02/03"
      pattern: "import"
    - from: "benchmarks/config/thresholds.json"
      to: "benchmarks/lib/guard.py Thresholds.from_json"
      via: "calibration output consumed by guard at session start"
      pattern: "thresholds"
---

<objective>
Wire the phase together and produce the deliverable: the session orchestrator (tracer for the whole architecture), the calibration session that turns guesswork into empirical thresholds, and the published stock HIP baseline matrix itself.

Purpose: phases 3-6 measure everything against these numbers; a contaminated or unguarded matrix poisons the entire project.
Output: run_session.py + calibrate.py + publish_matrix.py, thresholds.json, filled RUNBOOK.md, BASELINE-MATRIX.md under benchmarks/results/.
GPU COST (owner-visible): Task 1 ~10 min; Task 2 ~60-90 min incl. owner-supervised near-OOM window; Task 3 ~1.5-2.5 h + ~15 min repro re-run. Toasts fire on failures and at session close per D2-15.
</objective>

<execution_context>
@C:/Users/arhan/.pi/agent/gsd-core/workflows/execute-plan.md
@C:/Users/arhan/.pi/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-benchmark-harness-baseline-matrix/02-CONTEXT.md
@.planning/phases/02-benchmark-harness-baseline-matrix/02-RESEARCH.md
@.planning/phases/02-benchmark-harness-baseline-matrix/02-PATTERNS.md
@.planning/phases/02-benchmark-harness-baseline-matrix/02-VALIDATION.md
@benchmarks/environment/vram-probe.txt

Wave-1 contracts available (locked by plans 02-01/02/03): llabench.{build_argv,run_invocation,parse_rows,assert_tier_rows,enumerate_tiers,repro_ok}; fingerprint.collect_manifest/skew_check; guard.Thresholds.from_json/poll_proc/evaluate; store.RunStore; preflight.estimate_needed_mib/check/record_ledger; toast.send/send_summary; host daemons hwinfo_daemon --watch/--label-map and thermal_watchdog --pid-file.
Session facts: one invocation per tier caps OOM blast radius at 4 cells; warmup doubles prefill wall-clock (session budget 1.5-2.5 h); --delay sleeps before each test (16 cells x 30 s = ~8 min); HWiNFO non-Pro shared memory has a 12 h/day duty cycle — schedule both arms inside it; librocdxg#60 busy-spin fingerprint observation + optional taskset isolation decision recorded during calibration; stock tune-state re-verified at execution start feeds manifest tune_state (D2-17).
</context>

## Artifacts this phase produces (this plan)
New files: benchmarks/bin/run_session.py · benchmarks/bin/calibrate.py · benchmarks/bin/publish_matrix.py · benchmarks/tests/test_matrix_assembly.py · benchmarks/tests/smoke_matrix.sh · benchmarks/config/thresholds.json · filled benchmarks/RUNBOOK.md sections
Generated result dirs (in-repo, append-only): benchmarks/results/<YYYYMMDD_HHMMSS>_<run-id>/ each with manifest.json, rows.jsonl, telemetry/, logs/, CHECKSUMS.sha256; plus benchmarks/results/BASELINE-MATRIX.md + .json
Commands introduced: `python3 benchmarks/bin/run_session.py ...` · `python3 benchmarks/bin/calibrate.py {labels|profile|rehearse-kill|near-oom}` · `bash benchmarks/tests/smoke_matrix.sh` · `python3 benchmarks/bin/publish_matrix.py ...`

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — full-pipeline smoke session on one tiny cell set</name>
  <precondition>Wave-1 modules importable in guest and pinned binaries + model present (guaranteed by depends_on; assert file existence before starting)</precondition>
  <files>benchmarks/bin/run_session.py, benchmarks/tests/smoke_matrix.sh</files>
  <read_first>Wave-1 SUMMARYs for public contracts; 02-CONTEXT.md decisions D2-15/D2-16/D2-17; 02-PATTERNS.md S3 objective pass predicates + S4 spawn shape</read_first>
  <action>
    GPU: ~10 min (one model load, tiny cells).
    Create benchmarks/bin/run_session.py orchestrating EXACTLY this sequence: acquire flock on benchmarks/results/.session.lock non-blocking (exit 5 if another session holds it); mkdir run dir via store.RunStore.create; fingerprint.collect_manifest(backend_arm=HIP, telemetry_mode=<detected from daemon mode.txt if reachable else absent>); spawn host-side helpers when available (hwinfo_daemon --watch --pid-file, thermal_watchdog --pid-file --threshold-c 95) via powershell.exe interop, recording their absence gracefully otherwise; for each TierPlan from llabench.enumerate_tiers(args.tiers): preflight.check logged for every tier (mandatory gate behavior only differs at 32768 where a FAIL produces a FAILED:preflight-oom row and skips allocation — D2-18); write child PID file then llabench.run_invocation with stdout to rows.raw.jsonl and stderr to logs/bench_c<tier>.txt while guard.poll_proc threads on the child PID; parse+enrich rows (backend_arm, run_id, guard verdict object, telemetry_slice reference, tier label) into store.append_row per cell; toast on any non-OK verdict; after all tiers: store.write_checksums(), append index entry, toast_summary(ok_count, failed_count). Flags: --tiers, --delay 30, --repeats 5, --smoke (tiny 1024-cell override for this task). NO retry loops anywhere; a failed invocation records its failure row and moves to next tier (fail-fast per cell-set, S5/S6).
    smoke_matrix.sh wraps `run_session.py --smoke` as the VALIDATION.md full-suite GPU leg, asserting enumerated predicates post-run: run dir contains manifest.json with all fields, rows.jsonl line count equals expected tiny-cell count, zero contaminating signatures across rows, CHECKSUMS verifies via sha256sum -c exit 0.
    Tracer proof: execute the smoke session for real; keep its run dir as evidence.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && bash benchmarks/tests/smoke_matrix.sh'</automated>
  </verify>
  <acceptance_criteria>
    - Smoke run dir contains manifest.json + fsynced rows.jsonl + telemetry artifacts (or explicit mode markers) + verifying CHECKSUMS.sha256
    - Session lock held exclusively during run; concurrent second invocation exits 5 without touching state
    - Toast summary fired (or explicitly recorded as unavailable) at session close
  </acceptance_criteria>
  <done>The entire phase architecture proven against real hardware in miniature</done>
</task>

<task type="auto">
  <name>Task 2: Calibration session — labels, thresholds, thermal rehearsal, supervised near-OOM (per D2-13, D2-14, D2-20)</name>
  <precondition>GPU idle and cool at session start; owner available at end of task for the supervised near-OOM window (destructive-by-design per D2-14)</precondition>
  <files>benchmarks/bin/calibrate.py, benchmarks/config/thresholds.json, benchmarks/RUNBOOK.md</files>
  <read_first>02-CONTEXT.md decisions D2-13/D2-14/D2-20 + Claude's Discretion (calibration structure); 02-RESEARCH.md Open Question 1 (sensor labels) + Pitfall 5 (12 h/day cap) + librocdxg#60 note; 02-VALIDATION.md manual-only verifications list</read_first>
  <action>
    GPU: ~60-90 min total; the final sub-step is owner-supervised.
    Create benchmarks/bin/calibrate.py with subcommands: labels — run hwinfo_daemon --label-map dumping the full reading inventory into the calibration run dir resolving RESEARCH open question 1, recording exact matched labels per mandatory field; profile — run healthy guarded sessions across modest tiers (4096 and 8192) collecting RssProfile series and shared-GPU-memory series, then derive thresholds at ~1.5x steady-state margins per D2-13 writing benchmarks/config/thresholds.json {vmrss_fail_kb, vmswap_fail_kb, gpu_shared_climb_mb_per_min, repeat_deviation_max_ratio:2.0, derived_from:<run-id>, utc} — no other writer may ever create that file; rehearse-kill — arm thermal_watchdog with --threshold-c 60 against a dummy sleeper PID file, prove the kill fires, verdict marker lands, toast sends, then record PASS evidence (this satisfies the safe-threshold end-to-end rehearsal without real heat); near-oom — candidates per D2-14: tier 32768 with flash-attn off, or oversized batch override; run it supervised with guard armed expecting either a clean FAILED:suspected-spill/pre-flight-oom publication or a live trip; record which happened with full evidence triple.
    Fill RUNBOOK.md placeholder sections in dated-amendment style (versions.txt precedent): §thresholds (values + derivation run-id), §telemetry-modes (label map summary + shmem duty-cycle ceiling note: schedule sessions within HWiNFO non-Pro 12 h/day), §session-protocol (cool/idle start, continuous telemetry, clock-deviation flagging >5% from session median, hotspot >=105 C flags, cooldown gaps between heavy tiers, batched per-tier invocations, fixed D2-19 order — D2-16 operationalized), §thermal-policy (firmware backstop, watchdog always armed, supervised-override procedure), §busy-spin-fingerprint (librocdxg#60 observation from calibration CPU readings; taskset isolation decision for decode measurements recorded as applied-or-rejected with rationale).
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && test -s benchmarks/config/thresholds.json && grep -c "AMENDMENT\|thresholds" benchmarks/RUNBOOK.md'</automated>
    <human-check>Owner confirms during the near-OOM window: they were present, observed the deliberate overcommit attempt, and accept whether it ended clean-fail (row marked FAILED, system alive) or as a live guard trip; RUNBOOK near-OOM evidence line matches what they saw.</human-check>
  </verify>
  <acceptance_criteria>
    - thresholds.json exists with numeric values and a derivation run-id reference — created only by this calibration
    - Thermal kill rehearsal evidence archived: dummy process killed, FAILED:thermal-abort marker written, toast sent
    - Exactly ONE near-OOM execution performed and its outcome (clean-fail or live trip) recorded verbatim in RUNBOOK with run-id
    - Label map archived showing concrete matched labels for all nine mandatory fields (or documented gaps)
  </acceptance_criteria>
  <done>D2-13/D2-14 satisfied empirically; D2-20 kill path proven live; RUNBOOK becomes the binding protocol document</done>
</task>

<task type="auto">
  <name>Task 3: HIP baseline matrix execution + publish (per D2-16..D2-19)</name>
  <precondition>thresholds.json exists from Task 2; GPU cool/idle; HWiNFO duty-cycle budget available (12 h/day non-Pro ceiling)</precondition>
  <files>benchmarks/bin/publish_matrix.py, benchmarks/tests/test_matrix_assembly.py, benchmarks/results/BASELINE-MATRIX.md</files>
  <read_first>benchmarks/RUNBOOK.md (now-binding protocol); 02-CONTEXT.md decisions D2-07/D2-11/D2-17/D2-18/D2-19; 02-RESEARCH.md Pitfall 3 (warmup wall-clock) and Pitfall 4 (OOM blast radius)</read_first>
  <action>
    GPU: ~1.5-2.5 h continuous session + ~15 min repro re-run. Start cool/idle per protocol; re-verify stock tune-state at start (visual Adrenalin check + driver fingerprint) feeding manifest tune_state (D2-17).
    Execute the FULL canonical matrix through run_session.py: all four TIERS ascending, each tier one invocation with -p tier, explicit gen-vector zeroing, decode-at-context pairs, flash-attn sequence off-then-on, repeats 5, warmup on, delay gaps between heavy tiers (D2-19 order identical for every future session). The 32k tier passes through the mandatory pre-flight: predicted-over-budget publishes FAILED:preflight-oom rows and moves on (expected-fail still published, D2-18); probe-passing executes fully guarded like any cell.
    Create publish_matrix.py: reads run dirs listed on CLI (or --latest-per-arm), aggregates per-cell mean and stdev from raw samples_ts arrays (raw rows retained forever per D2-11; aggregates computed only here), enforces D2-19 ordering in output, EXCLUDES non-OK verdict rows from the clean table while publishing them in a failed-cells table with reason verbatim, carries REVIEW-flagged rows into a review section, scans the published set for contaminating default-cell signatures before writing anything, and emits benchmarks/results/BASELINE-MATRIX.md + .json where every row names its backend arm. Every performance claim in the document states backend: HIP.
    Reproducibility evidence (BENCH-01): re-run ONE tier (8192) in a fresh session after cool-down; compute variance via llabench.repro_ok between session means per cell; record the comparison in BASELINE-MATRIX.md; >5% deviation fails the gate and is investigated before phase verification.
    test_matrix_assembly.py: synthetic multi-session fixture rows exercise aggregation math (mean/stdev from samples arrays), D2-19 ordering of emitted tables, exclusion-but-publication of FAILED rows, review-row routing, and contamination-scan rejection.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_matrix_assembly.py -x -q && grep -c "HIP" benchmarks/results/BASELINE-MATRIX.md'</automated>
    <human-check>Owner eyeballs BASELINE-MATRIX.md: 16-row clean table (or documented expected-fails at 32k), plausible magnitudes vs Phase-1 anchors (~111 tok/s pp class, ~33 tok/s tg class at small context), failed cells carry reasons.</human-check>
  </verify>
  <acceptance_criteria>
    - Published matrix covers 4 tiers x {pp,pg} x {fa off,on} = 16 cells or fewer with explicit FAILED/review justification for every missing cell
    - Repro re-run recorded with per-cell variance percentages and an explicit PASS/FAIL against the 5% gate
    - All rows name backend HIP; no contaminating signature present (publisher scan green)
    - Matrix assembly tests green; every published number traceable to a run-id whose CHECKSUMS.sha256 verifies
  </acceptance_criteria>
  <done>BENCH-04 HIP half delivered; BENCH-01 reproducibility evidenced; stock baseline exists to beat or regress against forever</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> guest/host processes | PID files and kill commands cross the WSL boundary |
| calibration -> thresholds.json | derived config gates every future session's guard |
| matrix publisher | decides what numbers become citable project baselines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | DoS | supervised near-OOM run (WSL#40732 hard-crash pattern) | high | mitigate | human-check supervision required (owner present), pre-flight gate runs first, exactly one execution, fail-fast no retries |
| T-02-04-02 | DoS | thermal excursion during long sessions | medium | mitigate | watchdog armed every session (rehearsed at calibration), firmware backstop trusted per D2-20, hotspot flagging in protocol |
| T-02-04-03 | Tampering | concurrent sessions corrupting stores | medium | mitigate | flock exclusive session lock with distinct exit code; append-only run dirs keyed by UTC timestamp |
| T-02-04-04 | Repudiation | mis-aggregated published numbers | medium | mitigate | publisher computes from raw samples arrays only, contamination scan blocks output, every figure traceable to checksummed run dir |
| T-02-04-SC | Tampering | dependency supply chain | low | accept | stdlib-only orchestration; no package installs in this plan |
</threat_model>

<verification>
- Full unit suite green including test_matrix_assembly.py
- smoke_matrix.sh (GPU leg of VALIDATION.md full suite) exits 0 with complete run-dir evidence
- thresholds.json + RUNBOOK amendments present; near-OOM outcome recorded; BASELINE-MATRIX.md published with repro-gate verdict
</verification>

<success_criteria>
The stock HIP baseline matrix is published, guarded, fingerprinted, reproducible within 5%, and honest about every failure — ready for Phase 3 to rank bottlenecks against it.
</success_criteria>

<output>
Create `.planning/phases/02-benchmark-harness-baseline-matrix/02-04-SUMMARY.md` when done
</output>
