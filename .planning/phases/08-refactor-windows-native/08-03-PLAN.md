---
phase: 08-refactor-windows-native
plan: "03"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - build_windows.bat
  - README.md
  - .vscode/settings.json
autonomous: true
requirements:
  - REQ-WIN-03
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "build_windows.bat compiles clean on Windows 11 + VS Build Tools + HIP SDK via hipcc (clang), not cl, for .hip files"
    - "AMDGCN intrinsics __builtin_amdgcn_sudot4, __builtin_amdgcn_perm, __builtin_amdgcn_wmma_f32_16x16x16_f16_w32 compile without MSVC errors when targeting gfx1100"
    - "Ninja is the generator; MSBuild path is rejected/failed-fast"
  artifacts:
    - build_windows.bat
  key_links:
    - "build_windows.bat -> HIP_PATH/bin/clang++.exe -> cmake -G Ninja -> --offload-arch=gfx1100 -> llama-server.exe"
    - "Kernel .hip -> hipcc -> __builtin_amdgcn_* -> gfx1100 ISA"
---

<objective>
Provide foolproof build_windows.bat using cmake + ninja + Clang from AMD HIP SDK (HIP_PATH) targeting gfx1100, ensuring AMDGCN intrinsics compile via hipcc not cl — per REQ-WIN-03 (OBJ-3).

Purpose: Make Windows native build reproducible for any operator with HIP SDK + VS Build Tools.
Output: build_windows.bat that compiles llama-server.exe clean for gfx1100.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-refactor-windows-native/08-CONTEXT.md
@kernels/common/block_iq4_xs.h
@kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
@kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build_windows.bat (foolproof, no MSVC for HIP)</name>
  <files>build_windows.bat</files>
  <action>
    Create `build_windows.bat` at repo root (ANSI-safe, CRLF, `@echo off`, `setlocal EnableDelayedExpansion`). Contract per 08-CONTEXT.md specifics: 1) Resolve repo root via `%~dp0`, 2) Handle spaces in HIP_PATH: default `if not defined HIP_PATH set "HIP_PATH=C:\Program Files\AMD\ROCm\6.2"` with quotes, allow override `set "HIP_PATH=%HIP_PATH%"`, 3) Fail-fast checks with `where cmake` / `where ninja` / `if not exist "%HIP_PATH%\bin\clang++.exe"` → `echo ERROR: ... & exit /b 1`, 4) Validate VS Build Tools via `where cl` or `vswhere` hint, but do NOT use cl for HIP — set `CMAKE_HIP_COMPILER="%HIP_PATH%\bin\clang++.exe"` and `CMAKE_CXX_COMPILER=cl` implicitly, 5) Configure: `cmake -S "%~dp0" -B "%~dp0build-windows" -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_HIP_COMPILER="%HIP_PATH%\bin\clang++.exe" -DCMAKE_HIP_ARCHITECTURES=gfx1100 -DGGML_HIP=ON -DLLAMA_BUILD_SERVER=ON -DGGML_CUDA_ENABLE_CUSTOM_GFX1100=ON` (plus `CMAKE_PREFIX_PATH="%HIP_PATH%\lib\cmake\hip"` if needed), 6) Build: `cmake --build "%~dp0build-windows" --config Release --parallel`, 7) Post-check: locate `llama-server.exe` at `build-windows\bin\llama-server.exe` or `build-windows\llama-server.exe` or `build-windows\llama.cpp\build\bin\...` — echo found path and `dir` size, else `exit /b 1`. Include `timeout`-like behavior via `cmake --build` native timeout and document `set HIP_PATH` override. Add comments explaining why hipcc not cl for __builtin_amdgcn_* (clang builtins, not MSVC). Provide optional `BUILD_TYPE=Debug` env override and `MODEL_PATH` note. Per D-03, never invoke `cl` on `.hip` — all HIP files go via `clang++.exe --offload-arch=gfx1100`.
  </action>
  <verify>
    <automated>bash -c "cat build_windows.bat | head -n 90; echo '---'; grep -ci 'HIP_PATH' build_windows.bat; grep -ci 'clang++.exe' build_windows.bat; grep -ci 'Ninja' build_windows.bat; grep -ci 'gfx1100' build_windows.bat; grep -ci 'offload-arch' build_windows.bat; echo '---check no cl for hip'; grep -c 'cl.*\.hip' build_windows.bat || echo '0 bad cl-hip lines (good)'" --timeout 15</automated>
  </verify>
  <done>build_windows.bat exists, references HIP_PATH/bin/clang++.exe, -G Ninja, -DCMAKE_HIP_ARCHITECTURES=gfx1100, --offload-arch=gfx1100, fail-fast on missing cmake/ninja/clang, post-checks for llama-server.exe, and comments explain hipcc vs cl for builtins</done>
</task>

<task type="auto">
  <name>Task 2: Windows HIP toolchain validation + intrinsic probe</name>
  <files>kernels/matmul_iq4xs/check_intrinsics.hip, README.md</files>
  <action>
    Create minimal Windows intrinsic probe `kernels/matmul_iq4xs/check_intrinsics.hip` (or `kernels/check_intrinsics.hip`):
    ```cpp
    #include <hip/hip_runtime.h>
    __global__ void probe(int* out){ int a=0; a = __builtin_amdgcn_sudot4(a,a,a, false); *out = a; }
    ```
    plus a second probe for `__builtin_amdgcn_perm` and `__builtin_amdgcn_wmma_f32_16x16x16_f16_w32` (guarded with `#if __gfx1100__`). Document validation steps in `README.md` Windows section: prerequisites (Windows 11, VS C++ Build Tools 2022, AMD HIP SDK 6.2 with HIP_PATH, cmake 3.21+, ninja), how to run `build_windows.bat` (double-click or `cmd /c build_windows.bat`), expected output (`--offload-arch=gfx1100`, `Built target llama-server`), and troubleshooting table: R-01 HIP_PATH divergence (check env), R-02 Ninja vs MSBuild (must use Ninja), R-03 builtin not found (ensure `--offload-arch=gfx1100` and `CMAKE_HIP_COMPILER` is clang++.exe), R-04 amdgpu-arch (pinned gfx1100), R-09 CRLF patch (core.autocrlf false). Update README stock vs custom table note: Windows native replaces WSL ROCm 7.2.1 path. Provide smoke compile command for probe: `"%HIP_PATH%\bin\clang++.exe" --offload-arch=gfx1100 -c kernels/matmul_iq4xs/check_intrinsics.hip -o /tmp/probe.o` and `llvm-objdump --mcpu=gfx1100 probe.o | grep -E "v_dot4|v_wmma|perm"` expected. If on WSL/Linux CI, probe compile is dry-parse only; on Windows 11 bare metal it must succeed.
  </action>
  <verify>
    <automated>bash -c "ls -l kernels/matmul_iq4xs/check_intrinsics.hip 2>&1 | head -n 5; echo '---'; grep -c 'sudot4' kernels/matmul_iq4xs/check_intrinsics.hip; grep -c 'wmma' kernels/matmul_iq4xs/check_intrinsics.hip; grep -c 'perm' kernels/matmul_iq4xs/check_intrinsics.hip; echo '---'; grep -n 'build_windows' README.md | head -n 10" --timeout 15</automated>
  </verify>
  <done>check_intrinsics.hip exists and mentions all three builtins (sudot4, perm, wmma); README.md contains Windows prerequisites + build_windows.bat usage + troubleshooting for R-01..R-04/R-09; dry-parse grep confirms intrinsics would be compiled via clang --offload-arch=gfx1100, not cl</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_windows.bat -> HIP SDK filesystem | HIP_PATH may contain spaces, untrusted env |
| .hip source -> clang builtins | Builtins only available under hipcc/clang, not MSVC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Spoofing | HIP_PATH env | medium | mitigate | Quote paths, validate existence of clang++.exe before cmake; no auto-download |
| T-08-03-02 | Denial of Service | generator mismatch (MSBuild vs Ninja) | high | mitigate | Mandate -G Ninja, fail-fast if ninja missing; document MSBuild trap explicitly |
| T-08-03-03 | Tampering | __builtin_amdgcn_* misuse | high | mitigate | Probe file proves builtins compile; CMake forces HIP compiler to clang++.exe |
| T-08-03-SC | Tampering | hip/clang package | high | mitigate | No package install in this plan; HIP SDK is user_setup external |
</threat_model>

<verification>
- `type build_windows.bat` or `cat build_windows.bat` shows HIP_PATH quoted, Ninja, gfx1100, offload-arch, and no `cl *.hip`.
- `findstr /C:"clang++.exe" build_windows.bat` (or grep) finds HIP compiler.
- `kernels/matmul_iq4xs/check_intrinsics.hip` compiles on Windows via `%HIP_PATH%\bin\clang++.exe --offload-arch=gfx1100 -c` (or at least parses without cl error).
- `README.md` Windows section lists HIP SDK + VS Build Tools + ninja + gfx1100 and build_windows.bat steps.
</verification>

<success_criteria>
REQ-WIN-03 satisfied: build_windows.bat compiles clean on Windows 11 + VS Build Tools + HIP SDK targeting gfx1100; intrinsics compile via hipcc not cl; Ninja mandated.
</success_criteria>

<output>
Create `.planning/phases/08-refactor-windows-native/08-03-SUMMARY.md` when done
</output>
