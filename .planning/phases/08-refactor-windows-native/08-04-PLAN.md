---
phase: 08-refactor-windows-native
plan: "04"
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - README.md
  - models/README.md
autonomous: false
requirements:
  - REQ-WIN-04
estimate:
  tokens: 38000
  raw_tokens: 19000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Clean minimal tree: only kernels/, patches/, CMakeLists.txt, build_windows.bat, README/LICENSE/NOTICE/models/README remain (plus llama.cpp source via submodule/FetchContent)"
    - "llama-server.exe builds via build_windows.bat and runs: curl localhost:8000/v1/chat/completions returns 200 with choices[0].message.content"
    - "Kernels still pass cosine check (cosine >=0.999 vs ref_cpu) on Windows HIP"
  artifacts:
    - build-windows/bin/llama-server.exe
    - README.md
  key_links:
    - "Kernel cosine PASS -> llama-server build -> localhost:8000 OpenAI API 200"
    - "Patch apply -> CMake gfx1100 -> intrinsics compile -> server serves"
---

<objective>
Integrate patch, smoke-test Windows build, and verify llama-server.exe serves OpenAI API at localhost:8000 per REQ-WIN-04 (OBJ-4).

Purpose: Prove the refactored bloat-free repo is not just smaller but actually runs inference on Windows native.
Output: Built llama-server.exe, cosine-validated kernels, and verified /v1/chat/completions 200 on port 8000.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-refactor-windows-native/08-CONTEXT.md
@kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
@kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
@patches/0001-gfx1100-mul-mat-custom.patch
@build_windows.bat
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch integration + build llama-server.exe</name>
  <files>README.md, models/README.md</files>
  <action>
    On Windows 11, run `git -C llama.cpp apply --check ../patches/0001-gfx1100-mul-mat-custom.patch` with `--whitespace=fix` and `git config core.autocrlf false` to handle CRLF. If llama.cpp was fetched via FetchContent, apply patch via `patch -p1 < patches/0001-gfx1100-mul-mat-custom.patch` or CMake PATCH_COMMAND. Then execute `build_windows.bat` (timeout 600s). Verify: `llama-server.exe` exists at `build-windows\bin\llama-server.exe` or `build-windows\llama-server.exe` (check both via `dir /s llama-server.exe`), size > 10MB, and `llama-server.exe --help` shows `--host`/`--port`. Document model path handling: `models\Qwen3.8-27B-Uncensored-IQ4_XS.gguf` relative vs `MODEL_PATH` env absolute; quote paths with spaces. Update `models/README.md` to state Windows path, sha256 `53adc4bb…`, and that `/root/models` / `/mnt/e/...` WSL paths are obsolete. If GEMM was disabled (`return false`), keep it disabled for this smoke — document re-enable audit as follow-up. Capture build log to `logs\build-windows.log` (create logs/ minimally for build output, but gitignore it).
  </action>
  <verify>
    <automated>bash -c "ls -lh build-windows/bin/llama-server.exe 2>&1 | head -n 5; ls -lh build-windows/llama-server.exe 2>&1 | head -n 5; find build-windows -name 'llama-server.exe' -type f 2>&1 | head -n 5; cat logs/build-windows.log 2>&1 | tail -n 30" --timeout 15</automated>
  </verify>
  <done>llama-server.exe exists, size sanity >10MB, --help shows --host/--port, build log captured, patch applied clean with no rejects, models/README.md updated for Windows paths</done>
</task>

<task type="auto">
  <name>Task 2: Kernel cosine smoke on Windows HIP</name>
  <files>kernels/matmul_iq4xs/test_gemv_dp4a_compare.cpp, kernels/matmul_iq4xs/test_gemm_wmma_compare.cpp</files>
  <action>
    Build and run the minimal kernel tests on Windows HIP (via build-windows or standalone `cmake -S kernels -B kernels/build-windows -G Ninja -DCMAKE_HIP_COMPILER="%HIP_PATH%\bin\clang++.exe" -DCMAKE_HIP_ARCHITECTURES=gfx1100`): `./kernels/build-windows/test_gemv_dp4a_compare` must report `10/10 PASS cosine >=0.999` (or 1.000) and `./test_gemm_wmma_compare` must report `15 shapes cosine >=0.999`. Use the same `compute_metrics` from `matmul_test_util.h` (max_abs, mean_abs, cosine). If Windows bare metal not available in CI, at least prove `hipcc --save-temps` + `llvm-objdump --mcpu=gfx1100` shows `v_dot4`, `v_perm`, `v_wmma` in disasm for the two winner objects. Document VRAM sanity: 20GB card, driver 32.0.31041.1004, no `HSA_ENABLE_DXG_DETECTION` on Windows.
  </action>
  <verify>
    <automated>bash -c "cat kernels/matmul_iq4xs/test_gemv_dp4a_compare.cpp 2>&1 | grep -c 'cosine'; echo '---'; ls -l kernels/build-windows/test_gemv_dp4a_compare* 2>&1 | head -n 10; echo '---try run if on Windows'; kernels/build-windows/test_gemv_dp4a_compare.exe 2>&1 | tail -n 20 || echo 'not on Windows bare metal — disasm proof pending'" --timeout 30</automated>
  </verify>
  <done>Kernels pass cosine gate on Windows HIP; or disasm shows v_dot4/v_wmma primitives; no regression from Phase 7 hybrid (0.99998+)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Server smoke — OpenAI API at localhost:8000</name>
  <files>README.md</files>
  <action>
    Human runs on Windows 11 bare metal (RX 7900 XT, driver 32.0.31041.1004, 20GB VRAM): `build-windows\bin\llama-server.exe -m models\Qwen3.8-27B-Uncensored-IQ4_XS.gguf --host 127.0.0.1 --port 8000 --n-gpu-layers 99 -c 2048` (quote path if spaces). Wait for `Server listening on http://127.0.0.1:8000`. In second terminal: `curl http://127.0.0.1:8000/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\":\"qwen\",\"messages\":[{\"role\":\"user\",\"content\":\"Hi\"}]}"` must return HTTP 200 with JSON `choices[0].message.content` non-empty (coherent, not truncated ~5 tok). Capture `curl -v` output and `llama-server` startup log (showing gfx1100, VRAM offload 99 layers, no CPU fallback). Screenshot or log snippet suffices. If model not present, test with `--model` missing should fail fast with clear error, not silent hang. Document in README.md the exact Windows command and expected 200 response.
  </action>
  <verify>
    <human-check>curl 127.0.0.1:8000/v1/chat/completions returns 200; llama-server log shows gfx1100 + 99 layers offloaded; README documents Windows model path (no /root/models)</human-check>
  </verify>
  <done>Human attests server serves OpenAI API at localhost:8000 with 200; README contains copy-paste Windows commands that work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llama-server.exe -> localhost:8000 | Untrusted HTTP input crosses here |
| model file -> VRAM | 15GB GGUF load trusts file sha256 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Spoofing | localhost:8000 API | low | accept | Localhost only, no auth required for smoke; production would add token |
| T-08-04-02 | Tampering | model GGUF path | medium | mitigate | Verify sha256 53adc4bb… before load; quoted MODEL_PATH prevents injection |
| T-08-04-03 | Denial of Service | VRAM OOM (20GB) | medium | mitigate | Smoke uses -c 2048; document -c 4096 ceiling and OOM fail-fast |
| T-08-04-04 | Information Disclosure | patch provenance | low | mitigate | Patch commits retain author + bb4caa75 base |
| T-08-04-SC | Tampering | llama.cpp fetch | high | mitigate | Pinned bb4caa75 via GIT_TAG; if FetchContent hash missing mark [ASSUMED] and gate with human verify |
</threat_model>

<verification>
- `build-windows/**/llama-server.exe --help` shows --host/--port/--n-gpu-layers.
- `test_gemv_dp4a_compare` and `test_gemm_wmma_compare` exit 0 with cosine >=0.999.
- Human `curl http://127.0.0.1:8000/v1/chat/completions` returns 200 with choices[0].message.content non-empty.
- `README.md` documents minimal tree (`kernels/`, `patches/`, `CMakeLists.txt`, `build_windows.bat`) and Windows server command.
- Minimal tree `find . -type f ! -path "./.git/*" ! -path "./llama.cpp/*" ! -path "./build-windows/*" | sort` matches allowlist (no benchmarks/, no .py, no .mjs).
</verification>

<success_criteria>
REQ-WIN-04 satisfied: clean minimal tree, llama-server.exe built via build_windows.bat, serves OpenAI API at localhost:8000 (200), kernels still pass cosine >=0.999 on gfx1100.
</success_criteria>

<output>
Create `.planning/phases/08-refactor-windows-native/08-04-SUMMARY.md` when done
</output>
