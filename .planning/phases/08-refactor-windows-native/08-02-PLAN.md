---
phase: 08-refactor-windows-native
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - allowlist.yaml
  - cmake/toolchain-hip-gfx1100.cmake
  - .gitattributes
  - CMakeLists.txt
  - kernels/CMakeLists.txt
  - kernels/common/CMakeLists.txt
  - kernels/matmul_iq4xs/CMakeLists.txt
  - patches/0001-gfx1100-mul-mat-custom.patch
  - scripts/apply_patch.sh
autonomous: true
requirements:
  - REQ-WIN-02
estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "kernels/ contains only IQ4_XS essentials per D-02: block_iq4_xs.h, hip_helpers.h, bench.h, matmul_test_util.h, impl_gemv_dp4a_gfx1100.hip, impl_gemm_wmma_stream.hip, ref_cpu.h/.cpp and their minimal CMake"
    - "Root CMakeLists.txt exists and configures gfx1100 HIP target with혀 HIP_PATH-aware find_package, no amdgpu-arch auto-detect"
    - "Clean patch still applies: git apply --check patches/0001-gfx1100-mul-mat-custom.patch passes on fresh llama.cpp@bb4caa75"
  artifacts:
    - CMakeLists.txt
    - kernels/common/block_iq4_xs.h
    - kernels/common/hip_helpers.h
    - kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
    - kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
    - patches/0001-gfx1100-mul-mat-custom.patch
  key_links:
    - "Root CMakeLists -> kernels/CMakeLists -> matmul_iq4xs/CMakeLists -> HIP arch gfx1100"
    - "Patch header -> ggml/src/ggml-cuda/custom_gfx1100/ -> CMAKE_HIP link"
---

<objective>
Prune kernels/ to IQ4_XS essentials and provide minimal root CMake for Windows HIP per REQ-WIN-02 (OBJ-2).

Purpose: Reduce tree to the pure acceleration core that justifies its keep; ensure build system is Windows-native and HIP_PATH-aware.
Output: Minimal kernels/ + root CMakeLists.txt + preserved clean patch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-refactor-windows-native/08-CONTEXT.md
@kernels/CMakeLists.txt
@kernels/common/block_iq4_xs.h
@kernels/common/hip_helpers.h
@kernels/common/bench.h
@kernels/common/matmul_test_util.h
@kernels/matmul_iq4xs/CMakeLists.txt
@patches/0001-gfx1100-mul-mat-custom.patch
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune kernels/ to essentials</name>
  <files>kernels/CMakeLists.txt, kernels/common/CMakeLists.txt, kernels/matmul_iq4xs/CMakeLists.txt, kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip, kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip</files>
  <action>
    Delete per `allowlist.yaml` (single source per Thermos fix #1, not prose): `kernels/demo_iq4xs_dequant/` (entire), `kernels/template/` (entire), `kernels/fixtures/` — **delete whole dir entirely** (no `if referenced` branch; `manifest_*.json` 2KB is regeneratable via `gguf-py` per `08-CONTEXT`, decision is explicit: delete dir, no stub). `kernels/results` symlink, and under `kernels/matmul_iq4xs/`: `stock_hip_comparator.hip`, `real_stock_dp4a_comparator.hip`, `impl_gemv_gfx1100.hip`, `impl_gemm_wmma.hip`, `bench_gemv.cpp`, `bench_gemm.cpp`, `bench_matmul.cpp`, `bench_real_stock.cpp`, `test_stock_compare.cpp`, `test_real_stock_compare.cpp`, `test_gemv_compare.cpp`, `test_gemm_compare.cpp`, `BASELINE_DP4A.md`, `baseline_dp4a.json`. KEEP per `allowlist.yaml`: `kernels/common/block_iq4_xs.h`, `kernels/common/hip_helpers.h`, `kernels/common/bench.h`, `kernels/common/matmul_test_util.h`, `kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip` (canonical decode), `kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip` (keep ONE WMMA file — canonical name `impl_gemm_wmma.hip` if shorter, document in `allowlist.yaml`), `kernels/matmul_iq4xs/ref_cpu.h/.cpp`, `kernels/matmul_iq4xs/test_gemv_dp4a_compare.cpp`, `bench_gemv_dp4a.cpp`, `test_gemm_wmma_compare.cpp`, `bench_gemm_wmma.cpp` **plus** `benchmarks/lib/store_minimal.py` 30-line logger per fix #3 (do NOT delete entire `benchmarks/lib`). Rewrite `kernels/CMakeLists.txt` to only `add_subdirectory(common)` and `add_subdirectory(matmul_iq4xs)` but **do NOT duplicate `CMAKE_HIP_ARCHITECTURES=gfx1100` / `--offload-arch`** — that is owned solely by `cmake/toolchain-hip-gfx1100.cmake` per fix #2 (single owner). `kernels/common/CMakeLists.txt` exposes `kernels_common` INTERFACE. Rewrite `kernels/matmul_iq4xs/CMakeLists.txt` to define only `matmul_common_iface`, `matmul_ref_cpu`, `matmul_gemv_dp4a_hip`, `matmul_gemm_wmma_stream_hip`, and the four test/bench executables (drop naive/stock/real-stock). Ensure every kept .hip uses templated `WARP_SIZE` and `__launch_bounds__(256,4)` + `amdgpu_flat_work_group_size(256,256)` already present. Verify with `diff <(find kernels -type f | sort) <(yq '.keep[] | select(test("^kernels/"))' allowlist.yaml | sort)` — no hand-maintained `grep -c`.
  </action>
  <verify>
    <automated>bash -c "find kernels -type f | sort; echo '---count'; find kernels -type f | wc -l; echo '---ensure essentials exist'; ls -l kernels/common/block_iq4_xs.h kernels/common/hip_helpers.h kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip 2>&1 | head -n 20" --timeout 15</automated>
  </verify>
  <done>kernels/ file list is exactly allowlist (no demo/template/fixtures binaries); CMakeLists files are minimal and reference only essentials; find kernels -type f matches expected count within ±2</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal root CMakeLists.txt (Windows-native HIP)</name>
  <files>CMakeLists.txt</files>
  <action>
    Create root `CMakeLists.txt` (UTF-8 CRLF-safe) **thin wrapper** per fix #2: it only does `cmake_minimum_required(VERSION 3.21)`, `project(qwen-gfx1100-hip LANGUAGES HIP CXX)`, `include(cmake/toolchain-hip-gfx1100.cmake)` (single owner for `HIP_PATH` quoted, `find_package(hip)`, `--offload-arch=gfx1100`, `gfx1100` cache, `CMAKE_HIP_COMPILER` validation — see `allowlist.yaml` `vars`), then `option(GGML_CUDA_ENABLE_CUSTOM_GFX1100 ...)`, `add_subdirectory(kernels)`, `add_subdirectory(llama.cpp)` or `FetchContent` at `bb4caa75`. **Do NOT duplicate** `CMAKE_HIP_ARCHITECTURES`/`--offload-arch` here — `kernels/CMakeLists.txt` must not repeat them either (single toolchain file owns them). Ensure no `/opt/rocm` hardcode — use `$ENV{HIP_PATH}` via toolchain. Add `message(STATUS "HIP_PATH=$ENV{HIP_PATH} arch=${CMAKE_HIP_ARCHITECTURES}")`. Verify `grep -c "/opt/rocm" CMakeLists.txt` == 0 and `grep -c "HIP_PATH" cmake/toolchain-hip-gfx1100.cmake` >=2.
  </action>
  <verify>
    <automated>bash -c "cat CMakeLists.txt | head -n 80; echo '---'; grep -c 'gfx1100' CMakeLists.txt; grep -c 'HIP_PATH' CMakeLists.txt; grep -c '/opt/rocm' CMakeLists.txt; echo 'should be 0 /opt/rocm'" --timeout 15</automated>
  </verify>
  <done>CMakeLists.txt exists at repo root, pins gfx1100, is HIP_PATH-aware, contains no /opt/rocm reference, and includes option GGML_CUDA_ENABLE_CUSTOM_GFX1100</done>
</task>

<task type="auto">
  <name>Task 3: Validate patch cleanliness</name>
  <files>patches/0001-gfx1100-mul-mat-custom.patch</files>
  <action>
    Ensure `patches/0001-gfx1100-mul-mat-custom.patch` is clean 30-355-line hybrid (currently 30 lines GEMM disabled). Must vendor `gemv_iq4xs.cuh`+`gemm_iq4xs.cuh` into `custom_gfx1100/`, guard `mmq.cu`/`mmvq.cu` with `#if defined(GGML_CUDA_ENABLE_CUSTOM_GFX1100)`, keep OFF default, keep `custom_gemm_iq4xs_can_handle return false` with re-enable note. **Do NOT handle CRLF per-plan** — per fix #5, `.gitattributes` `*.patch text eol=lf` is single source; delete `core.autocrlf=false` prose here. Validate via single helper `scripts/apply_patch.sh` (see 08-03/08-04) — `scripts/apply_patch.sh --check` must pass, not `git -C llama.cpp apply --check` duplicated. If FetchContent, `PATCH_COMMAND` calls same helper. Patch helper is single owner.
  </action>
  <verify>
    <automated>bash -c "ls -l patches/0001-gfx1100-mul-mat-custom.patch; wc -l patches/0001-gfx1100-mul-mat-custom.patch; head -n 30 patches/0001-gfx1100-mul-mat-custom.patch; echo '---check'; git -C llama.cpp apply --check ../patches/0001-gfx1100-mul-mat-custom.patch 2>&1 || echo 'CHECK_FAILED'; echo '---grep gating'; grep -c 'GGML_CUDA_ENABLE_CUSTOM_GFX1100' patches/0001-gfx1100-mul-mat-custom.patch" --timeout 15</automated>
  </verify>
  <done>patch file exists with ~355 lines, contains GGML_CUDA_ENABLE_CUSTOM_GFX1100 guards, git apply --check passes on Windows CRLF/LF, GEMM can_handle still returns false by design with re-enable note</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root CMake -> HIP SDK | Untrusted env HIP_PATH crosses here |
| kernels/hip code -> gfx1100 driver | Device code must target correct arch or silent fallback/CPU |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Tampering | CMakeLists.txt HIP_PATH | medium | mitigate | Validate HIP_PATH exists, fail fast with message; no download without hash |
| T-08-02-02 | Elevation | patch apply | medium | mitigate | Patch is read-only quilt overlay, never overwrites upstream beyond custom_gfx1100/; git apply --check before apply |
| T-08-02-SC | Tampering | hip package fetch | high | mitigate | llama.cpp pinned @bb4caa75 with GIT_TAG; FetchContent without hash is [ASSUMED] — plan emits checkpoint if not vendored |
</threat_model>

<verification>
- `find kernels -type f | sort` equals allowlist per 08-CONTEXT.md D-02 (no demo/template/fixtures .bin).
- `grep -R "/opt/rocm" --include="CMakeLists.txt" .` is empty.
- `grep -c "gfx1100" CMakeLists.txt` >= 2 and `grep -c "HIP_PATH" CMakeLists.txt` >= 2.
- `git -C llama.cpp apply --check ../patches/0001-gfx1100-mul-mat-custom.patch` exits 0.
- `kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip` still contains `__builtin_amdgcn_sudot4` and `sh[32][33]`.
</verification>

<success_criteria>
REQ-WIN-02 satisfied: kernels/ minimal with only IQ4_XS winners + headers, root CMake configures gfx1100 via HIP_PATH, patch remains clean and check-passes.
</success_criteria>

<output>
Create `.planning/phases/08-refactor-windows-native/08-02-SUMMARY.md` when done
</output>
