---
phase: 08-refactor-windows-native
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - allowlist.yaml
  - .gitignore
  - .gitattributes
  - cmake/toolchain-hip-gfx1100.cmake
  - scripts/prune_bloat.py
  # INVENTORY.md is ephemeral under build-*/ (not committed) — generated from allowlist.yaml
autonomous: true
requirements:
  - REQ-WIN-01
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Repo free of Python/JS bloat: no .py in benchmarks/, no .mjs (qstar.mjs), no freetoken-rocm-probe, no .planning phases 01-07 remain tracked"
    - "Tracking shows deletions are committed and not silently gitignored without commit"
    - "Remaining tracked files are exactly allowlist per 08-CONTEXT.md D-01"
  artifacts:
    - .gitignore
    - INVENTORY.md
  key_links:
    - "Inventory allowlist -> deletion script -> git status clean -> verify no .py/.mjs remains"
---

<objective>
Inventory every file/dir to keep vs delete (explicit allowlist vs blocklist) and execute bloat removal per REQ-WIN-01 (OBJ-1).

Purpose: Establish the bloat-free baseline required before kernel pruning and Windows build can be validated.
Output: Pruned tree with only keep-list tracked, updated .gitignore, committed deletions, and inventory evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-windows-native/08-CONTEXT.md
@.gitignore
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate single-source allowlist and tag archive point</name>
  <files>allowlist.yaml, cmake/toolchain-hip-gfx1100.cmake, .gitattributes</files>
  <action>
    Create `allowlist.yaml` as single source of truth per Thermos fix #1 (replaces 5 prose copies). Sections: `keep:` (allowlist per 08-CONTEXT.md D-01/D-02 + vars `gfx_arch: gfx1100` `hip_path_default: "C:/Program Files/AMD/ROCm/6.2"`), `delete:` (blocklist). Also create `cmake/toolchain-hip-gfx1100.cmake` per fix #2 (owns HIP_PATH quoted, find_package, --offload-arch=gfx1100, Ninja mandate) so 08-02/08-03 do not duplicate. Add `.gitattributes` per fix #5: `*.patch text eol=lf` (delete per-plan CRLF prose). Generate ephemeral `build-08/INVENTORY.md` via `yq '.keep[] / .delete[]' allowlist.yaml` (not committed — fixes ephemeral vs shipped bloat). Verify exhaustive coverage: `diff &lt;(find . -type f ! -path "./.git/*" ! -path "./llama.cpp/*" | sort) &lt;(yq '.keep[] + .delete[]' allowlist.yaml | sort)` must be empty. Create single `scripts/prune_bloat.py` (manifest-driven) + thin `prune_bloat.sh` wrapper per fix #4 (collapse dual sh/ps1). Before deletion, `git tag archive/pre-windows-08` (push if remote). Per D-01, delete list must include: .planning/ phases 01-07 (but NOT 08 — 08 is ephemeral and gitignored after tag per fix #4), benchmarks/ Python harness, freetoken-rocm-probe/qstar.mjs, scrape_out, .rocprofv3, tools/dump_*, kernels/demo/template/fixtures, kernels/build/results, .agents, logs, src, baseline, docs (migrated), scrape_sahibinden.sh, qstar.mjs. Keep idempotent, timeout 30s per batch. Do NOT delete benchmarks/lib/store_minimal.py replacement per fix #3 — keep 30-line logger.
  </action>
  <verify>
    <automated>bash -c "ls -R --time-style=long-iso > /tmp/tree_before.txt; wc -l /tmp/tree_before.txt; cat INVENTORY.md | grep -c ALLOWLIST; cat INVENTORY.md | grep -c BLOCKLIST" --timeout 15</automated>
  </verify>
  <done>INVENTORY.md enumerates every repo file/dir in exactly one of ALLOWLIST/BLOCKLIST with justification; prune scripts exist and are executable; archive tag created or documented as local-only if no remote</done>
</task>

<task type="auto">
  <name>Task 2: Execute deletions and refresh .gitignore (ephemeral inventory)</name>
  <files>.gitignore, .gitattributes</files>
  <action>
    Execute `scripts/prune_bloat.py` manifest-driven (single source per fix #1, not dual sh/ps1). For tracked: `git rm -rf $(yq '.delete[]' allowlist.yaml)` timeout 30s per batch; untracked: `rm -rf`. After deletions, rewrite `.gitignore` minimal per fix #4: add `/.planning/` (ephemeral after tag), `/build-08/` (ephemeral inventory), keep `build/`, `build-windows/`, `build-*/`, `kernels/build/`, `*.exe/.pdb/.obj/.lib/.dll`, `models/*.gguf`, `logs/`, `*.log`, `Thumbs.db/.DS_Store/.vs/.vscode/`. Remove stale entries (`freetoken-rocm-probe/tools/` etc.). Remove negation magic `!kernels/fixtures/manifest*.json` — delete `kernels/fixtures/` entirely if regeneratable per `gguf-py` (explicit decision, not `if referenced` branch per Thermos). Keep `benchmarks/lib/store_minimal.py` 30-line logger per fix #3 (do NOT delete entire benchmarks/lib). Run `git status --porcelain` + `git ls-files | grep -E "\.py$|\.mjs$"` + `find` checks (`find . -type f -name "*.py" ! -path "./llama.cpp/*" ! -path "./.git/*" ! -path "./build-08/*" | wc -l` == 0, same for `*.mjs`). Stage `.gitignore`+`.gitattributes`+`allowlist.yaml`+`cmake/toolchain-hip-gfx1100.cmake` + deletions; `build-08/INVENTORY.md` stays untracked (ephemeral). Do not commit yet.
  </action>
  <verify>
    <automated>bash -c "find . -type f -name '*.py' ! -path './llama.cpp/*' ! -path './.git/*' | head -n 20; echo '---py count'; find . -type f -name '*.py' ! -path './llama.cpp/*' ! -path './.git/*' | wc -l; echo '---mjs count'; find . -type f -name '*.mjs' ! -path './llama.cpp/*' ! -path './.git/*' | wc -l; echo '---tracked py/mjs'; git ls-files | grep -E '\.py$|\.mjs$' | grep -v '^llama.cpp/' | head -n 20; echo '---status'; git status --porcelain | head -n 40" --timeout 15</automated>
  </verify>
  <done>.gitignore is minimal and committed; git ls-files outside llama.cpp/ contains zero .py/.mjs; find confirms zero Python/JS in pruned tree; git status shows deletions staged and no allowlist file accidentally removed (kernels/common/block_iq4_xs.h, hip_helpers.h, patches/0001* still tracked)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo filesystem -> git history | Deletion script crosses trust boundary; mis-scoped rm could destroy allowlist kernels/patches |
| blocklist enumeration -> execution | Inventory must be authoritative before any rm -rf runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | scripts/prune_bloat.* | high | mitigate | Inventory reviewed in Task 1 before execution; archive tag created; `git rm --dry-run` preview before real delete |
| T-08-01-02 | Denial of Service | .gitignore refresh | low | mitigate | Minimal gitignore leaves build-windows/ escapable; no negation of allowlist |
| T-08-01-SC | Tampering | package installs | high | mitigate | No npm/pip/cargo installs in this plan; no new packages |
</threat_model>

<verification>
- `cat INVENTORY.md` shows every file/dir assigned to exactly one list with justification referencing 08-CONTEXT.md D-01.
- `git tag archive/pre-windows-08 --list` exists or documented local tag.
- `git ls-files | grep -E "\.py$|\.mjs$" | grep -v "^llama.cpp/" | wc -l` == 0
- `find . -type f -name "*.py" ! -path "./llama.cpp/*" ! -path "./.git/*" | wc -l` == 0 ; same for `*.mjs` (qstar.mjs removed).
- `.gitignore` contains `build-windows/` and does not mention stale `freetoken-rocm-probe/` after cleanup.
</verification>

<success_criteria>
REQ-WIN-01 satisfied: zero Python/JS remains outside llama.cpp/; benchmarks/, freetoken-rocm-probe/, scrape_out/, .rocprofv3/, tools/dump_*, .planning phases 01-07 are deleted; only allowlist per 08-CONTEXT.md remains tracked.
</success_criteria>

<output>
Create `.planning/phases/08-refactor-windows-native/08-01-SUMMARY.md` when done
</output>
