---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - kernels/matmul_iq4xs/bench_real_stock.hardware.json
  - kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json
  - kernels/matmul_iq4xs/bench_gemv_xor.hardware.json
  - kernels/matmul_iq4xs/bench_gemm_wmma.hardware.json
  - kernels/matmul_iq4xs/bench_gemm_direct.json
  - benchmarks/results/phase7/llama_bench_stock_4tier_N10.json
  - benchmarks/results/phase7/llama_bench_custom_4tier_N10.json
  - benchmarks/results/phase7/race.py
  - benchmarks/results/phase7/rows.jsonl
  - benchmarks/results/phase7/CHECKSUMS.sha256
  - benchmarks/results/phase7/README.md
  - benchmarks/results/phase7/llm_qa_N15.json
  - benchmarks/profiling/KERNEL-BENCH-DIFF.md
  - docs/PUBLICATION.md
autonomous: true
requirements:
  - REQ-STAT-07
  - BENCH-01
  - REQ-PERF-07
  - QUAL-01
  - QUAL-02
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "bench_* --runs 10 emits valid JSON 45/45 entries each with runs:10 + median/mean/stddev/p95 (bench_real_stock 8 + bench_gemv_dp4a 8 + bench_gemv_xor 8 + bench_gemm_wmma N=10 + bench_gemm_direct) — no truncated JSON, no single-run claim"
    - "llama-bench 4-tier N=10 per build (pp @ 512/1024/2048/4096 + tg) produces 5 entries each with samples_ts length 10 + avg_ts/stddev_ts, interleaved via race.py --repeats 10 A,B,A,B thermal-paired, median >=1.10x gate reported as HONEST FAIL 1.05-1.07"
    - "LLM QA N=15 temp=0 fixed prompt -n 128 reports avg tok/s + avg latency + stddev + per-run 15-row table — single-run banned, 250 synthetic rows.jsonl median 1.05 FAIL not claimed as PASS"
    - "QUAL-01 op-gate and QUAL-02 model-gate each N=10 green (0 errors, PPL 6.4271 +/-1%) on custom ON build — hardware proof, not simulation"
    - "KERNEL-BENCH-DIFF.md §8 and PUBLICATION.md §8 updated with honest 1.05-1.07 FAIL tables, median/mean/stddev/p95, race winner rationale, no fabricated 1.10x PASS"
  artifacts:
    - kernels/matmul_iq4xs/bench_real_stock.hardware.json
    - kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json
    - kernels/matmul_iq4xs/bench_gemm_wmma.hardware.json
    - benchmarks/results/phase7/llama_bench_stock_4tier_N10.json
    - benchmarks/results/phase7/llama_bench_custom_4tier_N10.json
    - benchmarks/results/phase7/llm_qa_N15.json
    - benchmarks/profiling/KERNEL-BENCH-DIFF.md
    - docs/PUBLICATION.md
  key_links:
    - "bench_* --runs 10 --json -> hardware JSON runs:10 median/mean/stddev/p95 -> KERNEL-BENCH-DIFF §8 honest FAIL table"
    - "race.py --repeats 10 interleaved A,B,A,B -> llama-bench -r 10 x 4-tier N=10 -> rows.jsonl 250 lines + CHECKSUMS -> PUBLICATION §8 per-tier 1.10x verdict"
    - "llama-cli --temp 0 fixed prompt -n 128 x15 -> llm_qa_N15.json avg tok/s+latency+stddev+15-row -> PUBLICATION LLM QA section"
---

<objective>
Statistical rigour closure for REQ-STAT-07 / BENCH-01 amended N>=10 (LLM QA N=15) — prove every quantitative claim is 10x averaged with median/mean/stddev/p95, ban single-run, and publish honest 1.05-1.07 FAIL (no fabricated PASS) across microbench + llama-bench + quality gates.

Purpose: Close the last verifier gap (7-VERIFICATION.md 1/7) by making N>=10/15 not harness-ready but hardware-proven, with interleaved thermal pairing and publication-grade FAIL tables.
Output: Valid N=10 JSONs (45/45), 5-entry llama-bench N=10 pair, N=15 LLM QA artifact, QUAL-01/02 N=10 green, race.py interleaved proof, updated KERNEL-BENCH-DIFF §8 + PUBLICATION §8 honest tables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-VERIFICATION.md
@benchmarks/profiling/KERNEL-BENCH-DIFF.md
@docs/PUBLICATION.md
@benchmarks/results/phase7/race.py
@kernels/matmul_iq4xs/bench_real_stock.hardware.json
@kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json
</context>

<tasks>

<task type="tracer">
  <name>End-to-end N>=10 rigour tracer — bench_* + llama-bench + QA wiring</name>
  <files>kernels/matmul_iq4xs/bench_real_stock.hardware.json, kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json, kernels/matmul_iq4xs/bench_gemm_wmma.hardware.json, benchmarks/results/phase7/llama_bench_stock_4tier_N10.json, benchmarks/results/phase7/llama_bench_custom_4tier_N10.json, benchmarks/results/phase7/llm_qa_N15.json</files>
  <action>
    Wire ONE complete rigorous measurement slice end-to-end per REQ-STAT-07 / BENCH-01 amended (single-run banned). On WSL2 gfx1100 bare-metal with HSA_ENABLE_DXG_DETECTION=1 and 90s per-bench timeout: run bench_real_stock --runs 10 --json (8 shapes, each emits runs:10 + median/mean/stddev/p95 + speedup_vs_naive), bench_gemv_dp4a --runs 10 --json (8 shapes, variant XOR, speedup_median + speedup_mean_minus_1sigma), bench_gemm_wmma --runs 10 --json (covers M 128/512/1024 + 4-tier shapes, each variant emits median/mean/stddev/p95 + TFLOPS_median + GB/s, no truncated 12288B — use timeout 90 and buffered write). Verify each JSON with python -c "import json; d=json.load(open(f)); assert all(x['runs']==10 for x in d)" and count 45/45 valid entries across the three files (8+8+~14+extras). Then drive llama-bench -r 10 per tier per build: stock OFF vs custom ON interleaved via race.py --repeats 10 (A,B,A,B not AAAA BBBB) at {512,1024,2048,4096} pp + tg (4 pp +1 tg =5 entries), capturing avg_ts/stddev_ts/samples_ts length 10 per entry with hwinfo_daemon 1Hz + thermal_watchdog 90C in one window. Produce llm_qa_N15.json via llama-cli/llama-server fixed prompt temp=0 -n 128 repeated N=15 on custom path, recording per-run tok/s + latency_ms table (15 rows) plus avg tok/s, avg latency, stddev. Fail the tracer if any file is truncated, any runs !=10/15, or any single-run claim remains.
  </action>
  <verify>
    <automated>bash -c "python3 -c 'import json,glob; files=[\"kernels/matmul_iq4xs/bench_real_stock.hardware.json\",\"kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json\",\"kernels/matmul_iq4xs/bench_gemm_wmma.hardware.json\"]; total=sum(len(json.load(open(f))) for f in files); print(total); assert total>=24' && python3 -c 'import json; d=json.load(open(\"benchmarks/results/phase7/llama_bench_custom_4tier_N10.json\")); print(len(d)); assert len(d)==5 and all(len(x[\"samples_ts\"])==10 for x in d)' && python3 -c 'import json; d=json.load(open(\"benchmarks/results/phase7/llm_qa_N15.json\")); print(len(d[\"per_run\"])); assert len(d[\"per_run\"])==15'" --timeout 30</automated>
  </verify>
  <done>bench_* JSONs total 45/45 valid with runs:10 + median/mean/stddev/p95, llama-bench 5 entries each samples length 10, llm_qa_N15 15-row table present, single-run banned — tracer committed</done>
</task>

<task type="auto">
  <name>QUAL-01/02 N=10 green + race.py interleaved + RunStore checksums</name>
  <files>benchmarks/results/phase7/race.py, benchmarks/results/phase7/rows.jsonl, benchmarks/results/phase7/CHECKSUMS.sha256, benchmarks/results/phase7/README.md</files>
  <action>
    Enforce statistical rigour on quality gates per REQ-STAT-07: run QUAL-01 test-backend-ops --runs 10 (or via run_op_gate.py --runs 10) on build-custom ON, expecting 0 errors each run; run QUAL-02 run_model_gate.py --runs 10 (wikitext-2 PPL 6.4271 +/-1% [6.3628,6.4914] + 6/6 canaries) each run, capturing median/mean/stddev per gate. Use race.py --repeats 10 interleaved A,B,A,B to kill thermal bias (cite adelj88 pattern) — verify by grepping race.py for "A,B,A,B" / "interleaved" and --repeats 10 default. Append all bench + llama-bench repeats to benchmarks/results/phase7/rows.jsonl via RunStore (250 lines pp-only or 500 with pp+tg dual — document which), ensuring synthetic old rows (future ts 1787995716 median 1.05 FAIL) are either replaced or explicitly labeled synthetic not claimed as hardware PASS. Regenerate CHECKSUMS.sha256 over rows.jsonl + llama_bench JSONs + hardware JSONs. Update README.md with N=10/15 methodology: --runs 10, -r 10, temp=0 fixed prompt, median/mean/stddev/p95, thermal pairing, VRAM preflight >2GB + hipMalloc probe for 8192 conditional SKIPPED. No fabricated PASS — rows.jsonl median must remain 1.03-1.08 FAIL <1.10x until bare-metal beats it.
  </action>
  <verify>
    <automated>bash -c "grep -q 'interleaved' benchmarks/results/phase7/race.py && grep -q 'repeats.*10' benchmarks/results/phase7/race.py && echo race_ok; wc -l benchmarks/results/phase7/rows.jsonl; sha256sum -c benchmarks/results/phase7/CHECKSUMS.sha256 --quiet && echo checksums_ok; cat benchmarks/results/phase7/README.md | grep -qi 'N=10' && echo readme_ok" --timeout 30</automated>
  </verify>
  <done>QUAL-01/02 N=10 green proven per run, race.py interleaved A,B,A,B with --repeats 10 verified, rows.jsonl 250+ lines with CHECKSUMS matching, README documents N=10/15 rigour, no PASS fabricated</done>
</task>

<task type="auto">
  <name>Publish honest FAIL tables — KERNEL-BENCH-DIFF §8 + PUBLICATION §8</name>
  <files>benchmarks/profiling/KERNEL-BENCH-DIFF.md, docs/PUBLICATION.md</files>
  <action>
    Update benchmarks/profiling/KERNEL-BENCH-DIFF.md §8 (Hybrid provenance + High-Yield Variant Racing + Paired llama-bench A/B N=10) and docs/PUBLICATION.md §8 (Phase7 methodology) with HONEST N=10/15 tables — do NOT fabricate 1.10x PASS. Must include: (1) per-variant microbench N=10 median±stddev/p95 matrix vs real DP4A 99.5us (99.55±28.56 vs naive 543) with speedup_median per variant (64x32 P2+33 1.08x, 64x32 P4+XOR 1.12x, 64x64 P4+XOR 1.18x synthetic projection vs hardware 0.94 avg FAIL <1.2x), TFLOPS_median/GB/s where applicable; (2) paired llama-bench N=10 per-tier (512/1024/2048/4096 pp + tg) stock median±stddev vs custom median±stddev with median >=1.10x? and mean-1sigma >=1.10x? verdict column — MUST show HONEST FAIL 1.05-1.07 synthetic and hardware 808.18->849.75 pp4096 1.051x FAIL <1.10x, tg 1.046x FAIL, not PASS; (3) LLM QA N=15 avg tok/s + avg latency + stddev + per-run 15-row table placeholder filled or TBD with protocol; (4) QUAL-01/02 N=10 summary; (5) race winner rationale (P=4+XOR+b128 etc) as projection, not proven PASS; (6) explicit notes: single-run banned, bench_real_stock 99.55 vs 543 5.46x VERIFIED (runs:10), bench_gemv 0.94 FAIL, bench_gemm truncated 12288B fixed via timeout 90, can_handle stub fixed true gate, no 1.10x claim where <1.10x. Any PASS fabricated fails verification.
  </action>
  <verify>
    <automated>bash -c "grep -q 'HONEST FAIL' benchmarks/profiling/KERNEL-BENCH-DIFF.md && grep -q '1.051x FAIL' benchmarks/profiling/KERNEL-BENCH-DIFF.md && grep -q 'N=10' benchmarks/profiling/KERNEL-BENCH-DIFF.md && grep -q 'single-run banned' benchmarks/profiling/KERNEL-BENCH-DIFF.md && echo kdb_ok; grep -q 'HONEST FAIL' docs/PUBLICATION.md && grep -q 'N=15' docs/PUBLICATION.md && echo pub_ok; ! grep -q '1.10x PASS' benchmarks/profiling/KERNEL-BENCH-DIFF.md || (echo 'fabricated PASS found' && exit 1)" --timeout 30</automated>
  </verify>
  <done>KERNEL-BENCH-DIFF §8 and PUBLICATION §8 show honest 1.05-1.07 FAIL median/mean/stddev/p95 tables, LLM QA N=15 table, no fabricated 1.10x PASS, single-run banned noted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WSL2 gfx1100 bare-metal -> JSON artifact | Hardware JSON crosses trust boundary; truncated or single-run fabricates uplift |
| race.py interleaved -> RunStore rows.jsonl | Thermal bias (AAAA BBBB) would inflate PASS if not interleaved A,B,A,B |
| publication tables -> reader | Honest FAIL vs fabricated PASS — integrity boundary per REQ-PERF-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | bench_* hardware JSON | high | mitigate | Enforce --runs 10 + python json.load runs==10 + 45/45 count gate; timeout 90 prevents 12288B truncation |
| T-07-03-02 | Tampering | llama-bench N=10 A/B | high | mitigate | race.py --repeats 10 interleaved A,B,A,B + hwinfo_daemon 1Hz + thermal_watchdog 90C in one window; samples_ts length 10 asserted |
| T-07-03-03 | Repudiation | rows.jsonl synthetic vs hardware | medium | mitigate | CHECKSUMS.sha256 over rows.jsonl + explicit synthetic label (future ts 1787995716 median 1.05 FAIL not claimed) |
| T-07-03-04 | Information Disclosure | PUBLICATION §8 | medium | mitigate | Honest FAIL 1.051x/1.046x <1.10x tables, no fabricated PASS; grep -v 1.10x PASS gate |
| T-07-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — pure C++/HIP + CMake; package-legitimacy gate N/A |
</threat_model>

<verification>
- `python -c "import json; d=json.load(open('kernels/matmul_iq4xs/bench_real_stock.hardware.json')); assert all(x['runs']==10 for x in d)"` == 8/8 valid
- `python -c "import json; d=json.load(open('kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json')); assert len(d)==8 and all(x['runs']==10 for x in d)"` and `bench_gemm_wmma.hardware.json` not truncated (>12288B, json.load PASS) — total 45/45
- `python -c "import json; d=json.load(open('benchmarks/results/phase7/llama_bench_custom_4tier_N10.json')); assert len(d)==5 and all(len(x['samples_ts'])==10 for x in d)"` and same for stock
- `python -c "import json; d=json.load(open('benchmarks/results/phase7/llm_qa_N15.json')); assert len(d['per_run'])==15"` with avg tok/s+latency+stddev present
- `grep interleaved benchmarks/results/phase7/race.py && grep -q 'repeats.*10' benchmarks/results/phase7/race.py` and `sha256sum -c benchmarks/results/phase7/CHECKSUMS.sha256`
- `grep -q 'HONEST FAIL' benchmarks/profiling/KERNEL-BENCH-DIFF.md && grep -q 'HONEST FAIL' docs/PUBLICATION.md` and `! grep -q '1.10x PASS' benchmarks/profiling/KERNEL-BENCH-DIFF.md` (no fabricated PASS)
- `grep -q 'single-run banned' benchmarks/profiling/KERNEL-BENCH-DIFF.md` and `grep -q 'N=10' docs/PUBLICATION.md`
</verification>

<success_criteria>
- bench_* N=10 valid JSON 45/45 with runs:10 + median/mean/stddev/p95, no truncation, speedup vs real DP4A 99.5us honest
- llama-bench 4-tier N=10 5 entries each samples_ts 10, interleaved race.py --repeats 10 A,B,A,B thermal-paired, per-tier 1.10x verdict shows HONEST 1.05-1.07 FAIL (<1.10x) not PASS
- LLM QA N=15 temp=0 fixed prompt -n 128 with avg tok/s + latency + stddev + per-run 15-row table, rows.jsonl 250 lines median 1.05 FAIL not claimed
- QUAL-01/02 N=10 green (0 errors, PPL 6.4271 +/-1%)
- KERNEL-BENCH-DIFF §8 + PUBLICATION §8 updated honest FAIL tables, no fabricated PASS, single-run banned
</success_criteria>

<output>
Create `.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-03-SUMMARY.md` when done
</output>
