---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: "03"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
  - kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip
  - kernels/matmul_iq4xs/bench_gemm_wmma.cpp
  - kernels/matmul_iq4xs/test_gemm_wmma_compare.cpp
autonomous: true
requirements:
  - KERN-05
  - REQ-STAT-07
  - REQ-PERF-07
  - REQ-WIN-07
  - BENCH-01
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "impl_gemm_wmma_stream.hip streams IQ4_XS -> WMMA via hardware wmma_f32_16x16x16_f16_w32 (wave32, OPSEL, lane%16) with 64x32 base tiling, plus high-yield tile sweeps 64x32/64x64/128x32 gated by winner"
    - "bench_gemm_wmma --runs 10 --json reports median/mean/stddev/p95 + speedup_median + TFLOPS median per M={128,512,1024,8192} vs real stock DP4A MMQ, with per-variant table (P=2 vs P=4, +33 vs XOR, B-stationary, LUT mu=4)"
    - "P=2 vs P=4 double buffer with __builtin_amdgcn_sched_barrier(0x0080 DS / 0x0008 WMMA) + B-stationary + LUT mu=4 + calculator VGPR <=64 + rocprof lds_bank_conflict 0 + v_wmma disasm all gated"
  artifacts:
    - kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
    - kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip
    - kernels/matmul_iq4xs/bench_gemm_wmma.cpp
  key_links:
    - "bench_gemm_wmma.cpp -> impl_gemm_wmma_stream_hip + impl_gemm_lut_iq4xs_hip vs matmul_real_stock_hip (M>=128 tiling race)"
    - "impl_gemm_wmma_stream.hip -> sched_barrier pinning (GMEM->VGPR->LDS->VGPR->WMMA 4-stage)"
---

<objective>
Deliver RDNA3 WMMA streaming GEMM kernel that beats real stock MMQ prefill with high-yield tile/pipeline/LUT variants and N=10 rigour. Directly addresses ROADMAP criterion 3 (>950 t/s prefill N=10) and owns the prefill half of REQ-PERF-07 1.10x. High-yield variants explicit per 07-CONTEXT: tile sweeps 64x32/64x64/128x32, P=2 vs P=4 double buffer with sched_barrier, B-stationary, LUT mu=4, calculator VGPR gate.

Purpose: Prefill at 8192 needs 64x reuse + WMMA 1024 ops/CU vs DP4A 512; P=4 hides GMEM while WMMA runs on 800 GB/s roof.
Output: Variant-aware WMMA kernel + LUT secondary kernel + bench_gemm_wmma --runs 10 --json per-variant table + calculator/disasm/rocprof gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
@kernels/matmul_iq4xs/bench_gemm_wmma.cpp
@kernels/matmul_iq4xs/test_gemm_wmma_compare.cpp
</context>

<tasks>

<task type="auto">
  <name>Implement WMMA high-yield variants: tile sweeps + P=2/P=4 pipeline + B-stationary + LUT mu=4</name>
  <files>kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip, kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip</files>
  <action>Implement high-yield WMMA variants in kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip and add secondary LUT kernel kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip (offline dequant). Base: 64x32 per block (4x2 warps =256 thr, 8 warps) of 16x16x16 WMMA tiles via __builtin_amdgcn_wmma_f32_16x16x16_f16_w32 (wave32, OPSEL false low half, lane%16 replication). Keep __launch_bounds__(256,4)+amdgpu_flat_work_group_size(256,256) -> <=64 VGPR ->16 waves/SIMD. High-yield sweep: template<int TILE_M, int TILE_N> parameters to support 64x32 (4KB per buffer), 64x64 (8KB, 64x reuse, B-stationary), 128x32 (8x2 warps, for M=8192 ->128 blocks) - gate selection via compile flag TILE_M/N. LDS banking A/B: Variant A _Float16 sB[2][32][33] +33 padding (+3%, proven 4-way->0), Variant B XOR preshuffle template<xor_KPerBlock=64, KPack=8> idx = (row % (KPerBlock/KPack)) ^ col for 0% with float4 *lds+offset = *src. Pipeline P=2 vs P=4: current sB[2][32][33] double buffer P-1=1 lookahead; add P=4 variant sB[4][32][32] (+XOR to save LDS) with GMEM->VGPR->LDS->VGPR->WMMA 4-stage overlap (MARLIN P=4) pinned with __builtin_amdgcn_sched_barrier(0x0080) before DS and 0x0008 before WMMA (llvm.org AMDGPUUsage). B-stationary: keep IQ4_XS weight tile in VGPR frag (A_frag 8 VGPR, B_frag 8 VGPR wave32) while streaming Q8_1 activation via LDS ping-pong; document Shared Memory bypass of B pattern. On-the-fly dequant d*(ls-32)*kvalues_iq4nl -> v16f16/v8f32 overlapped with previous WMMA (pipelined), not inline. Create impl_gemm_lut_iq4xs.hip second kernel: LUT mu=4 16-entry half LUT per tile (32B/LUT, bake d*(ls-32) offline) as alternative to inline dequant, same tiling. All pure C++/HIP, Windows clang++.exe --offload-arch=gfx1100 clean. Document calculator gate in header: amd_matrix_instruction_calculator -a gfx1100 -i wmma_f32_16x16x16_f16 -d predicts A_frag 8/B_frag 8/D 8 VGPR wave32 -> <=64 gate before commit.</action>
  <verify>
    <automated>HSA_ENABLE_DXG_DETECTION=1 cmake -S kernels -B kernels/build -DCMAKE_HIP_ARCHITECTURES=gfx1100 && cmake --build kernels/build --target matmul_gemm_wmma_stream_hip -j 4 && grep -q "wmmma\|wmma_f32_16x16x16" kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip && grep -q "sB\[2\]\[32\]\[33\]\|sB\[4\]" kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip && grep -q "sched_barrier.*0x0080\|sched_barrier.*0x0008" kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip && grep -q "B-stationary\|LUT.*mu=4" kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip && hipcc --offload-arch=gfx1100 -c kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip -o /tmp/wmma.o && llvm-objdump --mcpu=gfx1100 /tmp/wmma.o | grep -q "v_wmma" && echo "WMMA tiles P2/P4 B-stationary LUT v_wmma OK" && hipcc --save-temps -Rpass-analysis --offload-arch=gfx1100 kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip -c 2>&1 | grep -qi "vgpr"</automated>
  </verify>
  <done>impl_gemm_wmma_stream.hip has tile sweeps 64x32/64x64/128x32, P=2 [2][32][33] and P=4 [4][32][32] + sched_barrier, B-stationary, LUT mu=4 kernel exists, llvm-objdump shows v_wmma, VGPR check runs.</done>
</task>

<task type="auto">
  <name>Add 128-bit b128 coalescing + offline 16x64 swizzle + calculator VGPR pre-gate</name>
  <files>kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip, kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip</files>
  <action>Wire 128-bit coalescing and offline swizzle docs into both GEMM kernels. For global loads: use __builtin_amdgcn_global_load_b128 / float4 / ulong2 16B for block_iq4_xs qs and Q8_1 activation (32 thr x4B -> 8x16B coalesced, SWDEV-556587 vector path 8x fewer transactions); add __builtin_assume_aligned(ptr,16) and ensure hipMalloc 256B alignment. Document offline 16x64 swizzle of IQ4_XS to 128B cache lines (MARLIN reshuffle, cp.async evict_first for B) referencing tools/swizzle_iq4xs.py from 07-02 (reuse, not new file). Add calculator pre-commit gate comment: predict VGPR for each variant (64x32 P2+33, 64x32 P4+XOR, 64x64 P4+XOR, 128x32) via amd_matrix_instruction_calculator -a gfx1100 -i wmma_f32_16x16x16_f16 -d -R --csv; if VGPR>64 then flag P=4 fallback. Ensure no Python shipped: all runtime is C++/HIP, calculator is offline-only. Verify b128 vectorization via llvm-objdump buffer_load_dword vs global_load_b128 pattern.</action>
  <verify>
    <automated>grep -q "global_load_b128\|float4\|ulong2" kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip && grep -q "assume_aligned\|swizzle\|16x64" kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip && grep -q "calculator\|VGPR.*64\|amdgpu_flat" kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip && echo "b128 + swizzle + VGPR gate present"</automated>
  </verify>
  <done>Both GEMM kernels have b128 16B coalescing + 16x64 swizzle docs + calculator VGPR <=64 gate comments; no Python shipped.</done>
</task>

<task type="auto">
  <name>Refactor bench_gemm_wmma to --runs 10 --json with per-variant N=10 table vs real DP4A</name>
  <files>kernels/matmul_iq4xs/bench_gemm_wmma.cpp, kernels/matmul_iq4xs/test_gemm_wmma_compare.cpp</files>
  <action>Refactor kernels/matmul_iq4xs/bench_gemm_wmma.cpp to CLI --runs 10 --json (default 10) and optional --variant and --shapes. For each shape (attn_q 5120x5120, ffn_gate 5120x17408, ffn_down 17408x5120) and each M in {128,512,1024,8192 if VRAM preflight passes, plus rectangular 4096,4096,2048 from adelj88), allocate W, X(K*M), Y(N*M), run gemm_iq4xs_stock_dp4a_gpu (real DP4A MMQ) vs gemm_iq4xs_wmma_stream_gpu (and LUT variant gemm_iq4xs_lut_gpu if TILE_M 64x32 etc) via bench_hip_event with warmup 10, runs 10, 30 iters. Compute BenchStats median/mean/stddev/p95/min/max/gb_s/tflops per variant; flops=2*N*M*K. Compute speedup_median = stock_median / wmma_median per shape/M, and per-variant table: 64x32 P2+33, 64x32 P4+XOR, 64x64 P4+XOR, 128x32, LUT mu=4. Emit JSON array with fields: op="gemm_iq4xs_wmma_stream", shape, K,N,M, bytes, flops, runs=10, variant, tile, P, banking, stock_median_us, stock_mean_us, stock_stddev_us, stock_p95_us, wmma_median_us, wmma_mean_us, wmma_stddev_us, wmma_p95_us, tiled_median_us, speedup_median, speedup_mean_minus_1sigma, TFLOPS_median, GB/s, winner, note. Sorting winner is median tok/s N=10, not single run. Support race.py --repeats 10 interleaved documentation in header (A,B,A,B not AAAA BBBB). Update test_gemm_wmma_compare.cpp to validate numerical parity vs CPU FP64 ref_cpu cosine >=0.999 over 15 shapes, N=10 averaged if stochastic, covering all tile/P/XOR/LUT variants. Gate: >1.2x median vs real stock DP4A at M>=128 (especially M=512 WMMA path), mean-1sigma >1.15x, and TFLOPS reported.</action>
  <verify>
    <automated>HSA_ENABLE_DXG_DETECTION=1 cmake --build kernels/build --target bench_gemm_wmma -j 4 && ./kernels/build/matmul_iq4xs/bench_gemm_wmma --runs 10 --json > /tmp/wmma.json && python3 -c "import json; d=json.load(open('/tmp/wmma.json')); assert len(d)>=9; assert all('wmma_median_us' in x and 'stock_median_us' in x and 'speedup_median' in x for x in d); assert all('stddev_us' in str(x) for x in d); print('bench_gemm_wmma N10 JSON OK', d[0]['variant'] if 'variant' in d[0] else 'no variant field', len(d))" && ./kernels/build/matmul_iq4xs/test_gemm_wmma_compare 2>&1 | grep -q "PASS\|cos"</automated>
  </verify>
  <done>bench_gemm_wmma --runs 10 --json emits per-variant table with median/mean/stddev/p95 + speedup_median + TFLOPS vs real DP4A MMQ for M={128,512,1024,8192} and rectangular, runs=10, winner is median not single-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GPU LDS (WMMA) | sB[2][32][33] vs XOR 0% Banking must be proven lds_bank_conflict 0 |
| host→GPU (GEMM tiling) | TILE_M/N/K bounds and VRAM preflight >2GB before 8192 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | LDS [32][32] naive 4-way conflict | high | mitigate | Always +33 or XOR via preshuffle; verify rocprof lds_bank_conflict 0 on bare metal (WSL2 blind); fallback to +33 if XOR adds cycle |
| T-07-03-02 | Tampering | P=4 pipeline reorder | medium | mitigate | Pin with sched_barrier 0x0080 DS / 0x0008 WMMA per llvm.org AMDGPUUsage; ensures GMEM->VGPR earliest hides while WMMA runs |
| T-07-03-03 | Denial of Service | 8192 tier OOM BSOD | high | mitigate | VRAM preflight >2GB free + hipMalloc probe before bench; mark SKIPPED if fail (3-5 OOMs -> BSOD per RESEARCH); no retry loops |
| T-07-03-SC | Tampering | PIP installs | high | mitigate | No runtime pip; calculator matrix_calculator.py offline pre-commit only |
</threat_model>

<verification>
- impl_gemm_wmma_stream.hip has tile templates 64x32/64x64/128x32, sB[2][32][33] and sB[4][32][32] P=2/P=4 + sched_barrier 0x0080/0x0008 + B-stationary + v_wmma_f32_16x16x16_f16_w32
- impl_gemm_lut_iq4xs.hip exists with LUT mu=4 16-entry half bake
- llvm-objdump --mcpu=gfx1100 shows v_wmma; hipcc --save-temps -Rpass-analysis shows VGPR <=64 per variant (16 waves/SIMD)
- b128 coalescing via global_load_b128/float4/ulong2 + assume_aligned present, 16x64 swizzle referenced
- bench_gemm_wmma --runs 10 --json emits JSON with median/mean/stddev/p95 + speedup_median + TFLOPS per variant vs real DP4A MMQ, runs=10
- test_gemm_wmma_compare 15 shapes cosine >=0.999 N=10
- rocprof lds_bank_conflict 0 gate documented for native bare metal (WSL2 blind noted per RESEARCH)
</verification>

<success_criteria>
1. WMMA streaming GEMM executes hardware matrix cores (v_wmma_f32_16x16x16_f16_w32, wave32, OPSEL, lane%16) with 64x32 base + tile sweeps 64x64/128x32 and achieves >1.2x median (N=10) vs real stock DP4A MMQ at M>=512 with mean-1sigma >1.15x, >950 t/s prefill slice N=10 (REQ-PERF-07 prefill)
2. High-yield GEMM variants explicit: P=2 vs P=4 double buffer with sched_barrier, B-stationary, LUT mu=4 (32B), XOR preshuffle vs +33, b128 float4 16B coalescing, 16x64 swizzle all present and per-variant benchable
3. bench_gemm_wmma --runs 10 --json reports median/mean/stddev/p95 + speedup_median per variant vs real DP4A, pure C++/HIP via HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 clean (REQ-WIN-07, REQ-STAT-07)
</success_criteria>

<output>
Create .planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-03-SUMMARY.md when done
</output>
