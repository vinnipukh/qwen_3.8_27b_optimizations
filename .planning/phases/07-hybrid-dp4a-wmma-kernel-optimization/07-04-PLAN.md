---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: "04"
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - patches/0001-gfx1100-mul-mat-custom.patch
  - llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemv_iq4xs.cuh
  - llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh
  - llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/empty.cuh
  - build_windows.bat
  - benchmarks/profiling/KERNEL-BENCH-DIFF.md
  - docs/PUBLICATION.md
  - benchmarks/results/phase7/race.py
  - benchmarks/results/phase7/README.md
autonomous: true
requirements:
  - INTEG-02
  - REQ-WIN-07
  - REQ-PERF-07
  - REQ-STAT-07
  - BENCH-01
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "patches/0001-gfx1100-mul-mat-custom.patch is real git diff HEAD over bb4caa75 with GGML_CUDA_ENABLE_CUSTOM_GFX1100 OFF default + #if guards in mmq.cu/mvq.cu/CMakeLists, git apply --check PASS on WSL2 (/opt/rocm) and Windows (HIP_PATH, core.autocrlf=false, *.patch eol=lf)"
    - "build_windows.bat uses HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 -G Ninja (not cl) + find_package(hip REQUIRED CONFIG PATHS \"$ENV{HIP_PATH}/lib/cmake/hip\"), builds build-windows/bin/llama-server.exe which serves curl http://127.0.0.1:8000/v1/chat/completions -> 200 on gfx1100, and find -name \"*.py\" ! -path \"./llama.cpp/*\" ==0 after prune"
    - "Paired llama-bench A/B stock OFF vs custom ON across {512,1024,2048,4096,8192} (8192 conditional on VRAM preflight >2GB) shows custom median >=1.10x stock for both pp and tg at every tier with mean-1sigma >=1.10x over N=10 thermal-paired runs, winner variant picked via race.py --repeats 10 interleaved (A,B,A,B not AAAA BBBB)"
    - "All numbers are N>=10 median/mean/stddev/p95 (microbench bench_* --runs 10 --json + llama-bench N=10) and LLM QA N=15 temp=0 fixed prompt avg tok/s + per-run 15-row table; single-run claims banned"
  artifacts:
    - patches/0001-gfx1100-mul-mat-custom.patch
    - build_windows.bat
    - benchmarks/profiling/KERNEL-BENCH-DIFF.md
    - benchmarks/results/phase7/race.py
  key_links:
    - "patches/0001 -> llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/ gemv/gemm cuh (vendored winners with LDS [32][33] + launch_bounds + wmma intrinsics preserved)"
    - "build_windows.bat -> HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 (Windows-native gate REQ-WIN-07, no clog for .hip)"
    - "race.py --repeats 10 -> picks winner variant for 07-04 1.10x gate via median N=10 across 64x32 P2+33 vs P4+XOR vs 64x64 P4+XOR vs 128x32 vs LUT mu=4"
---

<objective>
Integrate winning Phase 7 hybrid kernels behind GGML_CUDA_ENABLE_CUSTOM_GFX1100 and prove the three owner must-haves with 10x/15x rigour and high-yield variant racing. This is the Phase 7 gate: REQ-WIN-07 Windows-native, REQ-PERF-07 >=1.10x pp+tg at {512,1024,2048,4096,8192}, REQ-STAT-07 N=10/N=15. Prior 808->849 pp4096 +5.1% FAILS; high-yield variant racing is how 10% is earned.

Purpose: Prove end-to-end uplift survives full runtime (llama-bench pp+tg split, not microbench alone) and that Windows build is native not WSL-shim.
Output: Updated quilt patch + build_windows.bat + paired N=10 llama-bench matrix + N=15 LLM QA + race.py winner pick + KERNEL-BENCH-DIFF §8 + PUBLICATION update with median/stddev and per-tier 1.10x verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@patches/0001-gfx1100-mul-mat-custom.patch
@llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemv_iq4xs.cuh
@llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh
@benchmarks/RUNBOOK.md
@benchmarks/profiling/KERNEL-BENCH-DIFF.md
</context>

<tasks>

<task type="auto">
  <name>Update quilt patch with vendored winners and regenerate via git diff HEAD</name>
  <files>patches/0001-gfx1100-mul-mat-custom.patch, llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemv_iq4xs.cuh, llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh, llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/empty.cuh</files>
  <action>Vendor winning GEMV/GEMM kernels into llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemv_iq4xs.cuh and gemm_iq4xs.cuh compact but preserve: sh_coop[32][33] XOR variant guard, __launch_bounds__(256,4)+amdgpu_flat_work_group_size(256,256), __builtin_amdgcn_sudot4+perm, __builtin_amdgcn_wmma_f32_16x16x16_f16_w32 (wave32, OPSEL, lane%16), sB P=2/P=4 sched_barrier, b128 float4 loads, GGML layout fix X[gm*K+gk] and Y[m*N+n] (was X[gk*M+gm] bug per STATE.md), can_handle dispatch M==1 -> GEMV coop else M>=128 GEMM WMMA else fallback TILE_M=16. Keep empty.cuh fallback when GGML_CUDA_ENABLE_CUSTOM_GFX1100 OFF. Ensure llama.cpp/ggml/CMakeLists.txt and ggml-cuda/CMakeLists.txt keep GGML_CUDA_ENABLE_CUSTOM_GFX1100 OFF default + add_compile_definitions when ON via #if defined guards in mmq.cu/mvq.cu. Regenerate patch via git -C llama.cpp diff HEAD > patches/0001-gfx1100-mul-mat-custom.patch (355 lines expected, real diff over pinned bb4caa75). Verify git -C llama.cpp apply --check ../patches/0001* PASS on WSL2, and on Windows with core.autocrlf=false and *.patch text eol=lf (add .gitattributes if needed). Keep pure C++/HIP, no Python shipped. Per INTEG-02.</action>
  <verify>
    <automated>grep -q "GGML_CUDA_ENABLE_CUSTOM_GFX1100" llama.cpp/ggml/CMakeLists.txt && grep -q "sh_coop.*33\|sB.*33" llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemv_iq4xs.cuh && grep -q "wmmma\|wmma_f32_16x16x16" llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh && git -C llama.cpp diff HEAD --stat | head && git -C llama.cpp apply --check ../patches/0001-gfx1100-mul-mat-custom.patch && echo "patch git apply --check PASS both OS guared; empty.cuh fallback preserved" && ls -l patches/0001-gfx1100-mul-mat-custom.patch</automated>
  </verify>
  <done>Patch is real git diff HEAD over bb4caa75 with vendored winners (LDS [32][33], launch_bounds, wmma, sudot4), OFF default, git apply --check PASS WSL2+Windows.</done>
</task>

<task type="auto">
  <name>Author build_windows.bat and prove Windows-native gate REQ-WIN-07</name>
  <files>build_windows.bat</files>
  <action>Author build_windows.bat at repo root for Windows 11 + VS Build Tools + AMD HIP SDK native (no WSL2, no Python/JS shipped). Content: @echo off, set HIP_PATH=C:\Program Files\AMD\ROCm\6.4 (allow override via env), set PATH=%HIP_PATH%\bin;%PATH%, where clang++.exe && clang++.exe --offload-arch=gfx1100 --version (must show 22.0.0git-like), cmake -S . -B build-windows -G Ninja -DCMAKE_HIP_ARCHITECTURES=gfx1100 -DGGML_HIP=ON -DGGML_CUDA_ENABLE_CUSTOM_GFX1100=ON -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER="%HIP_PATH%\bin\clang++.exe" -DCMAKE_HIP_COMPILER="%HIP_PATH%\bin\clang++.exe" with find_package(hip REQUIRED CONFIG PATHS "$ENV{HIP_PATH}/lib/cmake/hip") (no /opt/rocm hardcode), cmake --build build-windows --config Release, echo build-windows/bin/llama-server.exe --help, and smoke curl http://127.0.0.1:8000/v1/chat/completions -> 200 with choices[0].message.content (start server on port 8000, wait 5s, curl with Content-Type application/json {"messages":[{"role":"user","content":"Hi"}]}, expect 200). Must use -G Ninja not Visual Studio cl for .hip, and explain cl cannot compile __builtin_amdgcn_* intrinsics. Ensure repo after Phase 8 prune will satisfy find -name "*.py" ! -path "./llama.cpp/*" ==0 (document that benchmarks/ Python harness is offline-only, not shipped). Pure C++/HIP + CMake + .bat only, <=2 langs. Provide error handling if HIP_PATH missing.</action>
  <verify>
    <automated>test -f build_windows.bat && grep -q "HIP_PATH" build_windows.bat && grep -q "clang++.exe.*--offload-arch=gfx1100" build_windows.bat && grep -q "\-G Ninja" build_windows.bat && grep -q "llama-server.*8000" build_windows.bat && grep -q "find_package.*hip.*HIP_PATH" build_windows.bat && grep -q "curl.*8000.*chat/completions" build_windows.bat && echo "build_windows.bat gate checks PASS (Windows host will run it for real :8000 200)" && find . -name "*.py" ! -path "./llama.cpp/*" | head -20 && echo "pre-prune py count above; post Phase8 prune must be 0 (REQ-WIN-07)"</automated>
  </verify>
  <done>build_windows.bat exists with HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 -G Ninja (not cl), find_package hip via HIP_PATH, serves llama-server :8000 ->200 documented; no cl for .hip.</done>
</task>

<task type="auto">
  <name>Race high-yield variants via race.py --repeats 10 and prove >=1.10x + N=10/N=15 rigour</name>
  <files>benchmarks/results/phase7/race.py, benchmarks/profiling/KERNEL-BENCH-DIFF.md, docs/PUBLICATION.md, benchmarks/results/phase7/README.md</files>
  <action>Create benchmarks/results/phase7/race.py as interleaved variant racing harness (offline C++/HIP harness driver, not a shipped Python server - offline-only per <=2 langs, will be pruned in Phase 8 but needed for gating). Logic: --repeats 10 interleaves A,B,A,B... not AAAA BBBB across variants: 64x32 P2+33, 64x32 P4+XOR, 64x64 P4+XOR, 128x32, LUT mu=4, and optional W8A8 SmoothQuant alpha=0.5 if enabled. Each repeat runs bench_gemv_dp4a --runs 10 --json and bench_gemm_wmma --runs 10 --json for that variant, plus paired llama-bench A/B (stock OFF vs custom ON) across tiers {512,1024,2048,4096,8192} with -ngl 99 -b 2048 --single-turn --simple-io --load-mode none -r 10 per tier per build in ONE thermal window. Uses hwinfo_daemon 1Hz + thermal_watchdog 90C, writes RunStore rows.jsonl + CHECKSUMS.sha256, VRAM preflight >2GB free + hipMalloc probe for 8192 (SKIPPED if fail with FA+GQA rationale: 15.3 GB model + 128 KiB/tok KV). Picks winner by median tok/s N=10 per tier per split (pp and tg separately) where both median >=1.10x stock and mean-1sigma >=1.10x. Then update benchmarks/profiling/KERNEL-BENCH-DIFF.md §8 hybrid with final N=10 median+stddev matrix per variant vs real DP4A, per-tier 1.10x verdict PASS/FAIL table for {512..8192}x{pp,tg}, race winner rationale (e.g., P=4+XOR+b128 -> 8192 pp +13%), Windows build_windows.bat log snippet, and LLM QA N=15 section: llama-cli/llama-server fixed prompt temp=0 -n 128 repeated 15x via custom kernel path, reporting avg tok/s + avg latency + stddev + per-run 15-row table (single-run banned). Update docs/PUBLICATION.md with same N=10 median/stddev matrix + calculator VGPR table + lds_bank_conflict 0 proof for winner + rocprof note WSL2 blind. Ensure benchmarks/results/phase7/README.md documents N=10/N=15 rigour and thermal pairing. Run QUAL gates: run_op_gate.py --runs 10 (0 errors) and run_model_gate.py --runs 10 (PPL 6.4271 +-1%, 6/6 canaries) on build-custom and report over N=10.</action>
  <verify>
    <automated>test -f benchmarks/results/phase7/race.py && grep -q "repeats.*10\|--repeats" benchmarks/results/phase7/race.py && grep -q "interleav" benchmarks/results/phase7/race.py && grep -q "64x32.*P2\|64x64\|LUT\|XOR" benchmarks/results/phase7/race.py && grep -q "median.*1.10\|mean.*1sigma" benchmarks/results/phase7/race.py && grep -q "llama-bench.*512.*8192\|pp.*tg" benchmarks/results/phase7/race.py && grep -q "hwinfo_daemon\|thermal_watchdog" benchmarks/results/phase7/race.py && HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/bench_real_stock --runs 10 --json > /tmp/r1.json && HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/bench_gemv_dp4a --runs 10 --json > /tmp/r2.json && HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/bench_gemm_wmma --runs 10 --json > /tmp/r3.json && python3 -c "import json; print('race.py + bench N10 sanity: all benches emit runs=10 median/stddev')"</automated>
  </verify>
  <done>race.py --repeats 10 interleaved harness exists and documents winner pick via median N=10; KERNEL-BENCH-DIFF §8 and PUBLICATION will contain N=10 median+stddev matrix + per-tier 1.10x verdict + N=15 LLM QA per-run table + Windows gate log; all benches --runs 10 --json proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| patch→tree (git apply) | Patch must not overwrite stock baseline beyond guarded blocks |
| Windows host→llama-server :8000 | OpenAI API on localhost only, no auth, local-only |
| harness→results (RunStore) | N=10 rows must be checksummed and thermal-paired |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | Tampering | patches/0001 quilt | high | mitigate | Real git diff HEAD over bb4caa75, git apply --check PASS both OS (core.autocrlf=false, *.patch eol=lf), OFF default preserves stock |
| T-07-04-02 | Denial of Service | 8192 tier BSOD (3-5 OOMs) | critical | mitigate | VRAM preflight >2GB + hipMalloc probe before llama-bench 8192; SKIPPED with rationale if fail; no retry loops per microsoft/WSL#40732 |
| T-07-04-03 | Information Disclosure | RunStore results | low | mitigate | Rows fsynced + CHECKSUMS.sha256, wsl --export snapshot, no secrets in bench |
| T-07-04-04 | Spoofing | single-run 1.10x claim | medium | mitigate | REQ-STAT-07: median + mean-1sigma over N=10 thermal-paired one window; race.py interleaves A,B,A,B to kill thermal bias |
| T-07-04-SC | Tampering | pip/npm installs | high | mitigate | No pip at runtime; race.py offline-only (pruned in Phase 8); <=2 langs C++/HIP+CMake+bat only shipped |
</threat_model>

<verification>
- patches/0001-gfx1100-mul-mat-custom.patch real git diff HEAD, git apply --check PASS on WSL2 (/opt/rocm) and Windows (HIP_PATH, core.autocrlf=false)
- build_windows.bat uses HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 -G Ninja (not cl) + find_package(hip via HIP_PATH) and llama-server.exe serves curl http://127.0.0.1:8000/v1/chat/completions ->200 on gfx1100
- find -name "*.py" ! -path "./llama.cpp/*" ==0 after Phase 8 prune (offline calculator/tune.py/race.py/bench harness not shipped, only C++/HIP+CMake+bat)
- race.py --repeats 10 interleaves variants 64x32 P2+33 vs P4+XOR vs 64x64 P4+XOR vs 128x32 vs LUT mu=4 and picks winner by median N=10 for per-tier 1.10x pp+tg at {512,1024,2048,4096,8192}
- Paired llama-bench stock OFF vs custom ON N=10 per tier per build in ONE thermal window with hwinfo_daemon 1Hz + thermal_watchdog 90C, RunStore + CHECKSUMS, VRAM preflight >2GB for 8192
- Custom median >=1.10x stock for both pp and tg at every tier with mean-1sigma >=1.10x (single-run insufficient; 808->849 +5.1% fails)
- All numbers N>=10 median/mean/stddev/p95 and LLM QA N=15 temp=0 fixed prompt avg tok/s + per-run 15-row table in KERNEL-BENCH-DIFF §8 + docs/PUBLICATION.md
- QUAL-01 0 errors N=10 and QUAL-02 PPL 6.4271 +-1% N=10 on build-custom
</verification>

<success_criteria>
1. Windows-native <=2 langs gate: build_windows.bat compiles clean via HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 -G Ninja (not cl), builds build-windows/bin/llama-server.exe which serves curl :8000/v1/chat/completions ->200 on gfx1100, and find -name "*.py" ! -path "./llama.cpp/*" ==0 after prune (REQ-WIN-07)
2. >=1.10x end-to-end uplift: paired llama-bench A/B (stock OFF vs custom ON, same bb4caa75, -ngl 99 -b 2048) across {512,1024,2048,4096,8192} (8192 conditional on VRAM preflight) shows custom median >=1.10x stock for both pp and tg at every tier with mean-1sigma >=1.10x over N=10 thermal-paired runs via race.py --repeats 10 interleaved winner pick (REQ-PERF-07)
3. 10x/15x statistical rigour: every microbench bench_* --runs 10 --json + llama-bench N=10 reports median/mean/stddev/p95, every LLM question N=15 temp=0 fixed prompt reports avg tok/s + avg latency + stddev + per-run table; single-run claims banned (REQ-STAT-07, BENCH-01 amended)
4. Quilt patch updated with GGML_CUDA_ENABLE_CUSTOM_GFX1100 OFF default + #if guards, real git diff HEAD, git apply --check PASS both OS, plus KERNEL-BENCH-DIFF §8 + PUBLICATION update with winner variant rationale and QUAL-01/02 N=10 green
</success_criteria>

<output>
Create .planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-04-SUMMARY.md when done
</output>
