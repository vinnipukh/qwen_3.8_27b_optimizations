# Plan 07-04: In-Tree Quilt Overlay & Paired End-to-End A/B Validation

**Goal:** Integrate winning Phase 7 hybrid kernels into `llama.cpp` behind `GGML_CUDA_ENABLE_CUSTOM_GFX1100=ON` and demonstrate measurable end-to-end tokens/s uplift in `llama-bench`.

**Deliverables:**
- Update `patches/0001-gfx1100-mul-mat-custom.patch` with hybrid DP4A GEMV and WMMA GEMM headers.
- Build `build-custom` (`-DGGML_CUDA_ENABLE_CUSTOM_GFX1100=ON`).
- Run `run_op_gate.py` (0 errors across 4200+ ops) and `run_model_gate.py` (WikiText-2 PPL 6.4271 and 6/6 canaries).
- Run paired `llama-bench` sweep across context tiers {512, 1024, 2048, 4096} comparing stock vs custom:
  - Assert custom decode ($M=1$) tok/s > stock baseline.
  - Assert custom prefill ($M \ge 128$) tok/s > stock baseline.
- Update `KERNEL-BENCH-DIFF.md` and `docs/PUBLICATION.md` with final real-stock vs hybrid-custom matrix.
