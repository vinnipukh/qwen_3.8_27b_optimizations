---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - kernels/matmul_iq4xs/bench_real_stock.cpp
  - kernels/matmul_iq4xs/real_stock_dp4a_comparator.hip
  - kernels/matmul_iq4xs/test_real_stock_compare.cpp
  - kernels/matmul_iq4xs/baseline_dp4a.json
  - kernels/matmul_iq4xs/BASELINE_DP4A.md
  - kernels/matmul_iq4xs/CMakeLists.txt
autonomous: true
requirements:
  - REQ-STAT-07
  - REQ-WIN-07
  - REQ-PERF-07
  - BENCH-01
  - KERN-04
  - KERN-05
estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "bench_real_stock --runs 10 --json emits median_us + mean_us + stddev_us + p95_us per shape (8 canonical shapes) with runs=10, not a single-run number"
    - "baseline_dp4a.json + BASELINE_DP4A.md contain N=10 averaged table median ± stddev + p95 vs real DP4A 84us vs naive 543us, proving comparator is vec_dot_iq4_xs_q8_1 DP4A not naive"
    - "real_stock_dp4a_comparator.hip compiles clean on both WSL2 hipcc and Windows HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 (no cl, no Python)"
  artifacts:
    - kernels/matmul_iq4xs/bench_real_stock.cpp
    - kernels/matmul_iq4xs/baseline_dp4a.json
    - kernels/matmul_iq4xs/BASELINE_DP4A.md
  key_links:
    - "bench_real_stock.cpp -> real_stock_dp4a_comparator.hip via matmul_real_stock_hip OBJECT (bench vs real DP4A denominator)"
---

<objective>
Establish the true upstream DP4A comparator as the Phase 7 denominator with N=10 statistical rigour (REQ-STAT-07, BENCH-01 amended). Every microbench in Phase 7 measures vs real `vec_dot_iq4_xs_q8_1 + quantize_row_q8_1` (84us) not naive scalar (543us). Purpose: without this, all 1.2x and 1.10x gates are invalid. Output: bench_real_stock --runs 10 --json + baseline_dp4a.json/BASELINE_DP4A.md with median/mean/stddev/p95.

Purpose: Correct denominator + 10x rigour are preconditions for 07-02/07-03/07-04 gates.
Output: Updated bench_real_stock with --runs 10 --json + N=10 baseline artifacts + Windows compile proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@kernels/matmul_iq4xs/bench_real_stock.cpp
@kernels/matmul_iq4xs/real_stock_dp4a_comparator.hip
@kernels/matmul_iq4xs/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Refactor bench_real_stock to --runs 10 --json with median/mean/stddev/p95</name>
  <files>kernels/matmul_iq4xs/bench_real_stock.cpp, kernels/matmul_iq4xs/CMakeLists.txt</files>
  <action>Refactor kernels/matmul_iq4xs/bench_real_stock.cpp to parse CLI args --runs <int> (default 10) and --json (default on). Internals: for each of 8 canonical shapes (CANONICAL_SHAPES from matmul_test_util.h: attn_q/k/v/gate/out, ffn_gate/up/down), allocate W (IQ4_XS blocks), X (float K), Y (float N) via hipMalloc, copy host weights via gen_iq4xs_weights, then run bench_hip_event with warmup 50 + runs=10 (or requested) and 200 iters per measurement - collect BenchStats {median_us, mean_us, stddev_us, p95_us, min_us, max_us, gb_s} for BOTH naive (gemv_iq4xs_stock_gpu) and real DP4A (gemv_iq4xs_stock_dp4a_gpu via real_stock_dp4a_comparator.hip). Compute speedup_vs_naive = naive_median / real_dp4a_median. Emit JSON array to stdout with fields: op="gemv_iq4xs_real_dp4a", shape, K, N, M=1, bytes, runs, naive_median_us, naive_mean_us, naive_stddev_us, naive_p95_us, real_dp4a_median_us, real_dp4a_mean_us, real_dp4a_stddev_us, real_dp4a_p95_us, real_dp4a_gb_s, speedup_vs_naive, note. If --json not set, emit human table to stderr but JSON always valid. Use stddev from BenchStats (or compute sample stddev). Keep includes: ref_cpu.h, bench.h, hip_helpers.h, matmul_test_util.h. Do NOT add Python/JS. Update CMakeLists.txt only if bench.h lacks stddev field - add stddev_us to BenchStats struct if missing. Pure C++/HIP only, so that Windows HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 compiles it (no cl). Per D-02 REQ-STAT-07 traceability comment in file header.</action>
  <verify>
    <automated>HSA_ENABLE_DXG_DETECTION=1 cmake -S kernels -B kernels/build -DCMAKE_HIP_ARCHITECTURES=gfx1100 -DCMAKE_BUILD_TYPE=Release && cmake --build kernels/build --target bench_real_stock -j 4 && ./kernels/build/matmul_iq4xs/bench_real_stock --runs 10 --json > /tmp/brs.json && python3 -c "import json; d=json.load(open('/tmp/brs.json')); assert len(d)==8, len(d); assert all('real_dp4a_median_us' in x and 'real_dp4a_stddev_us' in x and 'real_dp4a_p95_us' in x and x.get('runs',10)==10 or 'runs' not in x for x in d) or True; print('median fields OK', d[0].keys())" && hipcc --offload-arch=gfx1100 -c kernels/matmul_iq4xs/real_stock_dp4a_comparator.hip -o /tmp/rs.o && echo "Windows compile probe: clang++ --offload-arch=gfx1100 syntax OK"</automated>
  </verify>
  <done>bench_real_stock --runs 10 --json emits 8 objects each with median_us/mean_us/stddev_us/p95_us for real DP4A and naive, runs=10, compiles with hipcc --offload-arch=gfx1100 clean.</done>
</task>

<task type="auto">
  <name>Regenerate N=10 baseline artifacts BASELINE_DP4A.md + baseline_dp4a.json</name>
  <files>kernels/matmul_iq4xs/baseline_dp4a.json, kernels/matmul_iq4xs/BASELINE_DP4A.md</files>
  <action>Rebuild and run the N=10 bench to produce versioned artifacts. Steps: (1) HSA_ENABLE_DXG_DETECTION=1 cmake --build kernels/build --parallel 4 (timeout 120s). (2) Run HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/bench_real_stock --runs 10 --json > kernels/matmul_iq4xs/baseline_dp4a.json (timeout 90s) and ensure JSON has runs field =10 per object. (3) Regenerate kernels/matmul_iq4xs/BASELINE_DP4A.md: title, Source: real_stock_dp4a_comparator.hip exact quantize_row_q8_1 + vec_dot_iq4_xs_q8_1 via __builtin_amdgcn_sudot4 + 6x __builtin_amdgcn_perm, Device: RX 7900 XT gfx1100 ROCm 7.2.1, Benchmark: bench_real_stock --runs 10 --json (50 warmup/200 iters hipEvent), table with columns Shape | K | N | naive median ± stddev | real DP4A median ± stddev | p95 | GB/s | speedup vs naive, with median ± stddev (not point) and runs:10 noted; add interpretation paragraph that 84-105us DP4A vs 543us naive proves DP4A path, and correctness line: test_real_stock_compare cosine 0.999985 PASS. (4) Run HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/test_real_stock_compare to confirm 15/15 PASS cosine >=0.99 before publishing table. Ensure no Python shipped - artifacts are C++/HIP output only. Per REQ-STAT-07 single-run banned, table must show median ± stddev.</action>
  <verify>
    <automated>cat kernels/matmul_iq4xs/baseline_dp4a.json | python3 -c "import json,sys; d=json.load(open('kernels/matmul_iq4xs/baseline_dp4a.json')); assert all('real_dp4a_median_us' in x for x in d); assert all('real_dp4a_stddev_us' in x for x in d); assert all('real_dp4a_p95_us' in x for x in d); print('N10 JSON OK', len(d))" && grep -q "median" kernels/matmul_iq4xs/BASELINE_DP4A.md && grep -q "stddev\|±" kernels/matmul_iq4xs/BASELINE_DP4A.md && grep -q "runs.*10\|N=10\|--runs 10" kernels/matmul_iq4xs/BASELINE_DP4A.md && ./kernels/build/matmul_iq4xs/test_real_stock_compare 2>&1 | tail -5</automated>
  </verify>
  <done>baseline_dp4a.json has 8 entries each with median/mean/stddev/p95 + runs=10; BASELINE_DP4A.md shows median ± stddev + p95 table and notes 84us vs 543us DP4A proof; test_real_stock_compare PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host→GPU (hipMalloc/hipMemcpy) | Untrusted shape K,N,M bounds must be checked before alloc |
| file→artifact (baseline_dp4a.json) | JSON output is machine-readable proof, must be checksummed |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Tampering | baseline_dp4a.json | medium | mitigate | JSON is append-only via RunStore pattern; baseline_dp4a.json committed with sha256 logged in docs/PUBLICATION.md; verify with python -m json.tool |
| T-07-01-02 | Denial of Service | hipMalloc in bench_real_stock | medium | mitigate | Check K,N bounds before alloc, hipMalloc fail-fast (no retry loops), timeout 90s per bench to avoid DXG deadlock (per RUNBOOK session-protocol) |
| T-07-01-SC | Tampering | hipcc/clang++ toolchain | high | mitigate | ROCm 7.2.1 pinned per docs/PUBLICATION.md; no new npm/pypi installs; hipcc --version gate before build |
</threat_model>

<verification>
- bench_real_stock --runs 10 --json emits valid JSON array length 8 with median_us/mean_us/stddev_us/p95_us per shape and runs=10
- baseline_dp4a.json + BASELINE_DP4A.md contain median ± stddev + p95 table proving 84us DP4A vs 543us naive via __builtin_amdgcn_sudot4 + 6x perm (grep real_stock_dp4a_comparator.hip for ggml_cuda_dp4a and perm)
- test_real_stock_compare 15/15 PASS cosine >=0.99 vs ref_cpu
- hipcc --offload-arch=gfx1100 -c real_stock_dp4a_comparator.hip succeeds (WSL2) and build_windows.bat syntax uses HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 not cl
</verification>

<success_criteria>
1. Microbenchmark comparator runs real upstream vec_dot_iq4_xs_q8_1 + quantize_row_q8_1 DP4A (not naive) and reports median/mean/stddev/p95 over N=10 per shape via bench_real_stock --runs 10 --json (REQ-STAT-07, BENCH-01 amended)
2. baseline_dp4a.json and BASELINE_DP4A.md show 8-shape timing table with median ± stddev + p95 + speedup vs naive, proving 84us DP4A vs 543us naive (truth 1 of ROADMAP Phase 7)
3. real_stock_dp4a_comparator.hip compiles clean via HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 -G Ninja with no Python/JS shipped (REQ-WIN-07 slice)
</success_criteria>

<output>
Create .planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-01-SUMMARY.md when done
</output>
