# Plan 07-01: True Upstream DP4A Microbenchmark Comparator

**Goal:** Establish an accurate, direct microbenchmark comparator in `kernels/matmul_iq4xs/` running real upstream `llama.cpp` DP4A routines (`vec_dot_iq4_xs_q8_1` + `quantize_row_q8_1`).

**Deliverables:**
- `kernels/matmul_iq4xs/real_stock_dp4a_comparator.hip`: Implements the exact upstream single-warp MMVQ DP4A loop and Q8_1 activation quantizer.
- Update `test_stock_compare.cpp` and `bench_gemv.cpp` to benchmark against the real DP4A stock implementation in addition to the CPU oracle.
- Baseline timing table capturing true stock DP4A latency for all 8 canonical Qwen projection shapes.

**Verification:**
- Microbenchmark binary runs and matches upstream per-op latency profile (~20–40 µs range rather than 550 µs naive scalar).
