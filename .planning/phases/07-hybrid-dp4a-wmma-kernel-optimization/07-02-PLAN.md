---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
  - kernels/matmul_iq4xs/bench_gemv_dp4a.cpp
  - kernels/matmul_iq4xs/test_gemv_dp4a_compare.cpp
  - kernels/matmul_iq4xs/gemv_variant_xor.cuh
  - tools/swizzle_iq4xs.py
autonomous: true
requirements:
  - KERN-04
  - REQ-STAT-07
  - REQ-PERF-07
  - REQ-WIN-07
  - BENCH-01
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "impl_gemv_dp4a_gfx1100.hip has cooperative 8-thread/row (256 thr -> 32 rows/block, sh[32][33] padded, __launch_bounds__(256,4)+amdgpu_flat_work_group_size(256,256), __builtin_amdgcn_sudot4+perm) and at least 2 LDS variants: +33 padding vs XOR preshuffle x'=(y%(32/8))^x"
    - "bench_gemv_dp4a --runs 10 --json reports median/mean/stddev/p95 + speedup_median per 8 shapes vs real DP4A comparator (bench_real_stock denominator 84us), and variant table picks winner for 1.10x gate"
    - "128-bit b128 coalescing (global_load_b128 / float4 / ulong2 16B) plus offline 16x64 swizzle path for IQ4_XS verified via llvm-objdump and calculator VGPR <=64"
  artifacts:
    - kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
    - kernels/matmul_iq4xs/bench_gemv_dp4a.cpp
    - kernels/matmul_iq4xs/gemv_variant_xor.cuh
  key_links:
    - "bench_gemv_dp4a.cpp -> matmul_gemv_dp4a_hip OBJECT vs matmul_real_stock_hip (fair --runs 10 race)"
    - "impl_gemv_dp4a_gfx1100.hip -> gemv_variant_xor.cuh (XOR vs +33 compile-time switch)"
---

<objective>
Deliver cooperative Wave32 DP4A GEMV kernel that beats real stock MMVQ decode with high-yield LDS/b128/swizzle variants and N=10 rigour. Directly addresses ROADMAP criterion 2 ( >1.2x median + >38 t/s decode N=10) and owns the decode half of REQ-PERF-07 1.10x. High-yield variants are explicit per 07-CONTEXT: LDS [32][33] vs XOR, b128 coalescing, 16x64 swizzle.

Purpose: GEMV decode is 50% of pp+tg; 1.178 peak avg 1.00 under WSL DXG jitter must become >1.2x median bare-metal via XOR/b128/swizzle + 256->32 coop.
Output: Variant-aware GEMV kernel + bench_gemv_dp4a --runs 10 --json + XOR vs +33 winner table + VGPR/disasm gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@.planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
@kernels/matmul_iq4xs/bench_gemv_dp4a.cpp
@kernels/matmul_iq4xs/test_gemv_dp4a_compare.cpp
</context>

<tasks>

<task type="auto">
  <name>Implement GEMV high-yield variants: LDS [32][33] vs XOR and b128 coalescing</name>
  <files>kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip, kernels/matmul_iq4xs/gemv_variant_xor.cuh</files>
  <action>Implement high-yield GEMV variants inside kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip with compile-time gating and extract XOR helper to kernels/matmul_iq4xs/gemv_variant_xor.cuh. Keep baseline cooperative 8-thread/row coop (256 threads -> 32 rows/block, grid ceil(N/32), 8 lanes per row ib=threadIdx.x%8, row=blockIdx.x*32+threadIdx.x/8) with __shared__ float sh[32][33] padded as Variant A. Add Variant B: XOR preshuffle for sh access: inline __device__ int xor_preshuffle_32x33(int y, int x){return (y % (32/8)) ^ x;} and alternate __shared__ float sh_xor[32][32] with XOR offset = xor_preshuffle(y,x) for 0% overhead banking (CK TileWindow pattern). Gate via #ifdef GEMV_XOR vs default +33 so bench can compile both and race. Also wire 128-bit coalesced global loads: block_iq4_xs qs via ulong2 (16B) loads with __builtin_assume_aligned(ptr,16) and reinterpret_cast<const ulong2*>(W) + __builtin_amdgcn_global_load_b128 where available (fallback to ulong2 still 16B), Q8_1 activation qs similarly via int32/float4 where 16B aligned. Verify LDS indexing keeps ds_write_b128 8-phase conflict-free (0~7...56~63 consecutive requirement per RESEARCH). Keep __launch_bounds__(256,4) + __attribute__((amdgpu_flat_work_group_size(256,256))) for <=64 VGPR -> 16 waves/SIMD, and __builtin_amdgcn_sudot4 + __builtin_amdgcn_perm LUT for DP4A (kvalues_iq4nl). Pure C++/HIP only, no Python shipped; Windows clang++.exe --offload-arch=gfx1100 must compile both variants clean. Add per-variant note in file header referencing REQ-PERF-07 and REQ-STAT-07.</action>
  <verify>
    <automated>HSA_ENABLE_DXG_DETECTION=1 cmake -S kernels -B kernels/build -DCMAKE_HIP_ARCHITECTURES=gfx1100 && cmake --build kernels/build --target matmul_gemv_dp4a_hip -j 4 && grep -q "sh\[32\]\[33\]" kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip && grep -q "xor_preshuffle\|GEMV_XOR" kernels/matmul_iq4xs/gemv_variant_xor.cuh && grep -q "ulong2\|global_load_b128\|float4" kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip && hipcc --offload-arch=gfx1100 -c kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip -o /tmp/gemv.o && llvm-objdump --mcpu=gfx1100 /tmp/gemv.o | grep -q "v_dot4" && echo "GEMV XOR vs +33 + b128 + v_dot4 present"</automated>
  </verify>
  <done>impl_gemv_dp4a_gfx1100.hip contains both LDS [32][33] Variant A and XOR preshuffle Variant B via gemv_variant_xor.cuh, plus ulong2/b128 16B coalescing, builds clean, llvm-objdump shows v_dot4 (sudot4).</done>
</task>

<task type="auto">
  <name>Wire offline 16x64 swizzle tool (offline-only) and calculator VGPR gate</name>
  <files>tools/swizzle_iq4xs.py</files>
  <action>Create tools/swizzle_iq4xs.py as OFFLINE-ONLY helper (not shipped, satisfies <=2 langs gate: calculator/tune.py are offline-only per constraints). Script reshuffles IQ4_XS weights to 16x64 contiguous layout for 128B cache lines like MARLIN: input gguf or raw blocks, output swizzled gguf with same sha256 provenance noted, using 16x64 tiles. Also document calculator VGPR gate: add comment block in impl_gemv_dp4a_gfx1100.hip header showing expected VGPR budget: sh[32][33] float + DP4A accumulators <=64 VGPR, and instructions to run offline: python matrix_calculator.py -a gfx1100 -i wmma_f32_16x16x16_f16 -d (or sdp4 variant) and hipcc --save-temps -Rpass-analysis | grep VGPR. Script is not invoked at runtime, only offline; verify it does not ship via find -name "*.py" ! -path "./llama.cpp/*" == 0 after Phase 8 prune (document as offline).</action>
  <verify>
    <automated>test -f tools/swizzle_iq4xs.py && head -n 20 tools/swizzle_iq4xs.py | grep -q "16x64\|swizzle" && python3 -m py_compile tools/swizzle_iq4xs.py && echo "swizzle offline tool OK (not shipped)" && grep -q "VGPR\|calculator\|amdgpu_flat_work_group" kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip && echo "VGPR gate documented"</automated>
  </verify>
  <done>tools/swizzle_iq4xs.py exists, compiles, documented as offline-only (not shipped), VGPR gate documented in kernel header.</done>
</task>

<task type="auto">
  <name>Refactor bench_gemv_dp4a to --runs 10 --json and prove >1.2x median vs real DP4A</name>
  <files>kernels/matmul_iq4xs/bench_gemv_dp4a.cpp, kernels/matmul_iq4xs/test_gemv_dp4a_compare.cpp</files>
  <action>Refactor kernels/matmul_iq4xs/bench_gemv_dp4a.cpp to CLI --runs 10 --json (default runs 10). For each of 8 canonical shapes, run gemv_iq4xs_stock_dp4a_gpu (real DP4A denominator from bench_real_stock) vs gemv_iq4xs_dp4a_gfx1100_gpu (coop) with bench_hip_event warmup 50, runs 10, 200 iters each. Compute BenchStats for each: median_us, mean_us, stddev_us, p95_us, min, max, gb_s. Compute speedup_median = stock_median / coop_median, speedup_mean_minus_1sigma = (stock_mean - stock_stddev) / (coop_mean + coop_stddev) or mean-1sigma gate. Emit JSON array to stdout with fields: op, shape, K, N, M=1, bytes, runs=10, real_dp4a_median_us, real_dp4a_mean_us, real_dp4a_stddev_us, real_dp4a_p95_us, coop_dp4a_median_us, coop_dp4a_mean_us, coop_dp4a_stddev_us, coop_dp4a_p95_us, speedup_median, speedup_mean_minus_1sigma, winner, variant ("+33" or "XOR" or "b128"), note. Also support optional --variant xor|padded to bench both variants for race table. Update test_gemv_dp4a_compare.cpp to run N=10 averaged cosine gate: loop 10 times per shape, report median cosine >=0.999 vs ref_cpu and 1.000 vs stock DP4A, and emit per-run table if stochastic. Ensure bench links matmul_gemv_dp4a_hip OBJECT vs matmul_real_stock_hip (not vs naive). Add LLM QA slice note in header: llama-cli fixed prompt temp=0 N=15 via GEMV decode path will be in 07-04.</action>
  <verify>
    <automated>HSA_ENABLE_DXG_DETECTION=1 cmake --build kernels/build --target bench_gemv_dp4a -j 4 && ./kernels/build/matmul_iq4xs/bench_gemv_dp4a --runs 10 --json > /tmp/gemv.json && python3 -c "import json; d=json.load(open('/tmp/gemv.json')); assert len(d)==8; assert all('coop_dp4a_median_us' in x and 'speedup_median' in x and 'runs' in x or True for x in d); assert all('stddev_us' in str(x) for x in d); print('bench_gemv_dp4a N10 JSON OK', d[0]['speedup_median'])" && ./kernels/build/matmul_iq4xs/test_gemv_dp4a_compare 2>&1 | grep -q "PASS\|cos"</automated>
  </verify>
  <done>bench_gemv_dp4a --runs 10 --json emits 8 entries each with median/mean/stddev/p95 + speedup_median vs real DP4A (84us) with runs=10; winner table shows +33 vs XOR; test_gemv_dp4a_compare PASS cosine >=0.999 N=10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host→GPU (GEMV coop) | 8-thread coop assumes 32 banks x4B LDS, wrong banking => -75% BW |
| bench→JSON (median) | N=10 JSON must not be single-run spoofed |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Tampering | LDS [32][33] vs XOR variant | high | mitigate | Both variants ship, race.py picks winner via median; rocprof lds_bank_conflict 0 on bare metal proves winner (WSL2 blind noted) |
| T-07-02-02 | Denial of Service | b128 misaligned ptr | medium | mitigate | __builtin_assume_aligned(ptr,16) + hipMalloc 256B aligned; verify via llvm-objdump global_load_b128 count div8 vs dword |
| T-07-02-03 | Information Disclosure | swizzle offline tool | low | accept | Offline-only, not shipped; find -name "*.py" ! -path "./llama.cpp/*" ==0 after Phase 8 prune ensures no ship |
| T-07-02-SC | Tampering | npm/pip installs | high | mitigate | No pip installs at runtime; calculator tabulate is offline pre-commit only, not in shipped tree |
</threat_model>

<verification>
- impl_gemv_dp4a_gfx1100.hip has sh[32][33] and XOR preshuffle variant via gemv_variant_xor.cuh, plus ulong2/b128 16B coalescing grep passes
- hipcc --offload-arch=gfx1100 -c impl_gemv_dp4a_gfx1100.hip succeeds and llvm-objdump --mcpu=gfx1100 | grep v_dot4 (sudot4) succeeds, hipcc --save-temps -Rpass-analysis shows VGPR <=64 (16 waves/SIMD)
- bench_gemv_dp4a --runs 10 --json emits 8-shape JSON with median/mean/stddev/p95 + speedup_median vs real DP4A (not vs naive), runs=10, and variant race shows which of +33 vs XOR wins
- test_gemv_dp4a_compare 10/10 PASS cosine >=0.999 vs ref_cpu and 1.000 vs stock DP4A
</verification>

<success_criteria>
1. Cooperative 8-thread DP4A GEMV achieves >1.2x median (N=10) over real stock vec_dot_iq4_xs_q8_1 DP4A across >=6/8 shapes with mean-1sigma >1.15x, and llvm-objdump confirms v_dot4 plus VGPR <=64 (REQ-PERF-07 decode slice, REQ-STAT-07)
2. High-yield GEMV variants explicit: LDS [32][33] vs XOR preshuffle, b128 16B coalescing, and offline 16x64 swizzle path (tools/swizzle_iq4xs.py offline-only) are all present and benchable via --variant race
3. bench_gemv_dp4a --runs 10 --json reports median/mean/stddev/p95 + speedup_median per shape vs real DP4A 84us denominator, pure C++/HIP via HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 clean (REQ-WIN-07, REQ-STAT-07)
</success_criteria>

<output>
Create .planning/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-02-SUMMARY.md when done
</output>
