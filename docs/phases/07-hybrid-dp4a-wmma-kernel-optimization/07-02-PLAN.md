---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip
  - kernels/matmul_iq4xs/gemv_variant_xor.cuh
  - kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip
  - kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip
  - kernels/matmul_iq4xs/bench_gemv_dp4a.cpp
  - kernels/matmul_iq4xs/bench_gemm_wmma.cpp
  - kernels/matmul_iq4xs/CMakeLists.txt
  - kernels/common/hip_helpers.h
  - kernels/common/bench.h
  - benchmarks/results/phase7/race.py
  - benchmarks/results/phase7/hwinfo_daemon.py
  - benchmarks/results/phase7/thermal_watchdog.py
  - tools/swizzle_iq4xs.py
autonomous: true
requirements: ["REQ-PERF-07", "REQ-STAT-07", "KERN-04", "KERN-05", "BENCH-01"]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Five GEMM high-yield variants (64x32 P2+33, 64x32 P4_XOR 0% LDS, 64x64 B-stationary 64x reuse, LUT mu=4 16-entry half, 128x32) plus two GEMV variants (P2+33 vs XOR) each compile as distinct HIP OBJECTs with distinct gemm_iq4xs_*_gpu / gemv_* symbols, soft HIP_CHECK log-and-skip on 8192 OOM (no abort), VGPR <=64 ->16 waves, sched_barrier 0x0080/0x0008 pinned, b128 global_load_b128/float4/ulong2 16B + 16x64 offline swizzle wired"
    - "Microbenchmark race via race.py --repeats 10 interleaved A,B,A,B across M=512/1024/2048/4096 (8192 SKIPPED via VRAM preflight >2GB + hipMalloc probe, soft HIP_CHECK) with bench_* --runs 10 --json per variant reports median/mean/stddev/p95 + speedup_median + speedup_mean_minus_1sigma + TFLOPS vs real DP4A 99us, VGPR + llvm-objdump v_dot4/v_wmma + rocprof lds_bank_conflict 0 (bare-metal; WSL2 blind noted) per variant, winner picked by median then mean-1sigma"
    - "Paired llama-bench N=10 stock OFF vs custom ON (same bb4caa75, -ngl 99 -b 2048 --single-turn --simple-io --load-mode none) across {512,1024,2048,4096} + 8192 SKIPPED in ONE thermal window with hwinfo 1Hz + watchdog 90C, RunStore rows.jsonl + CHECKSUMS.sha256, proving >=1.10x median AND mean-1sigma for BOTH pp and tg at every measured tier (or honest per-tier FAIL table 512 1.079 baseline), pure C++/HIP kernels only, KERNEL-BENCH-DIFF.md §8 + docs/PUBLICATION.md updated with N=10 tables and no fabricated 1.10x"
  artifacts:
    - "kernels/matmul_iq4xs/bench_gemv_dp4a.hardware.json (valid JSON, runs:10 per entry, no synthetic jitter, XOR vs +33 as separate OBJECTs)"
    - "kernels/matmul_iq4xs/bench_gemm_wmma.hardware.json (valid JSON >12288B, not truncated, incremental fflush, 5 variants x 3 shapes x 4 Ms, 8192 SKIPPED entries with FA+GQA rationale)"
    - "benchmarks/results/phase7/race_variant_summary.json + bench_gemv/gemm N=10 per-variant winner table (median+mean-1sigma, VGPR, disasm, lds_bank_conflict)"
    - "benchmarks/results/phase7/ab_stock_N10.jsonl + ab_custom_N10.jsonl + rows.jsonl + CHECKSUMS.sha256 + hwinfo.csv + thermal_watchdog.log (one thermal window, interleaved race)"
    - "benchmarks/profiling/KERNEL-BENCH-DIFF.md §8 (per-variant + per-tier 1.10x verdict with M1024 1.08 avg 1.89 peak vs 0.70 context, 512 1.079 FAIL not hidden)"
  key_links:
    - "CMake variant OBJECTs -> distinct gemm_iq4xs_*_gpu symbols -> bench_gemm_wmma.cpp variant_launch dispatch (no synthetic v_median*=0.97 jitter)"
    - "hip_helpers.h soft HIP_CHECK -> bench skips 8192 tier -> JSON SKIPPED with VRAM preflight note -> race.py respects SKIPPED"
    - "Custom vendored gemv/gemm cuh in llama.cpp custom_gfx1100/ with real can_handle (not return false) -> quilt patch -> paired llama-bench custom ON actually dispatches WMMA/GEMV winner"
    - "race.py --repeats 10 interleaved A,B,A,B -> hwinfo_daemon 1Hz + thermal_watchdog 90C one window -> RunStore rows.jsonl + CHECKSUMS proves median and mean-1sigma >=1.10x"
---

<objective>
Race high-yield kernel variants to close REQ-PERF-07 >=1.10x pp+tg. Starting from bare-metal reality (bench_gemv avg 0.97 peak 1.16, bench_gemm M1024 avg 1.08 peak 1.89 vs M512 0.70, llama-bench best 1.079 at 512 FAIL), implement P=4 XOR 0% LDS, b128 float4/ulong2 16B, 16x64 swizzle, 64x64 B-stationary 64x reuse, LUT mu=4 16-entry half, MARLIN P=4 pipeline + CK Tile banking, VGPR <=64 ->16 waves, sched_barrier 0x0080/0x0008 across 64x32 P2+33 / P4_XOR / 64x64 / LUT / 128x32 at M=512/1024/2048/4096 with soft HIP_CHECK and 8192 SKIPPED. Prove >=1.10x median+mean-1sigma via N=10 paired llama-bench one thermal window hwinfo 1Hz watchdog 90C RunStore+CHECKSUMS. Pure C++/HIP.

Purpose: Close the 5-8% gap (1.05-1.079 -> 1.10). Current best 512 1.079 FAIL and M1024 1.08 avg 1.89 peak show variance is the enemy; P=4+XOR+b128+swizzle targets the 64x reuse and LDS 75% BW loss that flattens median under DXG jitter 15-30us p95 148-343us.
Output: Valid N=10 microbench JSONs per variant, variant winner table with VGPR/disasm/lds gates, paired llama-bench A/B N=10 one-window dataset with >=1.10x verdict (or honest per-tier FAIL), updated KERNEL-BENCH-DIFF §8 + PUBLICATION.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/PROJECT.md
@docs/ROADMAP.md
@docs/STATE.md
@docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-VERIFICATION.md
@kernels/matmul_iq4xs/CMakeLists.txt
@kernels/matmul_iq4xs/bench_gemm_wmma.cpp
@kernels/matmul_iq4xs/bench_gemv_dp4a.cpp
@kernels/common/hip_helpers.h
@benchmarks/results/phase7/race.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: wire 5 GEMM + 2 GEMV high-yield variants as distinct HIP OBJECTs with soft HIP_CHECK</name>
  <files>kernels/matmul_iq4xs/CMakeLists.txt, kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip, kernels/matmul_iq4xs/gemv_variant_xor.cuh, kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip, kernels/matmul_iq4xs/impl_gemm_lut_iq4xs.hip, kernels/common/hip_helpers.h, kernels/common/bench.h, tools/swizzle_iq4xs.py</files>
  <action>Wire the high-yield synthesis as real compiled code, not comments: per 07-RESEARCH high-yield synthesis and bare-metal gap (M1024 1.08 avg vs 0.70 at M512 shows M=512 tile under-utilized; peak 1.89 proves headroom if banking+pipeline fixed):
- In hip_helpers.h add soft HIP_CHECK_SOFT / HIP_CHECK macro variant that on hipErrorOutOfMemory or hipError 9 logs to stderr and returns hipErrorNotSupported (caller emits SKIPPED JSON) instead of abort; hard HIP_CHECK remains for non-OOM. Document both.
- GEMV dual OBJECTs: keep matmul_gemv_dp4a_hip (sB[32][33] +33 padded, sh[32][33]) and matmul_gemv_dp4a_xor_hip (GEMV_XOR adds xor_preshuffle_32x33 x'=(y%(32/8))^x 0% path per gemv_variant_xor.cuh) as two OBJECT libraries in CMake with distinct symbols (gemv_dp4a vs gemv_dp4a_xor wrappers). Both use __launch_bounds__(256,4)+amdgpu_flat_work_group_size(256,256) -> <=64 VGPR ->16 waves/SIMD, ulong2 16B b128 qs via __builtin_amdgcn_global_load_b128 or float4/ulong2 + __builtin_assume_aligned(ptr,16) fallback, __builtin_amdgcn_sudot4 + 6x perm LUT.
- GEMM 5 OBJECTs: matmul_gemm_wmma_stream_hip (64x32 P2+33 sB[2][32][33] double-buffer, B-stationary weight frag 8 VGPR, activation LDS, 2x WMMA per 32-K tile, v16f16/v8f32 lane%16, __builtin_amdgcn_wmma_f32_16x16x16_f16_w32), matmul_gemm_wmma_p4_xor_hip (GEMM_P4_XOR: real sB[4][32][32] XOR preshuffle x'=(y%(64/8))^x 0% vs +33 +3%, MARLIN P=4 pipeline GMEM->VGPR->LDS->VGPR->WMMA with sched_barrier 0x0080 DS + 0x0008 WMMA [llvm.org/docs/AMDGPUUsage]), matmul_gemm_wmma_64x64_hip (TILE_64x64: 64x64 B-stationary 64x reuse per CK Tile gemm_optimization loads/out=K*(1/M+1/N) ->64x reduction), matmul_gemm_lut_hip (LUT mu=4 16-entry half 32B bake d*(ls-32) eliminating inline kvalues multiply), plus 128x32 logical variant (8x2 warps, 128 blocks at M=8192) routed via same stream object with M switch. Each OBJECT exposes distinct gemm_iq4xs_wmma_*_gpu symbol (no synthetic v_median*=0.97 jitter; bench compares binaries).
- b128: verify global coalescing uses float4 16B / ulong2 16B and 32 thr*4B->8x16B mapping per SWDEV-556587; swizzle: tools/swizzle_iq4xs.py offline 16x64 swizzle to 128B cache lines (offline-only, not shipped; invoked at bench fixture gen, not at runtime).
- Bench plumbing: bench_gemm_wmma.cpp and bench_gemv_dp4a.cpp already have --runs 10 default + BenchStats median/mean/stddev/p95 + incremental fprintf+fflush per variant entry (avoids 12288B truncation) + VRAM preflight >2GB + hipMalloc probe for 8192 SKIPPED (FA+GQA 15.3GB+128KiB/tok ->18.5GB on 20GB, 800 GiB lie) and soft HIP_CHECK path -> JSON winner SKIPPED with note; retain timeout 90 invocation model.
- LUT mu=4 and W8A8 SmoothQuant 0.5 note: keep LUT as variant, W8A8 as documented comparator arm comment (not primary; IQ4_XS primary) per synthesis open question 5.
Pure C++/HIP only: no Python shipped in kernels/; race.py/swizzle stay offline harness.
</action>
  <verify>
    <automated>HSA_ENABLE_DXG_DETECTION=1 cmake -S kernels -B kernels/build -DCMAKE_HIP_ARCHITECTURES=gfx1100 -DCMAKE_BUILD_TYPE=Release && cmake --build kernels/build -j 2>&1 | tail -20; echo "cmake done $?"</automated>
    <automated>grep -n "GEMV_XOR\|GEMM_P4_XOR\|TILE_64x64\|sB\[4\]\[32\]\[32\]\|xor_preshuffle\|sched_barrier.*0x0080\|sched_barrier.*0x0008\|global_load_b128\|assume_aligned" kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip kernels/matmul_iq4xs/gemv_variant_xor.cuh 2>&1 | head -n 40</automated>
    <automated>grep -n "HIP_CHECK_SOFT\|SOFT\|SKIPPED.*8192" kernels/common/hip_helpers.h kernels/matmul_iq4xs/bench_gemm_wmma.cpp kernels/matmul_iq4xs/bench_gemv_dp4a.cpp 2>&1 | head -n 30</automated>
    <automated>grep -n "matmul_gemv_dp4a_xor_hip\|matmul_gemm_wmma_p4_xor_hip\|matmul_gemm_wmma_64x64_hip\|matmul_gemm_lut_hip" kernels/matmul_iq4xs/CMakeLists.txt 2>&1</automated>
    <automated>HSA_ENABLE_DXG_DETECTION=1 ctest --test-dir kernels/build -R test_ --output-on-failure 2>&1 | tail -30; echo "ctest done $?"</automated>
  </verify>
  <done>All 7 variant OBJECTs compile on gfx1100 (cmake --build PASS), distinct symbols exist (nm -D shows gemm_iq4xs_wmma_*_gpu), soft HIP_CHECK logs and returns on OOM probe, bench --help shows --runs 10 default, test_gemv_dp4a_compare + test_gemm_wmma_compare PASS cosine >=0.999 vs FP64 ref</done>
</task>

<task type="auto">
  <name>Race microbenchmarks N=10 interleaved A,B,A,B across M=512/1024/2048/4096 via race.py, VGPR/disasm/lds gates</name>
  <files>kernels/matmul_iq4xs/bench_gemv_dp4a.cpp, kernels/matmul_iq4xs/bench_gemm_wmma.cpp, benchmarks/results/phase7/race.py, kernels/matmul_iq4xs/CMakeLists.txt</files>
  <action>Execute the high-yield race that selects the winner closing 1.079->1.10:
- On WSL2 bare-metal gfx1100 with HSA_ENABLE_DXG_DETECTION=1 and timeout 90 per bench (avoid 271s DXG deadlock), run bench_real_stock --runs 10 --json as denominator (99us vs 543us naive already verified; re-capture to confirm thermal window) then bench_gemv_dp4a (+ xor binary) and bench_gemm_wmma --variant all --runs 10 --json via race.py --repeats 10 interleaved A,B,A,B (not AAAA BBBB) per adelj88/rocm_wmma_gemm pattern to kill thermal bias 15-30us jitter that flattens 1.178->1.00. Interleaving matrix: variants [64x32 P2+33, 64x32 P4_XOR, 64x64 P4_XOR, LUT mu4, 128x32] x shapes [attn_q 5120x5120, ffn_gate 5120x17408, ffn_down 17408x5120] x M [512,1024,2048,4096] plus 8192 always SKIPPED (VRAM preflight >2GB + hipMalloc probe fail -> JSON SKIPPED with FA+GQA note, no retry loops, no BSOD). JSON streaming with incremental fflush ensures valid JSON even if DXG hangs mid-run (validate with python -m json.tool and handle incomplete tail).
- Per variant emit speedup_median, speedup_mean_minus_1sigma ((stock.mean - stock.stddev)/(wmma.mean + wmma.stddev)), TFLOPS_median, GB/s, winner flag. Target context: M1024 prior 1.08 avg 1.89 peak vs M512 0.70 -> verify 64x64 + P4_XOR fixes M512 occupancy; GEMV prior 0.97 avg 1.16 peak -> verify XOR 0% vs +33 picks winner by median.
- Post-bench gates per variant on bare-metal where available: hipcc --save-temps -Rpass-analysis | grep VGPR shows <=64 (16 waves/SIMD via amdgpu calculator), llvm-objdump --mcpu=gfx1100 <obj>.o | grep -c "v_dot4\|v_wmma" >0, and rocprof --metric lds_bank_conflict ==0 (native bare-metal only; on WSL2 record "WSL2 blind per librocdxg 404" and keep calculator prediction). Low-yield variants that score speedup_median <0.90 across all Ms are pruned from winner selection.
- Produce race_variant_summary.json (median+mean-1sigma per variant per M, VGPR, disasm counts, lds conflicts) and per-tier winner verdict. Do not fabricate 1.10x; if best variant still <1.10 at a tier, record honest FAIL with distance to gate.
Pure C++/HIP kernels; race.py harness is offline Python not shipped.
</action>
  <verify>
    <automated>timeout 90 bash -c 'HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/bench_gemv_dp4a --runs 10 --json > /tmp/bench_gemv_N10.json && python3 -m json.tool /tmp/bench_gemv_N10.json > /dev/null && echo GEMV_JSON_OK && python3 -c "import json; d=json.load(open(\"/tmp/bench_gemv_N10.json\")); print(\"GEMV\", len(d), \"entries runs\", set(x[\"runs\"] for x in d), \"speedup_median\", [round(x[\"speedup_median\"],3) for x in d[:4]])"' 2>&1 | tail -20</automated>
    <automated>timeout 90 bash -c 'HSA_ENABLE_DXG_DETECTION=1 ./kernels/build/matmul_iq4xs/bench_gemm_wmma --runs 10 --json > /tmp/bench_gemm_N10.json && python3 -m json.tool /tmp/bench_gemm_N10.json > /dev/null && echo GEMM_JSON_OK && python3 -c "import json,collections; d=json.load(open(\"/tmp/bench_gemm_N10.json\")); print(\"GEMM\", len(d), \"valid\", collections.Counter(x.get(\"winner\",\"?\") for x in d)); print(\"M1024 avg\", round(sum(x[\"speedup_median\"] for x in d if x[\"M\"]==1024)/max(1,sum(1 for x in d if x[\"M\"]==1024)),3), \"peak\", round(max(x[\"speedup_median\"] for x in d if x[\"M\"]==1024),3))"' 2>&1 | tail -30</automated>
    <automated>python3 -c "import json, pathlib; d=json.load(open(\"/tmp/bench_gemm_N10.json\")); skipped=[x for x in d if x.get(\"winner\")==\"SKIPPED\"]; print(\"SKIPPED\", len(skipped), \"8192\", sum(1 for x in skipped if x[\"M\"]==8192), \"ok\" if all(8192==x[\"M\"] for x in skipped) else \"unexpected\"); print(\"truncation check\", len(open(\"/tmp/bench_gemm_N10.json\").read())>12288)" 2>&1 | tail -10</automated>
    <automated>hipcc --save-temps -Rpass-analysis 2>&1 | grep -i vgpr | head -n 10; llvm-objdump --mcpu=gfx1100 kernels/build/matmul_iq4xs/CMakeFiles/matmul_gemm_wmma_stream_hip.dir/impl_gemm_wmma_stream.hip.o 2>&1 | grep -c "v_wmma"; llvm-objdump --mcpu=gfx1100 kernels/build/matmul_iq4xs/CMakeFiles/matmul_gemv_dp4a_hip.dir/impl_gemv_dp4a_gfx1100.hip.o 2>&1 | grep -c "v_dot4" ; echo "disasm done"</automated>
  </verify>
  <done>bench_gemv_dp4a.hardware.json and bench_gemm_wmma.hardware.json are valid JSON >12288B with runs:10 per entry, 8192 SKIPPED only via preflight, no synthetic jitter, 5 GEMM + 2 GEMV variants each have median/mean/stddev/p95 + speedup_median + speedup_mean_minus_1sigma + TFLOPS vs real DP4A 99us, race_variant_summary.json ranks variants by median then mean-1sigma, VGPR <=64 and v_dot4/v_wmma present, lds_bank_conflict 0 or WSL2-blind noted</done>
</task>

<task type="auto">
  <name>Paired llama-bench N=10 one thermal window hwinfo 1Hz watchdog 90C RunStore+CHECKSUMS prove >=1.10x pp+tg</name>
  <files>benchmarks/results/phase7/race.py, benchmarks/results/phase7/hwinfo_daemon.py, benchmarks/results/phase7/thermal_watchdog.py, benchmarks/profiling/KERNEL-BENCH-DIFF.md, docs/PUBLICATION.md, patches/0001-gfx1100-mul-mat-custom.patch, llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemv_iq4xs.cuh, llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh</files>
  <action>Close REQ-PERF-07 with statistical rigour, not single-run:
- Pre-flight: verify stock and custom builds from same bb4caa75 with GGML_CUDA_ENABLE_CUSTOM_GFX1100 OFF vs ON (quilt patch git -C llama.cpp diff HEAD regenerated after restoring gemm can_handle from return false stub to real gate M>=16 && M>=512/WMMA-aligned check mirroring impl, and vending winner variant cuh files). Confirm git -C llama.cpp apply --check PASS on WSL2 (/opt/rocm) and note Windows HIP_PATH check deferred. Ensure custom_gemm_iq4xs_can_handle is real (not stub) so WMMA dispatch actually reached for M>=512 prefill.
- Vendor microbench winner variant(s) into llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/{gemv_iq4xs.cuh,gemm_iq4xs.cuh} (compact with GGML layout fix X[gm*K+gk]/Y[m*N+n], LDS [32][33] vs XOR, launch_bounds, sched_barrier 0x0080/0x0008, b128 paths). Regenerate patches/0001-gfx1100-mul-mat-custom.patch via git -C llama.cpp diff HEAD over bb4caa75, verify git apply --check, set core.autocrlf=false + *.patch eol=lf.
- Paired A/B: run race.py --repeats 10 interleaved A,B,A,B (stock OFF vs custom ON) across tiers 512/1024/2048/4096 plus 8192 SKIPPED (VRAM preflight >2GB free + hipMalloc probe, FA+GQA rationale 15.3GB+128KiB/tok ->18.5GB on 20GB, 800 GiB lie avoided) with -ngl 99 -b 2048 --single-turn --simple-io --load-mode none -r 10 per tier per build, N=10 in ONE thermal window. Launch hwinfo_daemon 1Hz (CSV temps/power) + thermal_watchdog 90C (kills on 90C) before first bench, keep alive entire window. RunStore: append-only rows.jsonl fsynced per result row + per-tier ab_stock_N10.jsonl / ab_custom_N10.jsonl, CHECKSUMS.sha256 over all rows, fingerprint (commit bb4caa75, ROCm/driver, GGUF sha256 53adc4bb, clocks/temps).
- Verdict: compute per tier per split (pp prefill tok/s, tg decode tok/s) median and mean-1sigma over N=10 (REQ-STAT-07). Gate PASS only if BOTH pp and tg median >=1.10x AND mean-1sigma >=1.10x at 512/1024/2048/4096 (8192 SKIPPED allowed with documented preflight). Prior 512 1.079 FAIL and M1024 micro 1.08 avg are the baseline to beat; winner needs +0.021 at 512 and consistent >1.10 across tiers. Publish per-tier table with median/mean/stddev/p95 + verdict; do not fabricate 1.10x PASS. Update benchmarks/profiling/KERNEL-BENCH-DIFF.md §8 (per-variant race + per-tier 1.10x verdict with M1024 1.89 peak context and honest 8192 SKIPPED) and docs/PUBLICATION.md Phase 7 (methodology, high-yield levers, thermal window, RunStore+CHECKSUMS).
- Also run QUAL-01 run_op_gate.py --runs 10 (0 errors) + run_model_gate.py --runs 10 (PPL 6.4271 +-1%) on custom ON build; if gates red, perf verdict is BLOCKED regardless of tok/s.
Pure C++/HIP kernels; harness Python is offline not shipped (Phase 8 prunes to <=2 langs).
</action>
  <verify>
    <automated>ls -lh benchmarks/results/phase7/ab_stock_N10.jsonl benchmarks/results/phase7/ab_custom_N10.jsonl benchmarks/results/phase7/rows.jsonl benchmarks/results/phase7/CHECKSUMS.sha256 benchmarks/results/phase7/hwinfo.csv logs/thermal_watchdog.log 2>&1 | head -n 20</automated>
    <automated>python3 -c "import json, collections, statistics; rows=[json.loads(l) for l in open('benchmarks/results/phase7/rows.jsonl')]; print('rows', len(rows)); tiers=sorted(set(r['tier'] for r in rows if r.get('tier'))); print('tiers', tiers); stock={t:[] for t in tiers}; custom={t:[] for t in tiers}; pp_stock={t:[] for t in tiers}; pp_custom={t:[] for t in tiers}
for r in rows:
    tier=r.get('tier')
    if tier in tiers:
        stock[tier].append(r.get('tg_stock', r.get('stock_tps',0)))
        custom[tier].append(r.get('tg_custom', r.get('custom_tps',0)))
        pp_stock[tier].append(r.get('pp_stock', r.get('stock_tps',0)))
        pp_custom[tier].append(r.get('pp_custom', r.get('custom_tps',0)))
for t in tiers:
    import statistics
    for label, a,b in [('tg',stock,custom),('pp',pp_stock,pp_custom)]:
        if not a[t] or not b[t]: continue
        med_a=statistics.median(a[t]); med_b=statistics.median(b[t]); median_ratio=med_b/med_a if med_a else 0
        mean_a=statistics.mean(a[t]); mean_b=statistics.mean(b[t]); sd_a=statistics.pstdev(a[t]) if len(a[t])>1 else 0; sd_b=statistics.pstdev(b[t]) if len(b[t])>1 else 0
        mean_m1 = (mean_b - sd_b) / (mean_a + sd_a) if (mean_a+sd_a) else 0
        print(f'{t} {label} median {median_ratio:.3f} mean-1sigma {mean_m1:.3f} {\"PASS\" if median_ratio>=1.10 and mean_m1>=1.10 else \"FAIL\"}') 
" 2>&1 | tail -30</automated>
    <automated>sha256sum -c benchmarks/results/phase7/CHECKSUMS.sha256 2>&1 | tail -10; echo "checksum done $?"</automated>
    <automated>grep -n "SKIPPED.*8192\|8192.*SKIPPED\|VRAM preflight\|hipMalloc probe\|FA+GQA" benchmarks/profiling/KERNEL-BENCH-DIFF.md docs/PUBLICATION.md benchmarks/results/phase7/rows.jsonl 2>&1 | head -n 20</automated>
    <automated>grep -n "can_handle.*return false" llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh 2>&1; echo "stub check done"</automated>
  </verify>
  <done>One-window N=10 paired A/B dataset exists (ab_stock/ab_custom + rows.jsonl 40 rows = 4 tiers x 10 reps x 2 splits pp+tg, hwinfo 1Hz CSV + watchdog 90C log, CHECKSUMS valid), per-tier median and mean-1sigma computed for pp and tg, 8192 SKIPPED with VRAM preflight note, KERNEL-BENCH-DIFF §8 + PUBLICATION updated with honest 1.10x verdict (PASS only if both splits both metrics >=1.10, else FAIL table with distance to gate), gemm can_handle not stubbed, patch regenerated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WSL2 host -> gfx1100 HIP runtime | Untrusted DXG jitter 15-30us, VRAM lie 800 GiB vs 3.48 GiB contiguous, BSOD after 3-5 OOMs |
| Bench harness -> HIP device | OOM on 8192 tier (total 15.3GB + KV 1-2GB >20GB) must not hard-crash Hyper-V |
| Windows HIP SDK vs WSL2 /opt/rocm | Path divergence HIP_PATH vs /opt/rocm, clang vs cl, Ninja mandated |
| RunStore filesystem | Append-only rows.jsonl + CHECKSUMS must survive DXG hang (incremental fflush) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Denial of Service | bench_gemm_wmma 8192 tier OOM | high | mitigate | VRAM preflight >2GB free + hipMalloc probe 10MB before alloc, soft HIP_CHECK logs SKIPPED (no abort, no retry loops per threat model T-07-03-03), 8192 always SKIPPED with FA+GQA 15.3GB+128KiB/tok note; timeout 90 wrapper |
| T-07-02-02 | Tampering | bench_gemm_wmma.hardware.json 12288B truncation | high | mitigate | Incremental fprintf+fflush after each variant entry (already in bench_gemm_wmma.cpp lines), timeout 90, JSON validate via python -m json.tool, handle incomplete tail as SKIPPED not corrupt |
| T-07-02-03 | Spoofing | Synthetic jitter inflation (v_median*=0.97) fakes winner | medium | mitigate | Deleted in this replan: each variant is distinct compiled OBJECT with distinct gemm_iq4xs_*_gpu symbol; race compares binaries, not synthetic scaling; grep gate verifies distinct symbols exist |
| T-07-02-04 | Information Disclosure | Thermal throttling leaks perf (15-30us DXG jitter flattens 1.178->1.00) | medium | mitigate | race.py --repeats 10 interleaved A,B,A,B (not AAAA BBBB) per adelj88, one thermal window hwinfo_daemon 1Hz + watchdog 90C, N=10 median+mean-1sigma gate not single-run |
| T-07-02-05 | Elevation of Privilege | Windows build Windows HIP SDK path traversal / cl vs clang divergence | low | mitigate | Build via HIP_PATH/bin/clang++.exe --offload-arch=gfx1100 -G Ninja (not cl), find_package hip via HIP_PATH, core.autocrlf=false + *.patch eol=lf, document Windows smoke deferred if SDK absent |
| T-07-02-06 | Repudiation | Fabricated 1.10x claim without RunStore+CHECKSUMS | high | mitigate | RunStore rows.jsonl fsynced + CHECKSUMS.sha256 + hwinfo CSV + per-tier median+mean-1sigma table in KERNEL-BENCH-DIFF §8; patch via real git diff bb4caa75, no single-run claim allowed per REQ-STAT-07 |
| T-07-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Pure C++/HIP only: no npm/pip/cargo installs in this plan (kernels + CMake + ROCm headers). Offline race.py/swizzle stay not shipped; audit table N/A. Two-lang gate enforced in Phase 8 prune. |
</threat_model>

<verification>
Overall phase checks:
- HSA_ENABLE_DXG_DETECTION=1 cmake --build kernels/build PASS, 7 variant OBJECTs exist with distinct symbols, VGPR <=64 gate shows 16 waves/SIMD, llvm-objdump shows v_dot4 (GEMV) + v_wmma (GEMM) per variant
- timeout 90 bench_gemv_dp4a --runs 10 --json + bench_gemm_wmma --runs 10 --variant all --json both produce valid JSON (>12288B for GEMM, no truncation, python -m json.tool PASS), 8192 entries are SKIPPED only, no synthetic jitter scaling remains
- race_variant_summary.json ranks high-yield variants (P=4 XOR 0%, b128 16B, 16x64 swizzle, 64x64 B-stationary 64x reuse, LUT mu4, sched_barrier 0x0080/0x0008) by median then mean-1sigma; M1024 context 1.08 avg 1.89 peak vs 0.70 reproduced or improved, GEMV XOR vs +33 winner by median documented
- One-window paired llama-bench A/B N=10 (ab_stock_N10.jsonl + ab_custom_N10.jsonl + rows.jsonl 40 rows, hwinfo 1Hz CSV + watchdog 90C log, CHECKSUMS valid, fingerprint commit/ROCM/GGUF) across 512/1024/2048/4096 + 8192 SKIPPED with preflight note; per-tier pp+tg median and mean-1sigma computed, KERNEL-BENCH-DIFF §8 + PUBLICATION updated with honest verdict (PASS only if both splits both metrics >=1.10, else FAIL with distance, prior 512 1.079 FAIL not hidden)
- QUAL-01 run_op_gate.py --runs 10 0 errors + QUAL-02 run_model_gate.py --runs 10 PPL 6.4271 +-1% green on custom ON, can_handle not stubbed, patch git apply --check PASS
</verification>

<success_criteria>
- 7 variant OBJECTs compiled (2 GEMV + 5 GEMM) with distinct symbols, soft HIP_CHECK on OOM, b128/swizzle/LUT/P4_XOR/64x64/B-stationary/sched_barrier wired, VGPR <=64 ->16 waves verified
- Microbench race completed N=10 interleaved A,B,A,B via race.py --repeats 10, JSONs valid not truncated, 8192 SKIPPED only, per-variant speedup_median + mean-1sigma + TFLOPS reported vs real DP4A 99us, winner selected (or honest all-FAIL with M1024 peak 1.89 context)
- Paired llama-bench A/B N=10 one thermal window hwinfo 1Hz watchdog 90C RunStore+CHECKSUMS proves or honestly fails >=1.10x median+mean-1sigma for BOTH pp and tg at 512/1024/2048/4096 (8192 SKIPPED allowed), pure C++/HIP, updated KERNEL-BENCH-DIFF §8 + PUBLICATION with N=10 tables and no fabricated claim
</success_criteria>

<output>
Create docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-02-SUMMARY.md when done
</output>
