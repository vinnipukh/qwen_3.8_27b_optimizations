---
phase: 07-hybrid-dp4a-wmma-kernel-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - build_windows.bat
  - CMakeLists.txt
  - kernels/CMakeLists.txt
  - kernels/matmul_iq4xs/CMakeLists.txt
  - .gitattributes
  - .gitignore
  - patches/0001-gfx1100-mul-mat-custom.patch
  - llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh
autonomous: false
requirements:
  - REQ-WIN-07
  - REQ-PERF-07
  - REQ-STAT-07
user_setup:
  - service: amd-hip-sdk
    why: "Windows-native gfx1100 compile — clang++.exe --offload-arch=gfx1100 is the only compiler that understands __builtin_amdgcn_sudot4/perm/wmma; MSVC cl fails on .hip"
    env_vars:
      - name: HIP_PATH
        source: "AMD HIP SDK installer default C:\\Program Files\\AMD\\ROCm\\6.4 — verify %HIP_PATH%\\bin\\clang++.exe exists"
    dashboard_config:
      - task: "Install HIP SDK 6.4 for Windows"
        location: "https://www.amd.com/en/developer/resources/rocm-hub/hip-sdk.html → Core SDK → Windows 11 22H2+"
      - task: "Install Ninja and CMake 3.21+"
        location: "winget install Ninja-build.Ninja && winget install Kitware.CMake — verify where ninja"
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "build_windows.bat builds clean on Windows 11 via %HIP_PATH%\\bin\\clang++.exe --offload-arch=gfx1100 -G Ninja (not cl) and produces build-windows/bin/llama-server.exe"
    - "CMake HIP discovery uses find_package(hip REQUIRED CONFIG PATHS \"$ENV{HIP_PATH}/lib/cmake/hip\") with HIP_PATH quoting — no hard-coded /opt/rocm-only path on Windows"
    - "Patch and git hygiene: *.patch eol=lf in .gitattributes, patch is 355 lines via git -C llama.cpp diff bb4caa75, git apply --check passes on both WSL2 (/opt/rocm) and Windows (HIP_PATH) with core.autocrlf=false and safe.directory set"
    - "Repo is pure C++/HIP+CMake+bat after prune — find -name \"*.py\" ! -path \"./llama.cpp/*\" == 0 (40→0) — no Python/JS servers shipped"
    - "llama-server.exe serves curl http://127.0.0.1:8000/v1/chat/completions → 200 with choices[0].message.content on gfx1100"
  artifacts:
    - "build_windows.bat (HIP_PATH quoted, where clang++.exe --offload-arch=gfx1100 --version, where ninja, -G Ninja, MODEL_PATH guard, curl :8000 smoke, safe.directory note)"
    - "CMakeLists.txt / kernels/CMakeLists.txt with find_package(hip REQUIRED CONFIG PATHS \"$ENV{HIP_PATH}/lib/cmake/hip\") and -DCMAKE_HIP_ARCHITECTURES=gfx1100"
    - ".gitattributes containing *.patch eol=lf and core.autocrlf=false verified"
    - "patches/0001-gfx1100-mul-mat-custom.patch regenerated (355 lines, 8 files, 276 insertions over bb4caa75, lf) with gemm can_handle restored (no stub return false)"
    - "build-windows/bin/llama-server.exe + build-windows/curl_out.json (200) + build-windows/server.log"
  key_links:
    - "build_windows.bat → %HIP_PATH%\\bin\\clang++.exe --offload-arch=gfx1100 → cmake -G Ninja → build-windows/bin/llama-server.exe"
    - "CMake find_package(hip via HIP_PATH) → hip-config.cmake → clang++ HIP device compile of __builtin_amdgcn_* intrinsics"
    - ".gitattributes eol=lf → patches/*.patch lf → git apply --check PASS both OS"
    - "Prune (py 40→0) → pure C++/HIP+CMake+bat ≤2 langs gate → REQ-WIN-07 satisfied, Phase 8 landing complete"
---

<objective>
Close REQ-WIN-07 Windows-native ≤2 langs gate in a single replan replacing deleted 07-01..07-04. Harden build_windows.bat HIP_PATH quoting, CMake HIP discovery via HIP_PATH, enforce -G Ninja not cl, set safe.directory and *.patch eol=lf, prune Python 40→0 to pure C++/HIP+CMake+bat, and prove llama-server.exe serves :8000 →200 on gfx1100. This is the Phase 7 must-have #1 closure; Phase 8 is the landing execution that keeps it green.

Purpose: Prior verifier scored 1/7 — GEMV 0.942 avg <1.2x, WMMA truncated JSON, gemm can_handle stub disabling dispatch, Windows build not executed, py 40 violates ≤2 langs. Bare-metal N=10 4-tier llama-bench 512 pp 1.079 FAIL (<1.10x) and tg 0.993 confirm we cannot yet claim ≥1.10x. This plan fixes the Windows-native toolchain so the 1.10x work has a native, reproducible build to race against, without fabricating GPU numbers.
Output: Hardened build_windows.bat + CMake HIP_PATH discovery + .gitattributes/safe.directory + regenerated lf patch with gemm gate restored + pruned pure C++/HIP tree (py 0) + Windows-built llama-server.exe serving :8000 200, all verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/PROJECT.md
@docs/ROADMAP.md
@docs/STATE.md
@docs/REQUIREMENTS.md
@docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-CONTEXT.md
@docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-RESEARCH.md
@docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-VERIFICATION.md
@docs/research/deep-research/phase7-3must-haves-exhaustive.md
@docs/research/deep-research/1000t-s-at-8k-gfx1100.md
@docs/phases/08-refactor-windows-native/08-CONTEXT.md
@build_windows.bat
@kernels/CMakeLists.txt
@kernels/matmul_iq4xs/CMakeLists.txt
@patches/0001-gfx1100-mul-mat-custom.patch
@llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden build_windows.bat HIP_PATH quoting + CMake HIP_PATH discovery + git hygiene (safe.directory, *.patch eol=lf)</name>
  <files>build_windows.bat, CMakeLists.txt, kernels/CMakeLists.txt, kernels/matmul_iq4xs/CMakeLists.txt, .gitattributes, .gitignore, patches/0001-gfx1100-mul-mat-custom.patch, llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh</files>
  <action>Per REQ-WIN-07 and 07-VERIFICATION gaps + 07-RESEARCH Windows-native pattern + 08-CONTEXT D-03/D-06: 1) Edit build_windows.bat so every HIP_PATH reference is quoted as "%HIP_PATH%" (handles C:\Program Files\AMD\ROCm\6.4 space). Ensure: if "%HIP_PATH%"=="" set "HIP_PATH=C:\Program Files\AMD\ROCm\6.4"; if not exist "%HIP_PATH%\bin\clang++.exe" error with install URL; set "PATH=%HIP_PATH%\bin;%PATH%"; where clang++.exe && clang++.exe --offload-arch=gfx1100 --version (fail if errorlevel 1); where ninja (fail with winget hint, explicitly error if VS generator cl path would be used — message must state vs generator cannot compile __builtin_amdgcn_*); cmake -S . -B build-windows -G Ninja -DCMAKE_HIP_ARCHITECTURES=gfx1100 -DGGML_HIP=ON -DGGML_CUDA_ENABLE_CUSTOM_GFX1100=ON -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER="%HIP_PATH%\bin\clang++.exe" -DCMAKE_HIP_COMPILER="%HIP_PATH%\bin\clang++.exe" -DHIP_PATH="%HIP_PATH%"; cmake --build build-windows --config Release; MODEL_PATH guard; server smoke curl :8000 with CURL_CODE capture; add early note: git config --global --add safe.directory "%CD%" (or repo absolute) to avoid dubious ownership on Windows/WSL shared checkout. Do not remove existing correct guards — only add missing quoting/safe.directory and ensure -G Ninja is the only generator. 2) Fix CMake HIP discovery: in kernels/CMakeLists.txt (and root CMakeLists.txt if present) replace any hard-coded find_package(hip ... PATHS "/opt/rocm/...") with find_package(hip REQUIRED CONFIG PATHS "$ENV{HIP_PATH}/lib/cmake/hip" "/opt/rocm/lib/cmake/hip") — HIP_PATH first, /opt/rocm as WSL2 fallback, plus set CMAKE_PREFIX_PATH to "$ENV{HIP_PATH}" "/opt/rocm" if needed. Verify hip-config.cmake search respects HIP_PATH env. 3) Git hygiene: ensure .gitattributes contains exactly "*.patch eol=lf" (create if missing) and patch file uses lf (run dos2unix or git add --renormalize). Ensure repo has core.autocrlf=false (set via git config core.autocrlf false and document). In llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh restore custom_gemm_iq4xs_can_handle from stub return false to real gate: return type==GGML_TYPE_IQ4_XS && M>=512 && M%16==0 && N%16==0 && K%16==0 && N>=32 (mirrors impl GEMM tiling gate; keep M<512 fallback to tiled path — do not enable WMMA at M=128 where 0.042x slower). After edit, regenerate patch via git -C llama.cpp diff bb4caa75 > patches/0001-gfx1100-mul-mat-custom.patch and ensure .gitattributes lf applies. Using HIP_PATH per user decision even though research notes /opt/rocm fallback — keep fallback for WSL2 bare-metal bench.</action>
  <verify>
    <automated>grep -q '"%HIP_PATH%\\bin\\clang++.exe"' build_windows.bat && grep -q 'where clang++.exe' build_windows.bat && grep -q 'where ninja' build_windows.bat && grep -q '\-G Ninja' build_windows.bat && ! grep -q 'Visual Studio' build_windows.bat && echo "BAT quoting + Ninja gate PASS"</automated>
    <automated>grep -q 'find_package(hip.*HIP_PATH' kernels/CMakeLists.txt && grep -q '\$ENV{HIP_PATH}/lib/cmake/hip' kernels/CMakeLists.txt && echo "CMake HIP_PATH discovery PASS"</automated>
    <automated>grep -q '^\*\.patch eol=lf' .gitattributes && git check-attr eol -- patches/0001-gfx1100-mul-mat-custom.patch | grep -q 'lf' && echo ".gitattributes lf PASS"</automated>
    <automated>grep -q 'custom_gemm_iq4xs_can_handle' llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh && ! grep -q 'return false.*can_handle' llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh && wc -l patches/0001-gfx1100-mul-mat-custom.patch | awk '{if($1>=300) print "PATCH 355 lines PASS " $1; else print "FAIL lines " $1; exit ($1<300)}'</automated>
    <automated>git -C llama.cpp diff --check 2>&1 | head -20; git -C . apply --check patches/0001-gfx1100-mul-mat-custom.patch 2>&1 && echo "git apply --check WSL2 PASS"; git config core.autocrlf | grep -q false && echo "core.autocrlf false PASS"</automated>
  </verify>
  <done>build_windows.bat has quoted "%HIP_PATH%" everywhere, checks where clang++.exe --offload-arch=gfx1100 --version and where ninja, uses only -G Ninja (no cl), documents safe.directory; CMake uses HIP_PATH-first discovery with /opt/rocm fallback; .gitattributes *.patch eol=lf present and patch is lf; gemm can_handle restored (no stub false) and patch regenerated 355 lines; git apply --check passes on WSL2 with core.autocrlf=false</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Windows HIP SDK 6.4 install + Ninja -G Ninja clean build gate (bare-metal)</name>
  <files>build-windows/bin/llama-server.exe, build-windows/server.log, build-windows/curl_out.json, build-windows/curl_code.txt</files>
  <action>Per REQ-WIN-07 and 07-VERIFICATION human_verification #2/#3 and 07-RESEARCH Standard Stack: human installs AMD HIP SDK 6.4 for Windows at HIP_PATH=C:\Program Files\AMD\ROCm\6.4 (https://www.amd.com/en/developer/resources/rocm-hub/hip-sdk.html), verifies %HIP_PATH%\bin\clang++.exe --offload-arch=gfx1100 --version prints clang 22.0.0git gfx1100 support, installs Ninja via winget install Ninja-build.Ninja (where ninja passes) and CMake 3.21+, sets HIP_PATH env if non-default, runs build_windows.bat in repo root. Expect: PATH=%HIP_PATH%\bin prepended, cmake configure with -G Ninja succeeds (no Visual Studio generator), __builtin_amdgcn_sudot4/perm/wmma compile without MSVC errors (cl must not be invoked for .hip), cmake --build completes and produces build-windows/bin/llama-server.exe. If rebuild needed after Task 1 gemm fix, verify patch still git apply --check passes on Windows (git config --global --add safe.directory C:/path/to/repo, git check-attr eol). Record: clang++.exe --version output, ninja --version, cmake --version, build log tail, and dir build-windows\bin listing.</action>
  <verify>
    <human-check>On Windows 11 + VS Build Tools + HIP SDK 6.4, run build_windows.bat and confirm: where clang++.exe prints %HIP_PATH%\bin\clang++.exe, clang++.exe --offload-arch=gfx1100 --version succeeds, where ninja succeeds, cmake -G Ninja configure succeeds, build produces build-windows\bin\llama-server.exe, and build-windows\bin\llama-server.exe --help exits 0. Paste build tail + dir listing.</human-check>
    <automated>ls -lh build-windows/bin/llama-server.exe 2>&1 | head -5; git check-attr eol -- patches/0001-gfx1100-mul-mat-custom.patch 2>&1 | head -5</automated>
  </verify>
  <done>Windows-native build executed on Windows 11 with HIP SDK 6.4: clang++.exe --offload-arch=gfx1100 and ninja both found via HIP_PATH, -G Ninja configure succeeds, no cl errors on __builtin_amdgcn_* , build-windows/bin/llama-server.exe exists and --help runs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Prune to pure C++/HIP+CMake+bat (py 40→0) + llama-server :8000 200 smoke</name>
  <files>build_windows.bat, .gitignore, README.md, CMakeLists.txt, kernels/common/block_iq4_xs.h, kernels/common/hip_helpers.h, kernels/matmul_iq4xs/impl_gemv_dp4a_gfx1100.hip, kernels/matmul_iq4xs/impl_gemm_wmma_stream.hip, patches/0001-gfx1100-mul-mat-custom.patch</files>
  <action>Per REQ-WIN-07 ≤2 langs (pure C++/HIP+CMake+bat, no Python shipped) and 07-VERIFICATION deferred item (py 40→0) and 08-CONTEXT D-01/D-02 allowlist: 1) In Git Bash run find . -name "*.py" ! -path "./llama.cpp/*" | sort — expect 40 including benchmarks/, tools/swizzle_iq4xs.py, benchmarks/results/phase7/race.py, output/ — document list. Prune to 0 per Phase 8 landing: delete or move to docs/archive/ (do not keep in shipped tree) all Python/JS harnesses outside llama.cpp: benchmarks/ harness Python, tools/swizzle_iq4xs.py (offline-only helper — archive its logic as comment in gemm header if needed), benchmarks/results/phase7/race.py, output/ python, docs/research/freetoken-probe/qstar.mjs if present, planning logs excluded from shipped tree via .gitignore (keep planning for history but ensure shipped tree definition for Windows user is kernels/ + patches/ + CMakeLists.txt + build_windows.bat + llama.cpp pinned at bb4caa75 + README/LICENSE/NOTICE/CHANGELOG). Update .gitignore to ignore build*/, build-windows/, *.exe, *.obj, kernels/build/ but not the allowlist. Keep minimal kernels per D-02 allowlist: block_iq4_xs.h, hip_helpers.h, bench.h if needed for smoke, impl_gemv_dp4a_gfx1100.hip, impl_gemm_wmma_stream.hip, ref_cpu.h/.cpp optional. Verify after prune: find . -name "*.py" ! -path "./llama.cpp/*" | wc -l == 0 and find . -name "*.mjs" ! -path "./llama.cpp/*" | wc -l == 0. 2) Smoke: if MODEL_PATH env points to Qwen3.8-27B IQ4_XS gguf, run start /B build-windows\bin\llama-server.exe -m "%MODEL_PATH%" --port 8000 --host 127.0.0.1 -ngl 99 -b 2048 and curl http://127.0.0.1:8000/v1/chat/completions -H "Content-Type: application/json" -d "{\"messages\":[{\"role\":\"user\",\"content\":\"Hi\"}]}" — expect HTTP 200 and body contains choices[0].message.content (choices string). Record curl_code.txt and curl_out.json. If no model on Windows host, document SKIPPED with MODEL_PATH not found and at least prove build-windows\bin\llama-server.exe --help and that no Python remains — model smoke deferred to WSL2 bench host but ≤2 langs gate still passes on build alone. Bare-metal 4-tier 512 pp 1.079 FAIL note must be preserved in docs/PUBLICATION.md — do not fabricate 1.10x.</action>
  <verify>
    <human-check>Run in Git Bash: find . -name "*.py" ! -path "./llama.cpp/*" | wc -l — must be 0 after prune (was 40). Then if model present, run build-windows\bin\llama-server.exe -m "%MODEL_PATH%" --port 8000 and curl :8000/v1/chat/completions → 200 with choices; paste curl_code.txt + first 30 lines curl_out.json. If no model, paste --help output and note SKIPPED with reason.</human-check>
    <automated>find . -name "*.py" ! -path "./llama.cpp/*" 2>&1 | head -20; echo "PY_COUNT:"; find . -name "*.py" ! -path "./llama.cpp/*" 2>&1 | wc -l; echo "MJS_COUNT:"; find . -name "*.mjs" ! -path "./llama.cpp/*" 2>&1 | wc -l</automated>
    <automated>ls -lh build-windows/bin/llama-server.exe 2>&1 | head -3; cat build-windows/curl_code.txt 2>&1 | head -3; head -30 build-windows/curl_out.json 2>&1 | head -30</automated>
  </verify>
  <done>find -name "*.py" ! -path "./llama.cpp/*" == 0 (was 40) and *.mjs == 0 — tree is pure C++/HIP+CMake+bat ≤2 langs; build-windows/bin/llama-server.exe exists and either serves curl :8000 →200 choices[0].message.content on gfx1100 or documents SKIPPED with MODEL_PATH not found (build gate still PASS); bare-metal 512 pp 1.079 FAIL preserved not fabricated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows host → HIP SDK (HIP_PATH) | Untrusted PATH/env — HIP_PATH with spaces must be quoted or arbitrary path traversal / command injection via bat could occur |
| Network → llama-server :8000 | Untrusted HTTP input at /v1/chat/completions — must not expose beyond 127.0.0.1 during smoke |
| Git patch → build tree | Patch applies code that compiles with clang++ device builtins — malicious patch could inject device code |
| Prune step → repo history | Deleting Python harnesses must not delete audit trail (retain in docs/archive or git history, not shipped tree) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | build_windows.bat HIP_PATH | medium | mitigate | Quote "%HIP_PATH%" everywhere, validate where clang++.exe before PATH prepend, fail if HIP_PATH/bin/clang++.exe missing |
| T-07-02 | Tampering | patches/*.patch lf / git apply | high | mitigate | Enforce *.patch eol=lf in .gitattributes, core.autocrlf=false, verify git apply --check passes before build; patch is git -C llama.cpp diff bb4caa75 (pinned, reviewable) |
| T-07-03 | Tampering | CMake HIP discovery | medium | mitigate | Use find_package(hip REQUIRED CONFIG PATHS "$ENV{HIP_PATH}/...") first — prevents hijacking via /opt/rocm spoof on Windows; HIP SDK ownership verified via clang++ --version |
| T-07-04 | Denial of Service | llama-server :8000 smoke | low | accept | Smoke binds only 127.0.0.1:8000, single-turn, MODEL_PATH guard — not exposed to network; taskkill after curl |
| T-07-05 | Information Disclosure | Prune py 40→0 | low | mitigate | Archive deleted harnesses to docs/archive/ with git history retained, not shipped — prevents loss of reproducibility audit |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package-manager installs in this plan — pure C++/HIP+CMake+bat, no npm/pip/cargo; package-legitimacy gate N/A |

</threat_model>

<verification>
Overall Phase 7 must-have #1 (REQ-WIN-07) closure checks — all must pass before Phase 8 landing is declared green:

- `grep -q '"%HIP_PATH%\\bin\\clang++.exe"' build_windows.bat && grep -q 'where ninja' build_windows.bat && grep -q '\-G Ninja' build_windows.bat` — HIP_PATH quoting + Ninja gate
- `grep -q 'find_package(hip.*HIP_PATH' kernels/CMakeLists.txt && grep -q '\$ENV{HIP_PATH}/lib/cmake/hip' kernels/CMakeLists.txt` — HIP discovery via HIP_PATH, no /opt/rocm-only hardcode
- `grep -q '^\*\.patch eol=lf' .gitattributes && git check-attr eol -- patches/0001-gfx1100-mul-mat-custom.patch | grep -q lf` — patch eol lf
- `git config core.autocrlf | grep -q false && git -C . apply --check patches/0001-gfx1100-mul-mat-custom.patch && git -C llama.cpp diff --check` — git hygiene both OS
- `! grep -q 'return false.*can_handle\|can_handle.*return false' llama.cpp/ggml/src/ggml-cuda/custom_gfx1100/gemm_iq4xs.cuh && wc -l patches/0001-gfx1100-mul-mat-custom.patch | awk '$1>=300'` — gemm gate restored, patch 355 lines
- `find . -name "*.py" ! -path "./llama.cpp/*" | wc -l` must be 0 after Task 3 (was 40) — ≤2 langs pure C++/HIP+CMake+bat
- `ls build-windows/bin/llama-server.exe && build-windows/bin/llama-server.exe --help` — Windows exe produced
- `cat build-windows/curl_code.txt | grep -q 200 && grep -q choices build-windows/curl_out.json` — :8000 smoke (or documented SKIPPED if MODEL_PATH absent, but py 0 + exe still required)
- Bare-metal note preserved: docs/PUBLICATION.md still states 512 pp 838 vs 904 1.079 FAIL (<1.10x) — no fabricated 1.10x PASS
</verification>

<success_criteria>
- build_windows.bat handles HIP_PATH with spaces via "%HIP_PATH%" quoting, checks where clang++.exe --offload-arch=gfx1100 --version and where ninja, uses only -G Ninja (not cl), sets safe.directory, and documents MODEL_PATH + HIP SDK URL
- kernels/CMakeLists.txt (and root if present) discovers HIP via find_package(hip REQUIRED CONFIG PATHS "$ENV{HIP_PATH}/lib/cmake/hip" "/opt/rocm/...") — HIP_PATH first
- .gitattributes contains *.patch eol=lf, core.autocrlf=false, patch is lf line endings and 355 lines (8 files, 276 insertions over bb4caa75), gemm can_handle stub removed, git apply --check passes on WSL2 and Windows
- Repo after prune is pure C++/HIP+CMake+bat: find -name "*.py" ! -path "./llama.cpp/*" == 0 (was 40) and *.mjs == 0 — no Python/JS servers shipped
- Windows HIP SDK 6.4 build produces build-windows/bin/llama-server.exe (clang++ gfx1100, no MSVC __builtin_amdgcn_* errors) and either serves curl :8000 →200 choices on gfx1100 or documents SKIPPED with MODEL_PATH reason — ≤2 langs build gate still passes
</success_criteria>

<output>
Create docs/phases/07-hybrid-dp4a-wmma-kernel-optimization/07-01-SUMMARY.md when done
</output>
