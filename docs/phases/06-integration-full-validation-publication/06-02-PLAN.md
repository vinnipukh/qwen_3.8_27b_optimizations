# Plan 06-02: Winner Patches Behind Switch (INTEG-01)

**Goal:** Promote `patches/phase5_mul_mat_custom.patch` (provisional) to final `patches/*.patch` series — GEMV + GEMM behind `GGML_CUDA_ENABLE_CUSTOM_GFX1100`.

**Deliverables:**
- `patches/0001-gfx1100-gemv-*.patch` + `0002-gfx1100-gemm-wmma-*.patch` over `bb4caa75`, `custom_gfx1100/{gemv,gemm}.cuh` vendoring `kernels/matmul_iq4xs/` (TILE_M=16+WMMA, B_lds[2][32][33])
- **Patch provenance (thermos H-4):** generated by real `git diff` from a patched tree; must pass `git apply --check` against pristine `bb4caa75`. The hand-written `phase5_mul_mat_custom.patch` is a design spec only — do NOT ship it.
- **Layout mapping:** ggml `src1` is `[M,K]` row-major and `dst` indexed `[m*N+n]`; kernels expect `X[k*M+m]` / write `Y[n*M+m]` — add transpose step or route through ggml conventions, else output is transposed/corrupt.
- All `can_handle`/dispatch code inside the `#if defined(GGML_CUDA_ENABLE_CUSTOM_GFX1100)` guard so OFF compiles bit-identical; HIP options (`--offload-arch=gfx1100 -mwavefrontsize32`) scoped to custom sources, not project-wide.
- `build-custom` `cmake -DGGML_CUDA_ENABLE_CUSTOM_GFX1100=ON` succeeds; `mmq/mvq` intercept only canonical shapes.
- Optional comparator arm: true stock MMVQ/MMQ kernel timings before any headline "beats stock llama.cpp" claim (complements the naive-scalar-baseline relabeling in 06-05).
- `build-custom` `cmake -DGGML_CUDA_ENABLE_CUSTOM_GFX1100=ON` succeeds; `mmq/mvq` intercept only canonical shapes.

**Verification:** `test_gemv/gemm_compare` still PASS via in-tree build; `hipcc --offload-arch=gfx1100` no `amdgpu-arch` fat binary.
