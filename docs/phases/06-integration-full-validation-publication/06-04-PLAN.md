# Plan 06-04: Publication Package + Paired Final Matrix (PUB-01)

**Goal:** Publish complete stock-vs-optimized matrix with methodology, raw data, and **release hygiene** — failures included.

**Deliverables:**
- **Smoke-first gate:** `run_session.py --tiers 4096 --repeats 3` (~15min) must survive on the custom build before the full session is scheduled.
- **Paired e2e matrix:** `run_session.py --tiers 4096 8192 16384 --repeats 5 --delay 10 --flash-attn on off` × `HIP stock vs HIP custom` + Vulkan arm, one thermal window (`hwinfo_daemon.py` + `fingerprint.py`), `RunStore` `final_*` + `CHECKSUMS.sha256`, run-to-run variance published (thermos H-2: cite the exact checksummed run the tables use)
- **Gates (fresh timestamped files, never overwrite history — thermos H-3):** `run_op_gate.py` 21k PASS; `run_model_gate.py --chunks 145` full wiki.test.raw → `PPL 6.4271±1% [6.3628,6.4914]` + 6/6 canaries; annotate the standing 2-chunk FAIL artifact with cause + supersession
- **Hygiene (PUB-01 in scope, not deferred):**
  - `LICENSE` Apache 2.0 (done 2026-08-25) + `NOTICE` for `Qwen/Qwen3.8-27B` + `block_iq4_xs.h`
  - `README.md` license section, `docs/*` Phase 5 done (5 writers)
  - Stray cleanup: `test_wmma*` in root + `benchmarks/results/tmp_bench_*` + `check_no_ggml.sh` PASS
  - `CHANGELOG` + `v1.0.0-gfx1100` tag (`config.json create_tag: true`)
  - No `models/*.gguf` in git, `.gitignore` verified
- **Publication:** `docs/PUBLICATION.md` + `benchmarks/results/BASELINE-MATRIX.md` final, exact `hipcc` cmds, `versions.txt`/`rocminfo`, `KERNEL-BENCH-DIFF.md` (30/32 wins, 2× M=16 0.82× losses per Rule #10), `patches/*.patch` series, known limits

**Verification:** `PUB-01` 3/3: paired matrix with temps/power, raw data + methodology, kernel source + failed log published.

**Time:** ~60-120min e2e + 30min hygiene; 06-01..06-03 can parallelize with doc drafting, 06-04 matrix is serial-last.
