---
phase: 1
plan: 01-02
type: auto
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/llama.cpp
  - baseline/binaries/v0.2.0-bb4caa75/
  - benchmarks/environment/llamacpp-pin.txt
autonomous: true
must_haves:
  - truth: "stock llama.cpp built from pinned v0.2.0 @ bb4caa75 targeting gfx1100"
  - truth: "four binaries (llama-cli, llama-bench, llama-perplexity, test-backend-ops) archived under baseline/binaries/"
  - truth: "test-backend-ops executed and result recorded"
  - truth: "repo total size verified <750 MB after archive, with breach branch defined"
  - artifact: "baseline/binaries/v0.2.0-bb4caa75/"
  - artifact: "benchmarks/environment/llamacpp-pin.txt"
---

# Plan 01-02: Pinned llama.cpp Build & Baseline Archive

## Objective
Build stock llama.cpp at pin v0.2.0 for gfx1100; archive binaries as the permanent reference; record build provenance. ENV-02 (+build-side ENV-04). D-05 lineage constraint pre-verified in 01-RESEARCH.md.

<context>
Pin already resolved: tag v0.2.0, commit bb4caa7540188872173c44d161602d9271386413 (published 2026-08-21; b8394=3a5cb629@2026-03-17 lineage satisfied). Build may already be running from execution-start launch — check logs/build.log first; adopt rather than duplicate. Fallback if this fresh tag misbehaves at build: newest b-series tag ≥ b850, record deviation + rationale.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone, pin, configure, build</name>
  <files>src/llama.cpp, logs/build.log</files>
  <action>Guest: clone ggml-org/llama.cpp into repo src/, `git checkout v0.2.0`, assert `git rev-parse HEAD` == bb4caa7540188872173c44d161602d9271386413. Configure: cmake -S . -B build-ci -G Ninja -DGGML_HIP=ON -DGPU_TARGETS=gfx1100 -DCMAKE_BUILD_TYPE=Release -DLLAMA_CURL=ON with ccache launchers. Build targets llama-cli llama-bench llama-perplexity test-backend-ops -j14. If a concurrent build is already running (check logs/build.log tail), wait and adopt instead of re-running. On configure/build failure at THIS pin: capture last 50 log lines, switch to fallback tag per context rule, re-run, record deviation.</action>
  <verify>test -x all four binaries under src/llama.cpp/build-ci/bin/; git -C src/llama.cpp rev-parse HEAD prints expected sha</verify>
  <acceptance_criteria>
    - HEAD == bb4caa7540188872173c44d161602d9271386413 (or recorded fallback ≥b850)
    - four ELF executables exist in build-ci/bin/
  </acceptance_criteria>
  <done>Pinned stock binaries built successfully for gfx1100</done>
</task>

<task type="auto">
  <name>Task 2: Op-test smoke + archive + D-06 size gate + provenance</name>
  <files>baseline/binaries/v0.2.0-bb4caa75/, benchmarks/environment/llamacpp-pin.txt, benchmarks/environment/test-backend-ops-phase1.txt</files>
  <read_first>benchmarks/environment/versions.txt</read_first>
  <action>Run ./test-backend-ops capturing tail to benchmarks/environment/test-backend-ops-phase1.txt (record pass/fail counts verbatim). Copy the four binaries to baseline/binaries/v0.2.0-bb4caa75/. D-06 GATE: measure `git count-objects -vH` size-pack + du of would-be-tracked payload AFTER staging — if total repo payload would exceed 750 MB, MOVE binaries outside the repo to E:\gsd-baselines\v0.2.0-bb4caa75\ and keep only baseline/binaries/README.md pointer (path already gitignored); else leave tracked. Write llamacpp-pin.txt: pin-tag, commit sha, tag-date 2026-08-21, lineage-check line, full configure line, compiler versions (gcc --version, hipcc --version), ccache stats summary.</action>
  <verify>All 4 files present in archive dir (or external dir + README pointer exists); llamacpp-pin.txt contains 'commit:' and 'configure:' keys; test-backend-ops output file ends with a results summary line</verify>
  <acceptance_criteria>
    - baseline/binaries/v0.2.0-bb4caa75/ holds 4 ELFs AND measured tracked-repo payload <750 MB (or documented external move)
    - test-backend-ops phase-1 record exists with explicit PASS/FAIL counts
  </acceptance_criteria>
  <done>Permanent stock baseline archived with provenance; ENV-02 satisfied</done>
</task>

</tasks>

<verification>
- ls baseline/binaries/v0.2.0-bb4caa75/ → 4 binaries
- grep -E 'commit:|configure:' benchmarks/environment/llamacpp-pin.txt → both present
</verification>

<success_criteria>
Stock reference baseline exists, reproducible from recorded pin + flags.
</success_criteria>

## Rollback notes
Archive is additive-only; delete build-ci to rebuild clean.
