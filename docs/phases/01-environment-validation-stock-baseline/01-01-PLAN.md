---
phase: 1
plan: 01-01
type: auto
wave: 1
depends_on: []
files_modified:
  - benchmarks/environment/rocminfo.txt
  - benchmarks/environment/hipconfig.txt
  - benchmarks/environment/versions.txt
autonomous: true
must_haves:
  - truth: "rocminfo enumerates AMD Radeon RX 7900 XT as gfx1100 GPU agent inside Ubuntu-24.04 WSL"
  - truth: "a minimal HIP program executes on that device (SC1 smoke half — kill-gate complete in wave 1)"
  - truth: "ROCm version pinned exactly 7.2.1 and frozen pairing recorded"
  - truth: "Adrenalin driver auto-updates paused on Windows host (D-04)"
  - artifact: "benchmarks/environment/rocminfo.txt"
  - artifact: "benchmarks/environment/versions.txt"
---

# Plan 01-01: ROCm/HIP Stack Install & Validation

## Objective
Install ROCm 7.2.1 + librocdxg (root-only), prove gfx1100 enumeration AND device-code execution, pause Windows-side driver auto-updates, capture fingerprints. ENV-01 + D-04.

<context>
Host pairing already verified live: RX 7900 XT @ 32.0.31041.1004 (Adrenalin 26.10.41). Guest ground truth: /dev/dxg present, no ROCm installed at start. NOTE (deviation recorded): amdgpu-install 30.30.x has NO 'wsl' usecase — use `--usecase=rocm --no-dkms` (equivalent; already applied during execution-start probe).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apt prerequisites + pinned ROCm 7.2.1 install</name>
  <files>benchmarks/environment/versions.txt</files>
  <action>Inside guest as root: apt-get update; install build-essential cmake ninja-build ccache git libcurl4-openssl-dev pkg-config curl. Download EXACTLY https://repo.radeon.com/amdgpu-install/7.2.1/ubuntu/noble/amdgpu-install_7.2.1.70201-1_all.deb (never newer — D-03 freeze), dpkg -i it, then `amdgpu-install --usecase=rocm --no-dkms -y`. Verify /opt/rocm symlink resolves to rocm-7.2.1.</action>
  <verify>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'readlink -f /opt/rocm && which cmake ninja ccache'</verify>
  <acceptance_criteria>
    - readlink output ends in rocm-7.2.1
    - cmake, ninja, ccache all resolve to /usr/bin paths
  </acceptance_criteria>
  <done>ROCm tree at /opt/rocm-7.2.1 with toolchain packages present</done>
</task>

<task type="auto">
  <name>Task 2: librocdxg v1.2.2 debs + persistent DXG env</name>
  <files>benchmarks/environment/versions.txt</files>
  <action>Download and dpkg -i BOTH v1.2.2 debs from github.com/ROCm/librocdxg/releases (rocdxg-roct_1.2.2_amd64.deb, rocdxg-amd-smi-lib_1.2.2_amd64.deb). Write /etc/profile.d/rocdxg.sh via base64 pipe (Git-bash mangles $VAR through wsl.exe args): line1 `export HSA_ENABLE_DXG_DETECTION=1`, line2 PATH-guarded append of /opt/rocm/bin.</action>
  <verify>Write probe script containing ${VARS} to guest file first, then execute it — assert HSA=[1], PATH contains :/opt/rocm/bin:, dpkg -l shows both rocdxg pkgs ii</verify>
  <acceptance_criteria>
    - dpkg -l | grep rocdxg lists rocdxg-roct and rocdxg-amd-smi-lib at 1.2.2
    - probe prints HSA=[1] from a fresh login shell
  </acceptance_criteria>
  <done>DXG runtime libraries installed; env var survives login shells (verified via script file, not inline echo)</done>
</task>

<task type="auto">
  <name>Task 3: D-04 — pause Windows Adrenalin/driver auto-updates</name>
  <files>benchmarks/environment/versions.txt</files>
  <action>Windows side (PowerShell): set Windows Update paused via registry (UX\Settings PauseUpdatesExpiryTime +21 days) AND disable AMD auto-update notification by setting HKLM\SOFTWARE\AMD\CN → DriverUpdateNotifType=0 (Notify-me-only). If any step fails, record failure and fall back to documenting manual requirement. Append applied settings + frozen pairing (driver 32.0.31041.1004 / Adrenalin 26.10.41) to benchmarks/environment/versions.txt.</action>
  <verify>reg query HKLM\SOFTWARE\AMD\CN /v DriverUpdateNotifType returns 0; versions.txt contains 'Driver(frozen D-04)' line and 'updates-paused' line</verify>
  <acceptance_criteria>
    - versions.txt records both the frozen pairing AND the applied pause mechanism
  </acceptance_criteria>
  <done>D-04 fully delivered: pairing recorded + update path paused or explicitly escalated</done>
</task>

<task type="auto">
  <name>Task 4: GATE — gfx1100 enumeration + HIP device-execution smoke + fingerprints</name>
  <files>benchmarks/environment/rocminfo.txt, benchmarks/environment/hipconfig.txt, benchmarks/environment/versions.txt</files>
  <read_first>/etc/profile.d/rocdxg.sh</read_first>
  <action>Guest: source env; write 8-line HIP smoke kernel (hipMalloc + hipMemset + hipMemcpy back + hipGetDeviceProperties print gcnArchName) to /tmp/hipsmoke.cpp; compile with hipcc; run it — output MUST contain gfx1100. Capture: rocminfo full output → benchmarks/environment/rocminfo.txt; hipconfig --full → hipconfig.txt; append to versions.txt: ROCm .info/version, uname -r, librocdxg 1.2.2, smoke result, UTC date. FAILURE of enumeration OR smoke ⇒ STOP phase: platform kill-gate → native-Linux contingency decision escalates to owner.</action>
  <verify>cat benchmarks/environment/rocminfo.txt | grep -c gfx1100 ≥ 2; hipsmoke binary stdout contains 'gfx1100'; versions.txt non-empty with 4+ lines</verify>
  <acceptance_criteria>
    - rocminfo.txt contains Marketing Name 'AMD Radeon RX 7900 XT' with Name: gfx1100
    - HIP smoke executable runs without error and prints gfx1100 arch
  </acceptance_criteria>
  <done>ENV-01 fully satisfied including SC1's device-execution clause; fingerprints archived</done>
</task>

</tasks>

<verification>
- grep -c gfx1100 benchmarks/environment/rocminfo.txt returns ≥2
- HIP smoke binary executed successfully inside guest
- versions.txt carries ROCm 7.2.1 + frozen driver pairing + pause status
</verification>

<success_criteria>
All four tasks done; ENV-01 closed; platform kill-gate passed.
</success_criteria>

## Rollback notes
Guest stack disposable pre-snapshot: `amdgpu-install --uninstall` + `dpkg -r rocdxg-roct rocdxg-amd-smi-lib`.
