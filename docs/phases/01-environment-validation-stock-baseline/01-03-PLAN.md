---
phase: 1
plan: 01-03
type: auto
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf
  - models/README.md
  - benchmarks/environment/startup-log.txt
  - benchmarks/environment/vram-probe.txt
autonomous: true
must_haves:
  - truth: "artifact byte-exact vs locked sha256 before any model run"
  - truth: "startup log proves zero CPU fallback across BOTH Gated DeltaNet and gated attention layer families"
  - truth: "empirical (not DXG-reported) VRAM actuals recorded; D-02 .wslconfig escalation precedes any -ngl descent"
  - truth: "WSL snapshot tarball exists, taken serially last"
  - artifact: "models/README.md"
  - artifact: "benchmarks/environment/startup-log.txt"
---

# Plan 01-03: Model Artifact, Runtime Gate, Provenance & Snapshot

## Objective
Fetch + verify the locked IQ4_XS artifact, prove full-GPU residency of both layer families (ENV-03) with objective log predicates, record provenance + empirical VRAM (ENV-04), snapshot serial-last. D-07 superseded by owner 2026-08-22: download is ours.

<context>
Download may already be in flight from execution-start launch (models/*.gguf + logs). Adopt partial file with curl -C -. Expected: size 15309039008 B; sha256 53adc4bbed67044d662273356bbf3a50fdec667ac21bbf18d13e5815fbccc7f5; HF rev dee0a3164d9e11bbbebf5b63f52ba99443d14fc3.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download + sha256 gate</name>
  <files>models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf</files>
  <action>Windows side: curl -L -C - --retry 5 --retry-delay 5 -o models\Qwen3.8-27B-Uncensored-IQ4_XS.gguf https://huggingface.co/JonathanColetti/Qwen3.8-27B-Uncensored-GGUF/resolve/main/Qwen3.8-27B-Uncensored-IQ4_XS.gguf (resume-safe; adopt existing partial). Then guest-side hash gate over /mnt/e path: write expected-hash file to guest tmp via base64 pipe and `sha256sum -c`. Mismatch ⇒ delete file, ONE re-download, re-gate, else FAIL phase.</action>
  <verify>stat -c%s == 15309039008 AND sha256sum -c prints OK</verify>
  <acceptance_criteria>
    - exact byte size match
    - sha256sum exit 0 against locked digest
  </acceptance_criteria>
  <done>Artifact verified byte-exact — hard precondition for Task 2</done>
</task>

<task type="auto">
  <name>Task 2: Provenance record</name>
  <files>models/README.md</files>
  <read_first>docs/research/MODEL-DECISION.md</read_first>
  <action>Write models/README.md: repo id JonathanColetti/Qwen3.8-27B-Uncensored-GGUF · file Qwen3.8-27B-Uncensored-IQ4_XS.gguf · size 15,309,039,008 B · full sha256 · HF rev dee0a3164d9e11bbbebf5b63f52ba99443d14fc3 · quantizer llama.cpp a94d563ed · imatrix embedded (wikitext-2, 200×512 chunks) · base Qwen/Qwen3.8-27B Apache-2.0 · arch qwen35 hybrid: 48 Gated DeltaNet + 16 gated attention layers, ctx 262144 native, MTP head present · verified-date.</action>
  <verify>grep -c 'sha256\|dee0a316\|qwen35' models/README.md ≥ 3</verify>
  <acceptance_criteria>
    - all ten provenance fields present with verified date
  </acceptance_criteria>
  <done>ENV-04 artifact provenance complete</done>
</task>

<task type="auto">
  <name>Task 3: ENV-03 runtime residency gate (objective predicates)</name>
  <files>benchmarks/environment/startup-log.txt, benchmarks/environment/vram-probe.txt</files>
  <read_first>benchmarks/environment/llamacpp-pin.txt</read_first>
  <action>Guest, using archived baseline binary (NOT build-ci copy): run llama-cli -m /mnt/e/.../models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf -ngl 99 -c 2048 -p 'Hello' -n 32 --temp 0 -e, tee full output to benchmarks/environment/startup-log.txt. OBJECTIVE PASS PREDICATES on startup-log.txt: (a) contains 'load_tensors: offloaded 64/64 layers' or equivalent full-count offload line; (b) contains CUDA0 buffer size lines whose KiB total ≥ 14000000 KiB (~13.7+ GiB resident); (c) ZERO lines matching 'CPU model buffer|CPU KV buffer|CUDA0 failed|failed to allocate'; (d) process exit code 0 AND ≥16 emitted non-whitespace chars. On allocation-failure: FIRST apply D-02 escalation — create %USERPROFILE%\.wslconfig with [wsl2] memory=24GB, `wsl --shutdown`, retry once (ROCm#6022-class clamp hypothesis); ONLY then descend -ngl {96,92,88,...}, recording each attempt's largest fully-resident config into vram-probe.txt with method note 'escalating -ngl residency via startup logs; DXG numbers recorded for contrast only'. Partial residency WITHOUT documented D-02 attempt = task failure.</action>
  <verify>Predicates (a)-(d) all true on startup-log.txt; vram-probe.txt exists with probe-method header</verify>
  <acceptance_criteria>
    - offload line shows 64/64 (or best config after logged D-02 + descent)
    - no forbidden fallback lines present
    - exit 0 with ≥16 generated characters
  </acceptance_criteria>
  <done>ENV-03 satisfied: both GDN and gated-attention paths resident on gfx1100, evidence archived</done>
</task>

<task type="auto">
  <name>Task 4: SERIAL-LAST — WSL snapshot + ledger append</name>
  <files>benchmarks/environment/versions.txt</files>
  <read_first>benchmarks/environment/versions.txt</read_first>
  <action>Only after Tasks 1-3 pass: PowerShell checks E:\ free space ≥30 GB, creates E:\wsl-snapshots, `wsl --terminate Ubuntu-24.04`, `wsl --export Ubuntu-24.04 E:\wsl-snapshots\ubuntu-2404-rocm721-phase1.tar`. Then append to benchmarks/environment/versions.txt: 'snapshot: ubuntu-2404-rocm721-phase1.tar <exact-bytes> <utc-timestamp>'.</action>
  <verify>Tarball >2 GB exists; versions.txt final line starts with 'snapshot:'</verify>
  <acceptance_criteria>
    - snapshot file present with recorded size in versions ledger
  </acceptance_criteria>
  <done>Phase-1 environment frozen and recoverable; contingency insurance captured</done>
</task>

</tasks>

<verification>
- sha256 gate green before runtime gate (ordering enforced by depends_on chain within plan)
- startup-log.txt passes all four predicates
- snapshot tarball exists with ledger entry
</verification>

<success_criteria>
Locked artifact runs fully on GPU at stock baseline; environment state snapshotted.
</success_criteria>

## Rollback notes
Model re-downloadable; snapshot redoable; no destructive steps anywhere in plan.
