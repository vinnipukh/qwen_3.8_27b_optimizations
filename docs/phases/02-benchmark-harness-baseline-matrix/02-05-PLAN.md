---
phase: 2
plan: 02-05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - benchmarks/vulkan/build-vulkan-arm.ps1
  - benchmarks/vulkan/vulkan-pin.txt
  - benchmarks/vulkan/gate-report.txt
  - benchmarks/tests/vulkan_gate.sh
  - benchmarks/environment/vulkan-support.csv
  - benchmarks/environment/hip-support-comparator.csv
  - benchmarks/lib/guard.py
  - benchmarks/vulkan/run_session_vulkan.py
  - benchmarks/bin/publish_matrix.py
  - benchmarks/results/BASELINE-MATRIX.md
  - benchmarks/RUNBOOK.md
autonomous: true
requirements: [BENCH-04]
user_setup:
  - service: VS2022 Build Tools + Vulkan SDK
    why: "D2-02 locks the native-Windows comparator-arm toolchain (MSVC + glslc) — these are human-installed system software Claude cannot silently install"
    env_vars:
      - name: VULKAN_SDK
        source: "Set by the LunarG Vulkan SDK installer (default C:\\VulkanSDK\\<version>)"
    dashboard_config:
      - task: "Install VS2022 Build Tools with the C++ desktop workload, and the LunarG Vulkan SDK including glslc"
        location: "visualstudio.microsoft.com/downloads + vulkan.lunarg.com/sdk/home"
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A llama.cpp build at EXACTLY bb4caa7540188872173c44d161602d9271386413 with GGML_VULKAN=ON exists in a separate native-Windows tree (E:\\vulkan-arm\\llama.cpp, D2-02/D2-03), its provenance recorded in benchmarks/vulkan/vulkan-pin.txt"
    - "The six-part D2-04 coverage gate is executable as benchmarks/tests/vulkan_gate.sh, archives both backends' support CSVs, and exits non-zero until every part passes — no Vulkan benchmark row can exist before GATE_GREEN"
    - "A full guarded, fingerprinted Vulkan matrix session lands in the identical run-store discipline: manifest carries backend_arm=Vulkan with the designed not-applicable-native-arm degradations (02-02), rows fsynced, checksums closed"
    - "benchmarks/results/BASELINE-MATRIX.md carries a backend-named Vulkan section beside the HIP matrix, with a per-cell HIP-vs-Vulkan delta table and any partial-support/CPU-fallback caveat verbatim"
  artifacts:
    - path: "benchmarks/vulkan/build-vulkan-arm.ps1 + vulkan-pin.txt"
      provides: "pinned native build procedure + S1-style provenance record (commit/configure/toolchain/binary-sha256s/captured-UTC)"
    - path: "benchmarks/tests/vulkan_gate.sh"
      provides: "six-part D2-04 gate: support CSVs both arms, four GDN-family ops asserted, residency 132/0, greedy smoke, caveats verbatim"
    - path: "benchmarks/vulkan/run_session_vulkan.py"
      provides: "native session driver reusing the locked wave-1 contracts with platform-appropriate paths"
    - path: "benchmarks/bin/publish_matrix.py (compare mode)"
      provides: "backend-named Vulkan publication + HIP-vs-Vulkan delta table in BASELINE-MATRIX.md"
  key_links:
    - from: "benchmarks/tests/vulkan_gate.sh"
      to: "benchmarks/environment/{vulkan-support.csv,hip-support-comparator.csv}"
      via: "gate consumes exactly what it archives; four named ops must be supported on the Vulkan CSV"
      pattern: "supported=true"
    - from: "benchmarks/vulkan/run_session_vulkan.py"
      to: "benchmarks/lib/{llabench,fingerprint,store,guard,toast}.py"
      via: "wave-1 contracts imported with overridden bin_path/model (paths are parameters, not hard-coded)"
      pattern: "backend_arm.*Vulkan"
    - from: "benchmarks/bin/publish_matrix.py"
      to: "benchmarks/results/BASELINE-MATRIX.md"
      via: "appends D2-19-ordered Vulkan section where every performance claim names backend Vulkan"
      pattern: "backend: Vulkan"
---

<objective>
Deliver the second half of BENCH-04: the stock-Vulkan comparator arm — a native-Windows build of the SAME pin bb4caa75 (only the backend differs, per D2-03), protected by the six-part D2-04 coverage gate BEFORE any Vulkan number publishes, then one guarded, fingerprinted matrix session through the same locked wave-1 contracts as HIP, published backend-named into BASELINE-MATRIX.md.

Purpose: phases 3-6 compare every optimization against BOTH stock baselines; an ungated or unfingerprinted Vulkan number would poison cross-backend claims (comparator honesty is a locked CONTEXT requirement).
Output: benchmarks/vulkan/ build+pin+gate records, benchmarks/tests/vulkan_gate.sh, archived support CSVs for both arms, extended guard.py (Windows poller), run_session_vulkan.py, publish_matrix.py compare mode, Vulkan section in BASELINE-MATRIX.md, RUNBOOK §comparator-arm amendment.
GPU COST (owner-visible): Task 1 GPU-light (~5 min version checks; compile dominates wall-clock); Task 2 ~15 min GPU session; Task 3 ~45-90 min continuous GPU session. Toasts fire on failures and at session close per D2-15.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/phases/02-benchmark-harness-baseline-matrix/02-CONTEXT.md
@docs/phases/02-benchmark-harness-baseline-matrix/02-RESEARCH.md
@docs/phases/02-benchmark-harness-baseline-matrix/02-PATTERNS.md

Wave-1 contracts available (locked by plans 02-01/02/02-03): llabench.{build_argv (bin_path/model are overridable parameters),parse_rows,assert_cell_integrity,repro_ok,TIERS}; fingerprint.collect_manifest(backend_arm, telemetry_mode) — ROCm/librocdxg/guest-kernel/.wslconfig fields degrade to the literal not-applicable-native-arm by design; guard.Thresholds/evaluate + store.RunStore + toast.send/send_summary. Session facts: native Windows host Python 3.14.7; frozen Adrenalin driver pairing (32.0.31041.1004) supplies the Vulkan runtime; guest HIP binaries reachable via wsl.exe; model reachable from Windows via the \\wsl.localhost\Ubuntu-24.04 UNC path or a native copy — whichever is used MUST be recorded in vulkan-pin.txt (load-path is provenance-relevant); Adrenalin reports TRUE free VRAM (no DXG deficit natively, so a 32k PASS is legitimate); HWiNFO duty-cycle budget still applies to native sessions.
</context>

## Artifacts this phase produces (this plan)
New files: benchmarks/vulkan/build-vulkan-arm.ps1 · benchmarks/vulkan/vulkan-pin.txt · benchmarks/vulkan/build-log.txt · benchmarks/vulkan/gate-report.txt · benchmarks/tests/vulkan_gate.sh · benchmarks/environment/{vulkan-support.csv, hip-support-comparator.csv} · benchmarks/vulkan/run_session_vulkan.py
Modified: benchmarks/lib/guard.py (platform-dispatched Windows RSS poller, evaluate() untouched) · benchmarks/bin/publish_matrix.py (compare mode) · benchmarks/results/BASELINE-MATRIX.md (backend-named Vulkan section) · benchmarks/RUNBOOK.md (§comparator-arm dated amendment)
Generated (in-repo, append-only): Vulkan run dirs under benchmarks/results/<YYYYMMDD_HHMMSS>_<run-id>/ in the standard store layout
Commands introduced: `powershell -File benchmarks/vulkan/build-vulkan-arm.ps1` (host) · `bash benchmarks/tests/vulkan_gate.sh` (host) · `python benchmarks/vulkan/run_session_vulkan.py ...` (host)

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — native-Windows Vulkan build at the locked pin proves the toolchain end-to-end</name>
  <precondition>VS2022 Build Tools (C++ workload) and Vulkan SDK with glslc installed per user_setup (D2-02 toolchain); assert glslc and cl.exe resolvable before starting</precondition>
  <files>benchmarks/vulkan/build-vulkan-arm.ps1, benchmarks/vulkan/vulkan-pin.txt</files>
  <read_first>02-CONTEXT.md decisions D2-02 and D2-03; 02-RESEARCH.md docs/build.md Vulkan-section pointer (lines 397-470) + Environment Availability Vulkan row; 02-PATTERNS.md S1 capture format with llamacpp-pin.txt as the exact record analog</read_first>
  <action>
    GPU-light: ~5 min total (version checks only — no model load in this task; compile dominates wall-clock).
    Create benchmarks/vulkan/build-vulkan-arm.ps1 (PowerShell, host-side): clone https://github.com/ggml-org/llama.cpp into E:\vulkan-arm\llama.cpp — a SEPARATE native tree that never shares the WSL guest checkout (D2-02) — then check out exactly bb4caa7540188872173c44d161602d9271386413 and ABORT unless git rev-parse HEAD equals that hash (D2-03 pin identity is a hard gate, not a note). Configure with cmake using -DGGML_VULKAN=ON -DCMAKE_BUILD_TYPE=Release -DLLAMA_CURL=OFF -DLLAMA_BUILD_SERVER=OFF (mirroring the llamacpp-pin.txt flag vocabulary so both arms differ ONLY by backend), build Release configs for llama-cli, llama-bench, and test-backend-ops; tee the FULL build output to benchmarks/vulkan/build-log.txt. Resume-safe: if the checkout already sits at the pin with all three exes present, skip straight to verification (house resume-skip idiom).
    Run it, then record benchmarks/vulkan/vulkan-pin.txt in the llamacpp-pin.txt key:value vocabulary: pin-tag v0.2.0; commit bb4caa7540188872173c44d161602d9271386413; configure flags incl. GGML_VULKAN=ON; MSVC compiler version; glslc version; source-tree path E:\vulkan-arm\llama.cpp; sha256 of each of the three built exes; the model load path actually usable from Windows (native copy or \\wsl.localhost\Ubuntu-24.04\root\models\Qwen3.8-27B-Uncensored-IQ4_XS.gguf UNC reference — RECORD WHICH); closing captured: <ISO8601>Z UTC line.
    Tracer proof: run llama-cli.exe --version on the freshly built exes and confirm the printed commit is bb4caa7540188872173c44d161602d9271386413 — proving clone, pin identity, Vulkan toolchain, and link all succeeded before anything heavier runs.
  </action>
  <verify>
    <automated>grep -qx "commit: bb4caa7540188872173c44d161602d9271386413" benchmarks/vulkan/vulkan-pin.txt && test -s benchmarks/vulkan/build-log.txt && powershell.exe -NoProfile -Command "& 'E:\vulkan-arm\llama.cpp\build\bin\Release\llama-cli.exe' --version" | grep -c bb4caa75</automated>
  </verify>
  <acceptance_criteria>
    - vulkan-pin.txt records pin-tag, exact commit, configure flags (GGML_VULKAN=ON), MSVC + glslc versions, source-tree path, three binary sha256s, model load path, and captured-UTC line
    - build-log.txt shows the GGML_VULKAN=ON configure line and successful Release builds of llama-cli, llama-bench, test-backend-ops
    - llama-cli --version output names commit bb4caa7540188872173c44d161602d9271386413 (grep count >= 1)
  </acceptance_criteria>
  <done>Native comparator arm exists at the EXACT HIP pin in its own Windows tree (D2-02/D2-03 honored), with machine-checkable provenance</done>
</task>

<task type="auto">
  <name>Task 2: Six-part D2-04 coverage gate — vulkan_gate.sh blocks any Vulkan publish until green</name>
  <precondition>Native arm built at pin (Task 1 complete); guest HIP binaries reachable via wsl.exe (Phase-1 artifacts)</precondition>
  <files>benchmarks/tests/vulkan_gate.sh, benchmarks/environment/vulkan-support.csv, benchmarks/environment/hip-support-comparator.csv, benchmarks/vulkan/gate-report.txt</files>
  <read_first>02-CONTEXT.md decision D2-04 (the authoritative six parts); 02-RESEARCH.md "test-backend-ops support CSV columns"; 02-PATTERNS.md vulkan_gate.sh row (docs/TESTING.md gate doctrine + house bash idioms: set -euo pipefail, per-step status echoes, resume-skip-on-existing-output)</read_first>
  <action>
    GPU session: ~15 min (support CSVs + full op sweep + one tiny model load for residency/smoke).
    Write benchmarks/tests/vulkan_gate.sh implementing ALL SIX D2-04 parts as individually tagged steps with distinct exit codes per failing part (S5) and resume-skip when archived evidence already exists. Part 1: verify the static shader-inventory evidence at pin is archived and names gated_delta_net.comp, solve_tri.comp, dequant_iq4_xs.comp as present — and carry the recorded nuance verbatim into the report: no dedicated mul_mat_vec_iq4_xs vec-dot shader exists at this commit; IQ4_XS decode routes through generic mul_mat_vecq. Part 2: run test-backend-ops support --output csv on BOTH backends — HIP side via the guest binary through wsl.exe, Vulkan side via the native exe — archiving both outputs VERBATIM to benchmarks/environment/hip-support-comparator.csv and vulkan-support.csv (Phase-1 test-backend-ops-phase1.txt precedent); assert the GATED_DELTA_NET, SOLVE_TRI, SSM_CONV, and SSM_SCAN rows show supported on the Vulkan CSV. Part 3: run the FULL test-backend-ops suite on the Vulkan backend and require exit 0. Part 4: verbose-startup residency — one llama-cli -v load capturing per-layer assignment lines; assert 132 tensor layers assigned to the Vulkan device and 0 to CPU (v0.2.0 has no offload summary line; the per-layer lines ARE the evidence per Phase-1 finding). Part 5: coherent greedy-decode smoke on the fixed prompt benchmarks/prompts/short_code_01.txt (--temp 0 --single-turn), archiving non-empty output. Part 6: scan both CSVs for partial-support/error rows and CPU-fallback indicators, emitting ANY such row verbatim into the report. Close benchmarks/vulkan/gate-report.txt with the house verdict:/evidence:/flags: triple (S1) and a final line GATE_GREEN written ONLY when all six parts passed; any failure leaves a non-zero exit so downstream publication is impossible.
    Gate doctrine (docs/TESTING.md): gates ORDER performance claims — this script passing is the precondition the Task-3 publication step asserts.
  </action>
  <verify>
    <automated>bash benchmarks/tests/vulkan_gate.sh && tail -n 1 benchmarks/vulkan/gate-report.txt | grep -x "GATE_GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - All six parts executed with their evidence archived; gate-report.txt ends with the GATE_GREEN verdict triple
    - hip-support-comparator.csv and vulkan-support.csv archived verbatim; the four named ops show supported on the Vulkan CSV
    - Residency evidence shows 132 device layers / 0 CPU layers from the captured verbose log
    - Greedy smoke output archived non-empty; every partial-support or fallback row appears verbatim in the report
  </acceptance_criteria>
  <done>D2-04 satisfied mechanically: no Vulkan number can come into existence ahead of the six-part gate</done>
</task>

<task type="auto">
  <name>Task 3: Guarded Vulkan matrix session + backend-named comparator publication (per D2-16..D2-19)</name>
  <precondition>GATE_GREEN from Task 2; benchmarks/config/thresholds.json exists (calibrated by plan 02-04) so the guard runs enforcing rather than observe-only; GPU/host cool-idle; HWiNFO duty-cycle budget available</precondition>
  <files>benchmarks/lib/guard.py, benchmarks/vulkan/run_session_vulkan.py, benchmarks/bin/publish_matrix.py, benchmarks/results/BASELINE-MATRIX.md, benchmarks/RUNBOOK.md</files>
  <read_first>Wave-1 SUMMARYs for the locked public contracts (llabench/fingerprint/store/guard/toast); 02-CONTEXT.md decisions D2-15..D2-19; 02-RESEARCH.md Open Question 3 resolution (invocation parity: identical cells, threads = physical cores, deviations recorded in manifest); 02-PATTERNS.md S4 spawn shape adapted natively</read_first>
  <action>
    GPU session: ~45-90 min continuous, started cool/idle per D2-16; stock tune-state re-verified visually at start (Adrenalin factory settings) feeding manifest tune_state (D2-17).
    First extend benchmarks/lib/guard.py minimally: add a platform-dispatched Windows poller (ctypes psapi GetProcessMemoryInfo reading WorkingSetSize/PagefileUsage) filling the SAME RssProfile shape produced by the /proc poller, so evaluate(), Thresholds, and every existing test are untouched — zero new threshold constants anywhere.
    Create benchmarks/vulkan/run_session_vulkan.py (HOST Python 3.14) mirroring the plan-02-04 orchestrator sequence natively: acquire an exclusive lock-file session lock; create the run dir via store.RunStore.create; collect fingerprint.collect_manifest(backend_arm="Vulkan", telemetry_mode=<detected from the daemon mode file else absent>) — the ROCm, librocdxg, guest-kernel, and .wslconfig fields take the designed literal not-applicable-native-arm (honest degradation, NEVER omission, per the 02-02 design); spawn hwinfo_daemon --watch and thermal_watchdog --pid-file --threshold-c 95 --kill-mode native (the taskkill mode built in plan 02-02) when reachable, recording absence gracefully otherwise. For each tier in ascending order call llabench.build_argv(prompt_tokens=tier, gen_pairs=[(tier,128)], bin_path=E:\vulkan-arm\llama.cpp\build\bin\Release\llama-bench.exe, model=<the path recorded in vulkan-pin.txt>) — IDENTICAL cell semantics to HIP: -p tier AND -pg tier,128, explicit -n 0 killing the default vectors, banned empty-context cells impossible by construction, repeats 5, warmup on, --delay cooldown gaps between heavy tiers, D2-19 order identical to every future session (Open Question 3 parity resolution). Run the MANDATORY pre-flight for the 32k tier: weights+KV buffer math is portable; free MiB comes from the NATIVE verbose startup log's device-memory figure (Adrenalin reports true VRAM — no DXG deficit — so a PASS legitimately executes guarded like any cell; predicted-over-budget publishes FAILED:preflight-oom rows and moves on, D2-18). Guard-poll the child PID; enrich rows (backend_arm="Vulkan", guard verdict object, telemetry slice reference) fsync-per-row into rows.jsonl; close with CHECKSUMS.sha256; fire toast_summary (D2-15). NO retry loops anywhere (fail-fast policy).
    Extend benchmarks/bin/publish_matrix.py with a compare mode reading the latest HIP and latest Vulkan run dirs: enforce D2-19 ordering, compute per-cell HIP-vs-Vulkan deltas (mean ts difference and percent), and APPEND a Vulkan section to benchmarks/results/BASELINE-MATRIX.md where every performance claim states backend: Vulkan, failed cells appear with reason verbatim (S6), and every part-6 partial-support caveat from gate-report.txt is embedded near the table. Run the contamination scan before writing anything.
    Amend benchmarks/RUNBOOK.md with a dated §comparator-arm block: build-provenance pointer, gate-evidence reference, native-session protocol deltas, and the model-load-path note.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/ -x -q' && grep -c "Vulkan" benchmarks/results/BASELINE-MATRIX.md</automated>
    <human-check>Owner eyeballs the Vulkan section of BASELINE-MATRIX.md: magnitudes plausible against the HIP anchors, decode gap in the llama.cpp#20934 15-25% ballpark, every row backend-named, caveats visible.</human-check>
  </verify>
  <acceptance_criteria>
    - Vulkan run dirs complete in the standard store layout: manifest with backend_arm=Vulkan and explicit not-applicable-native-arm fields, fsynced enriched rows, CHECKSUMS.sha256 verifying
    - 32k tier went through the mandatory pre-flight — either a guarded PASS execution or honest FAILED:preflight-oom publication, never silence
    - BASELINE-MATRIX.md carries a D2-19-ordered Vulkan section with every row naming backend Vulkan, failed cells with reasons, caveats verbatim, plus a HIP-vs-Vulkan delta table
    - Guard ran natively through the new Windows poller with evaluate()/threshold semantics unchanged (existing suites still green)
  </acceptance_criteria>
  <done>BENCH-04 delivered whole: the stock-Vulkan comparator stands beside the HIP baseline, gated by D2-04, guarded like HIP, and impossible to quote without its backend name</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub clone -> native build | upstream code is fetched and compiled into locally-executed binaries |
| host <-> guest interop | support CSVs and daemon control cross the wsl.exe boundary |
| publisher -> citable baselines | decides which numbers become project-reference comparisons |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering/Elevation | cloned-source native build | high | mitigate | exact-pin checkout enforced by rev-parse equality ABORT before configure (D2-03); official upstream URL only; binary sha256s recorded in vulkan-pin.txt |
| T-02-05-02 | Repudiation | comparator provenance | medium | mitigate | identical manifest/store/CHECKSUMS discipline as HIP; backend_arm=Vulkan on every row; gate report archived alongside results |
| T-02-05-03 | DoS | thermal excursion during native sessions | medium | mitigate | thermal watchdog armed --kill-mode native (mechanics rehearsed at plan-02-04 calibration), D2-20 firmware backstop, cool/idle session start |
| T-02-05-04 | Tampering | publishing before the gate is green | medium | mitigate | Task 3 precondition requires GATE_GREEN; vulkan_gate.sh distinct-exit-code contract; publisher embeds caveats only from the archived gate report |
| T-02-05-SC | Tampering | dependency supply chain (source clone + SDK installers) | medium | mitigate | pinned-commit verification at clone time; SDK arrives via vendor-signed installers declared in user_setup; zero registry packages (no pip/npm/cargo installs in this plan) |
</threat_model>

<verification>
- vulkan_gate.sh exits 0 ending GATE_GREEN with all six D2-04 parts evidenced and both support CSVs archived
- Guest quick suite green: python3 -m pytest benchmarks/tests/ -x -q (guard extension provably did not alter evaluate/threshold semantics)
- BASELINE-MATRIX.md contains backend-named HIP and Vulkan sections plus the delta table; Vulkan run dirs verify via sha256sum -c
</verification>

<success_criteria>
A reader quoting any baseline number learns its backend from the row itself; the Vulkan comparator was built at the identical pin, gated before publication, guarded during measurement, and stored indistinguishably from HIP evidence.
</success_criteria>

<output>
Create `docs/phases/02-benchmark-harness-baseline-matrix/02-05-SUMMARY.md` when done
</output>
