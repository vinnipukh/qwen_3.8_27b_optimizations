---
phase: 2
plan: 02-03
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/lib/store.py
  - benchmarks/lib/guard.py
  - benchmarks/lib/preflight.py
  - benchmarks/tests/test_guard_fixtures.py
  - benchmarks/tests/test_journal_crash.py
  - benchmarks/tests/test_preflight.py
  - benchmarks/tests/fixtures/gen_rss_trace.py
autonomous: true
requirements: [BENCH-03]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A run directory is a crash-resilient append-only journal: enriched rows land with fsync per row, a simulated SIGKILL mid-write loses nothing already written, and CHECKSUMS.sha256 closes every run in sha256sum -c format (D2-09)"
    - "The three-signal guard converts spiked RSS/shared-GPU-memory/repeat-deviation traces into the exact locked verdict vocabulary, driven by thresholds loaded from calibration output — never guessed hard-coded caps (D2-12/D2-13/D2-14)"
    - "Pre-flight buffer math against the measured DXG anchor yields PASS or FAILED:preflight-oom before any risky tier allocates (D2-18)"
    - "Per-cell VRAM ledger entries record buffers parsed from llama.cpp verbose logs plus guard peaks in the house verdict/evidence/flags format"
  artifacts:
    - path: "benchmarks/lib/store.py"
      provides: "RunStore.create/append_row(fsync)/write_checksums/supersede"
    - path: "benchmarks/lib/guard.py"
      provides: "/proc poller + three-signal evaluator + threshold loader"
    - path: "benchmarks/lib/preflight.py"
      provides: "buffer-math estimator + DXG anchor parser + verdict record"
    - path: "benchmarks/tests/test_guard_fixtures.py"
      provides: "D2-14 permanent regression: synthetic spill series must FAIL"
  key_links:
    - from: "benchmarks/lib/store.py"
      to: "CHECKSUMS.sha256"
      via: "sha256sum -c compatible manifest written at run close"
      pattern: "sha256"
    - from: "benchmarks/lib/guard.py"
      to: "benchmarks/config/thresholds.json (written by plan 02-04 calibration)"
      via: "Thresholds.from_json; absent file switches guard to observe-only mode"
      pattern: "thresholds"
    - from: "benchmarks/lib/preflight.py"
      to: "benchmarks/environment/startup-log.txt DXG line"
      via: "free-MiB parsed from the device-init pattern (18245 of 20421 MiB at freeze)"
      pattern: "MiB free"
---

<objective>
Build BENCH-03's defense layer: the crash-resilient append-only result store (D2-09/D2-11), the three-signal RSS/spill guard that labels silent overcommit instead of trusting flowing tokens (D2-12), empirically-thresholded config loading (D2-13), fixture-based guard validation plus the supervised near-OOM hook (D2-14), and the 32k pre-flight gate (D2-18).

Purpose: microsoft/WSL#11050 means spilled runs still emit tokens at 5-10x collapse — without this layer the matrix would publish lies.
Output: benchmarks/lib/{store.py,guard.py,preflight.py} + three permanent regression test modules. CPU-only plan: no GPU sessions required.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/phases/02-benchmark-harness-baseline-matrix/02-CONTEXT.md
@docs/phases/02-benchmark-harness-baseline-matrix/02-RESEARCH.md
@docs/phases/02-benchmark-harness-baseline-matrix/02-PATTERNS.md
@benchmarks/environment/vram-probe.txt

Facts (verified): upstream jsonl printer already fflushes each row (llama-bench.cpp:1806) so raw cells survive hard aborts — store.py adds the enrichment layer on top; OOM exits code 1 losing only remaining cells of THAT invocation; /proc/<pid>/status exposes VmRSS/VmSwap; DXG anchor from device-init log is "(20421 MiB, 18245 MiB free)"; KV cost ~64 KiB/token f16 for this hybrid arch (16 attention layers); weights 15.31 GB; house verdict format is verdict:/evidence:/flags: triples (vram-probe.txt style); verdict vocabulary locked: OK | FAILED:suspected-spill | FAILED:preflight-oom | FAILED:thermal-abort.
</context>

## Artifacts this phase produces (this plan)
New files: benchmarks/lib/store.py · benchmarks/lib/guard.py · benchmarks/lib/preflight.py · benchmarks/tests/test_guard_fixtures.py · benchmarks/tests/test_journal_crash.py · benchmarks/tests/test_preflight.py · benchmarks/tests/fixtures/gen_rss_trace.py
Commands introduced: `python3 -m pytest benchmarks/tests/test_{guard_fixtures,journal_crash,preflight}.py` (guest, CPU-only)

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — store round-trip survives SIGKILL and closes with verifiable checksums</name>
  <reversibility rating="costly">rows.jsonl enrichment schema and manifest field names are consumed by Phase 3 profiling and CTX-01 ledger extension; changing them after those land needs a coordinated cross-phase change</reversibility>
  <files>benchmarks/lib/store.py, benchmarks/tests/test_journal_crash.py</files>
  <read_first>02-CONTEXT.md decisions D2-09 + D2-11; 02-RESEARCH.md Pattern 2 (crash-resilient journal, verbatim semantics); 02-PATTERNS.md store.py assignment (models/README hash-of-record discipline)</read_first>
  <action>
    First, idempotently ensure the Wave-0 test scaffold independent of plan 02-01 landing first: if benchmarks/tests/pytest.ini is absent, create it with the minimal testpaths-relative config; if the guest lacks pytest, apt-get install -y python3-pytest. Both steps are existence-checked so parallel wave-mates converge on identical content instead of racing.
    Create benchmarks/lib/store.py implementing exactly: RunStore.create(results_root, run_id) -> RunStore creating results/<run_id>/ with telemetry/ and logs/ subdirs (run_id format YYYYMMDD_HHMMSS_<label> per D2-09); .append_row(dict) serializing compact JSON, appending one line, flush() then os.fsync(fileno()) EVERY row; .write_checksums() walking the run dir and emitting CHECKSUMS.sha256 in sha256sum -c compatible <hash>  <relative-path> lines (excluding CHECKSUMS.sha256 itself); .supersede(old_run_id, reason) recording {supersedes:{run_id,reason}} into the store's meta.json so corrections keep both runs visible per D2-11; .index_entry() returning the dict appended by callers to results/index.jsonl (run-id, UTC close time, backend_arm, ok/failed counts). Enrichment contract for callers (documented module docstring): each row carries backend_arm, run_id, guard{verdict,vmrss_peak_kb,vmswap_peak_kb}, telemetry_slice path reference — plan 02-04's orchestrator fills these.
    Tracer proof on real filesystem: create a scratch RunStore, append five enriched rows, simulate writer death via os.kill(os.getpid(), signal.SIGKILL) inside a forked child AFTER fsyncing row three, reopen from parent and assert all fsynced rows persist byte-exact, then write_checksums and verify with `sha256sum -c CHECKSUMS.sha256` exit 0.
    test_journal_crash.py encodes that scenario permanently using tmpdir + multiprocessing fork: child appends N rows with fsync, gets SIGKILLed mid-stream; parent asserts prefix integrity and checksum closure. Also assert append_row ordering and that CHECKSUMS verification fails if any tracked file is later modified (tamper evidence).
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_journal_crash.py -x -q'</automated>
  </verify>
  <acceptance_criteria>
    - Forked-child SIGKILL test proves every fsynced row survives and none are corrupted
    - sha256sum -c CHECKSUMS.sha256 exits 0 on a closed run dir, and exits non-zero after a scripted single-byte tamper of a tracked file
    - supersede writes meta.json referencing old run-id while keeping it intact
  </acceptance_criteria>
  <done>D2-09/D2-11 store semantics proven including crash and tamper paths</done>
</task>

<task type="auto">
  <name>Task 2: Three-signal guard with calibration-loaded thresholds (per D2-12, D2-13, D2-14 fixtures half)</name>
  <files>benchmarks/lib/guard.py, benchmarks/tests/test_guard_fixtures.py, benchmarks/tests/fixtures/gen_rss_trace.py</files>
  <read_first>02-CONTEXT.md decisions D2-12/D2-13/D2-14; 02-RESEARCH.md Pitfall 7 (silent spill) + Validation Architecture map row for test_guard_fixtures.py; 02-PATTERNS.md S5 tagged-output fail-fast style</read_first>
  <action>
    Create benchmarks/lib/guard.py implementing exactly: class Thresholds with fields vmrss_fail_kb, vmswap_fail_kb, gpu_shared_climb_mb_per_min, repeat_deviation_max_ratio (default 2.0), loaded via Thresholds.from_json(path) reading benchmarks/config/thresholds.json (the file plan 02-04's calibration session writes); when the file is ABSENT, from_json returns None and the evaluator runs observe_only=True — recording profiles but never failing rows, because no guessed caps may exist before empirical derivation (D2-13). poll_proc(pid:int, stop_event, interval_s=1.0) -> RssProfile reading /proc/<pid>/status VmRSS/VmSwap lines at 1 Hz, tracking peaks and keeping the series (process disappearance ends polling cleanly — not an error). evaluate(rss_profile|None, gpu_shared_series|None, repeat_means|None, thresholds|None, observe_only=False) -> GuardVerdict(verdict:str, signals:dict, flagged_for_review:bool): signal 1 trips when vmrss or vmswap peak crosses its threshold during steady state; signal 2 trips when shared-GPU-memory series climbs monotonically beyond gpu_shared_climb_mb_per_min across a decode window; signals 1+2 set verdict FAILED:suspected-spill exactly. Signal 3 (>repeat_deviation_max_ratio spread across repeat means) sets flagged_for_review=True and verdict REVIEW:repeat-deviation — the decision text distinguishes review-flagging from the spill verdict, and REVIEW rows stay out of the clean published table pending owner disposition through the supersede model. Verdict strings come ONLY from the locked vocabulary constants defined module-level in guard.py itself — VERDICT_OK, VERDICT_SPILL, VERDICT_REVIEW, plus VERDICT_THERMAL ("FAILED:thermal-abort") and VERDICT_PREFLIGHT ("FAILED:preflight-oom") declared in that same block; sibling modules that emit markers (plan 02-02's watchdog, preflight.py) repeat the locked literal and both suites pin string equality, so no cross-plan import edge exists between wave-1 plans.
    gen_rss_trace.py generates trace fixtures: healthy flat series, slow-climb series, spike-crossing series, swap-growing series, and repeat-mean sets at ratios 1.3x and 2.5x. test_guard_fixtures.py (permanent D2-14 regression): healthy+observe-only yield OK; every spiked variant yields FAILED:suspected-spill with the tripping signal named in signals dict; 2.5x repeats yield flagged_for_review True; absent thresholds never produce FAILED. CPU-only, no GPU.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_guard_fixtures.py -x -q'</automated>
  </verify>
  <acceptance_criteria>
    - All spiked fixtures produce FAILED:suspected-spill; healthy fixtures produce OK; ratio case produces REVIEW flag
    - No threshold constant appears in source — evaluator requires Thresholds object or observe-only mode (grep for large literal kb numbers in guard.py returns nothing)
    - Suite runs under 30 s with zero GPU contact
  </acceptance_criteria>
  <done>Silent-overcommit detection logic proven against adversarial fixtures; live trip evidence lands at calibration (plan 02-04)</done>
</task>

<task type="auto">
  <name>Task 3: VRAM pre-flight gate + per-cell ledger records (per D2-18)</name>
  <files>benchmarks/lib/preflight.py, benchmarks/tests/test_preflight.py</files>
  <read_first>02-CONTEXT.md decision D2-18; 02-RESEARCH.md Open Question 2 (pre-flight method choice) + Don't Hand-Roll free-VRAM row; benchmarks/environment/vram-probe.txt and startup-log.txt first lines</read_first>
  <action>
    Method choice (planner discretion, documented here): buffer math decides go/no-go; a startup probe confirms only if the math is within 10 percent of the budget edge. Create benchmarks/lib/preflight.py implementing exactly: KV_BYTES_PER_TOKEN_F16 = 64*1024 (hybrid arch, 16 attention layers); WEIGHTS_BYTES = 15309039008 (byte size-of-record 15,309,039,008 from models/README.md — the tracked provenance file for the sha256-of-record artifact); parse_free_mib(text) -> int extracting total/free from the llama.cpp device-init line pattern containing MiB figures in parentheses; parse_buffer_lines(stderr_text) -> dict extracting KV/compute buffer sizes from verbose startup logs (keys kv_mib, compute_mib; empty dict when absent); estimate_needed_mib(tier:int, compute_mib_observed=None) -> float = weights bytes + tier * KV_BYTES_PER_TOKEN_F16 + (observed compute_mib if provided else a conservative 1536 MiB fallback multiplier clearly labeled estimated in output); check(needed_mib, free_mib, margin=1.05) -> PreflightVerdict(verdict, evidence, flags) writing the house verdict/evidence/flags triple: PASS when needed*margin <= free, otherwise FAILED:preflight-oom with the arithmetic in evidence — the expected-fail path publishes rather than hides (D2-18, S6 doctrine). record_ledger(run_store, cell_label, buffers:dict, guard_peaks:dict, dxg_line:str) appends a vram-ledger row combining parsed buffers, guard peaks, and the raw DXG free-VRAM string so VRAM is measured as carefully as throughput (methodology rule 5).
    test_preflight.py: known-input arithmetic assertions for all four tiers (4096/8192/16384/32768) against the frozen 18245 MiB free anchor — 4096 and 8192 PASS comfortably, 16384 marginal-pass computed exactly, 32768 exceeds and returns FAILED:preflight-oom; parse_free_mib unit cases on sample log lines; ledger row lands via a scratch RunStore.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_preflight.py benchmarks/tests/test_guard_fixtures.py benchmarks/tests/test_journal_crash.py -q'</automated>
  </verify>
  <acceptance_criteria>
    - Tier arithmetic matches hand-computed expectations; 32k verdict is exactly FAILED:preflight-oom with numeric evidence
    - Ledger rows appear in scratch store with buffers + guard peaks + DXG string
    - Full plan suite green in guest, CPU-only
  </acceptance_criteria>
  <done>BENCH-03's software surface complete: journal, guard, thresholds seam, pre-flight, ledger — awaiting live calibration data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark process -> /proc poller | process-supplied kernel counters read as external input |
| result files -> future auditors | store contents are the project's evidentiary record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | result store integrity | high | mitigate | CHECKSUMS.sha256 at close in sha256sum -c format, append-only discipline, supersede model keeps originals visible, tamper-detection asserted in tests |
| T-02-03-02 | DoS | runaway benchmark harming host | medium | mitigate | fail-fast policy with zero retry loops around allocation failure, flock session lock (consumer side in plan 02-04), guard polling provides kill-target PID file |
| T-02-03-03 | Repudiation | false spill accusations | low | accept | every verdict carries its numeric evidence in signals/evidence dicts; misclassification risk documented rather than hidden |
| T-02-03-SC | Tampering | dependency supply chain | low | accept | stdlib-only modules, no package installs |
</threat_model>

<verification>
- Guest quick suite green: python3 -m pytest benchmarks/tests/ -x -q (all three new modules)
- SIGKILL journal proof and checksum-tamper proof both encoded as permanent tests
- No hard-coded threshold literals anywhere in guard.py
</verification>

<success_criteria>
Given any synthetic or real trace, the guard's verdict is deterministic, vocabulary-locked, evidence-carrying — and impossible to produce before thresholds exist except in observe-only mode.
</success_criteria>

<output>
Create `docs/phases/02-benchmark-harness-baseline-matrix/02-03-SUMMARY.md` when done
</output>
