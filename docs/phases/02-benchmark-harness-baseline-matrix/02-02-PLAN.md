---
phase: 2
plan: 02-02
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/lib/fingerprint.py
  - benchmarks/lib/toast.py
  - benchmarks/host/hwinfo_daemon.py
  - benchmarks/host/thermal_watchdog.py
  - benchmarks/tests/test_manifest.py
  - benchmarks/tests/test_shmem_digest.py
  - benchmarks/tests/fixtures/gen_shmem_snapshot.py
autonomous: true
requirements: [BENCH-02]
user_setup:
  - service: HWiNFO64
    why: "Primary telemetry source (D2-01) — Shared Memory v2 feed must be enabled; absence degrades the phase to documented manual-CSV fallback, it does not block"
    env_vars: []
    dashboard_config:
      - task: "Enable Shared Memory Support (Settings -> Safety/OSD area per installed version) and leave HWiNFO sensors-only polling running"
        location: "HWiNFO64 settings dialog on Windows host"
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every run directory can carry a manifest.json with every D2-10 fingerprint field present, non-empty, and recomputable from the machine (BENCH-02)"
    - "Windows-side telemetry captures the nine mandatory sensor fields into per-run CSV slices inside the run directory, time-joined by UTC with a recorded skew check — or degrades explicitly to a recorded manual-fallback/absent mode"
    - "A thermal watchdog armed at 95 C junction kills the current benchmark process through the verified wsl.exe interop path and marks the row FAILED:thermal-abort"
    - "Guard trips and session summaries reach the owner as Windows toasts (D2-15)"
  artifacts:
    - path: "benchmarks/lib/fingerprint.py"
      provides: "collect() writing complete D2-10 manifest.json; sha256_file(); skew_check()"
    - path: "benchmarks/host/hwinfo_daemon.py"
      provides: "SM2 shmem reader daemon + label-map dump + manual-CSV fallback parser"
    - path: "benchmarks/host/thermal_watchdog.py"
      provides: "junction-temp watcher with validated-PID cross-boundary kill (D2-20)"
    - path: "benchmarks/lib/toast.py"
      provides: "XML-escaped toast channel via powershell.exe interop"
  key_links:
    - from: "benchmarks/lib/fingerprint.py"
      to: "benchmarks/environment/versions.txt + llamacpp-pin.txt"
      via: "field vocabulary extended into JSON manifest fields"
      pattern: "ROCm|commit|pin-tag"
    - from: "benchmarks/host/thermal_watchdog.py"
      to: "wsl.exe kill path"
      via: "integer-validated PID interpolated into verified interop command"
      pattern: "Ubuntu-24.04"
---

<objective>
Make every result row traceable to its exact environment (BENCH-02): guest-side fingerprint manifest (D2-10), the Windows-side HWiNFO shared-memory telemetry bridge with documented manual fallback (D2-01), and the two host safety/notification services every later session depends on — thermal watchdog (D2-20) and toast channel (D2-15).

Purpose: rocm-smi does not exist under ROCDXG; without this bridge, clocks/temps/shared-GPU-memory are unprovable assertions.
Output: benchmarks/lib/{fingerprint.py,toast.py}, benchmarks/host/{hwinfo_daemon.py,thermal_watchdog.py}, plus their fixture-driven tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/phases/02-benchmark-harness-baseline-matrix/02-CONTEXT.md
@docs/phases/02-benchmark-harness-baseline-matrix/02-RESEARCH.md
@docs/phases/02-benchmark-harness-baseline-matrix/02-PATTERNS.md
@benchmarks/environment/versions.txt

Environment facts (verified): powershell.exe interop works from guest AND wsl.exe from host; kill path `wsl.exe -d Ubuntu-24.04 -u root -- sh -c 'kill -9 <pid>'` live-tested (CROSS_KILL_OK); host Python 3.14.7 present; .wslconfig at %USERPROFILE%/.wslconfig (memory=28GB, swap=16GB); HWiNFO SM2 map name Global\HWiNFO_SENS_SM2 with HWiS/DEAD signatures and mutex Global\HWiNFO_SM2_MUTEX; non-Pro HWiNFO caps shared memory at 12 h/day. Guest python3 = 3.12.3 runs all pytest; host-side modules must keep parsing logic in pure functions so guest tests exercise them via byte fixtures.
</context>

## Artifacts this phase produces (this plan)
New files: benchmarks/lib/fingerprint.py · benchmarks/lib/toast.py · benchmarks/host/hwinfo_daemon.py · benchmarks/host/thermal_watchdog.py · benchmarks/tests/test_manifest.py · benchmarks/tests/test_shmem_digest.py · benchmarks/tests/fixtures/gen_shmem_snapshot.py
Commands introduced: `python benchmarks/host/hwinfo_daemon.py --label-map <out>` / `--watch --pid-file P --out-dir D` (host), `python benchmarks/host/thermal_watchdog.py --pid-file P --threshold-c N [--kill-mode wsl|native] [--dry-run]` (host), `python3 -c "import ..." fingerprint` (guest)

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — one real manifest snapshot with every D2-10 field</name>
  <files>benchmarks/lib/fingerprint.py, benchmarks/tests/test_manifest.py</files>
  <read_first>02-CONTEXT.md decision D2-10; 02-PATTERNS.md fingerprint.py assignment (versions.txt / llamacpp-pin.txt key vocabulary); 02-RESEARCH.md Assumption A8</read_first>
  <action>
    First, idempotently ensure the Wave-0 test scaffold independent of plan 02-01 landing first: if benchmarks/tests/pytest.ini is absent, create it with the minimal testpaths-relative config; if the guest lacks pytest, apt-get install -y python3-pytest. Both steps are existence-checked so parallel wave-mates converge on identical content instead of racing.
    Create benchmarks/lib/fingerprint.py implementing exactly: sha256_file(path) -> hex str (hashlib, streaming 1 MiB chunks — never hand-rolled hashing, S2); utc_now_iso() -> ISO8601 Z string (every capture carries the UTC stamp discipline of versions.txt); skew_check() -> dict comparing guest date -u output against host Get-Date UTC fetched via powershell.exe -NoProfile -Command, returning {guest_utc, host_utc, delta_seconds}; collect_manifest(run_dir, backend_arm, telemetry_mode, supersedes=None) -> dict gathering ALL D2-10 fields: harness git rev via git rev-parse HEAD of the repo; llama.cpp commit+tag+build flags parsed from benchmarks/environment/llamacpp-pin.txt; binary sha256 over /root/llama.cpp/build-ci/bin/llama-bench (one-shot compute, compare against archived baseline copy digest if present, record both); model sha256 recomputed once per session from /root/models/Qwen3.8-27B-Uncensored-IQ4_XS.gguf plus HF rev parsed from models/README.md; backend_arm as passed (HIP|Vulkan); ROCm 7.2.1 + librocdxg 1.2.2 parsed from versions.txt; guest kernel via uname -r, WSL kernel string via /proc/version tail, Windows build number via powershell.exe interop (cmd /c ver equivalent); Adrenalin driver version via powershell.exe WMI query Win32_VideoController DriverVersion; .wslconfig sha256 over %USERPROFILE%/.wslconfig; telemetry_mode as passed (shmem|manual-fallback|absent); UTC start/end stamps plus skew_check result; tune_state confirmation field recording method-string + UTC (stock re-verification itself is performed at session start by plan 02-04's protocol). Write manifest.json into run_dir atomically (tmp then os.replace). For the native-Vulkan arm later, ROCm/librocdxg fields take the literal value not-applicable-native-arm — honest degradation, never omission.
    Tracer proof: call collect_manifest against a scratch run dir on the real machine; assert every field non-empty and that sha256_file on the model matches the digest of record in models/README.md.
    test_manifest.py: asserts collect_manifest returns all D2-10 keys non-empty when run in-guest (skip with clear message when /proc/version lacks microsoft marker), recomputes binary/model digests matching independent sha256sum calls, and validates skew delta_seconds is a small integer.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_manifest.py -x -q'</automated>
  </verify>
  <acceptance_criteria>
    - Scratch manifest.json contains all D2-10 fields, none empty or null
    - Model digest equals the models/README.md sha256-of-record (53adc4bb… prefix family)
    - skew_check reports numeric delta_seconds between guest and host UTC
  </acceptance_criteria>
  <done>Fingerprint contract proven against the real environment end-to-end</done>
</task>

<task type="auto">
  <name>Task 2: HWiNFO shmem daemon + manual-fallback parser (per D2-01)</name>
  <precondition>If HWiNFO64 user_setup has not been completed, the daemon must still run and record telemetry_mode=absent rather than fail — fallback behavior is part of the deliverable</precondition>
  <files>benchmarks/host/hwinfo_daemon.py, benchmarks/tests/test_shmem_digest.py, benchmarks/tests/fixtures/gen_shmem_snapshot.py</files>
  <read_first>02-RESEARCH.md "HWiNFO SM2 reader skeleton", Pitfall 5 (12 h/day cap), Pitfall 9 (CSV encoding traps); 02-CONTEXT.md decision D2-01 mandatory field list; 02-PATTERNS.md hwinfo_daemon row</read_first>
  <action>
    Create benchmarks/host/hwinfo_daemon.py for HOST Python 3.14 following the RESEARCH skeleton semantics: open mmap of Global\HWiNFO_SENS_SM2, acquire Global\HWiNFO_SM2_MUTEX briefly around each snapshot, verify b'HWiS' signature, treat b'DEAD' as disabled-shared-memory. Keep the struct decoding in PURE functions importable without mmap: parse_header(buf) -> dict (signature/version/revision/poll_time/offsets/sizes/counts/poll_period per the mirrored hwisenssm2.h layout), iter_readings(buf) yielding decoded elements (tType/index/id/label[128]/user[128]/unit[16]/val/dmin/dmax/davg doubles, pack=1), match_labels(readings, wanted:list[str]) -> dict keyed by canonical field name using case-insensitive substring matching driven by a label-map file. Mandatory canonical fields (D2-01 + D2-12): gpu_core_clock_mhz, gpu_mem_clock_mhz, temp_edge_c, temp_hotspot_c, power_board_w, fan_pct, gpu_util_pct, vram_used_mb, shared_gpu_memory_mb.
    CLI modes: --label-map PATH dumps every reading label+unit to text for calibration (open question #1 resolution); --watch --pid-file P --out-dir D --interval-s 1.0 polls until the PID-file process disappears or --duration elapses, appending ISO-UTC-stamped rows to D/telemetry/host.csv, asserting freshness each poll (poll_time must advance within 2x dwPollingPeriod else write staleness marker file telemetry/STALE and exit code 3; DEAD signature exits 4). On any degraded state the daemon writes telemetry/mode.txt containing shmem|manual-fallback|absent so the orchestrator can pass it to the manifest. Include parse_manual_csv(path) -> list[dict] for the documented contingency: decode ISO-8859-1 (HWiNFO logging CSVs are not UTF-8), key columns off the header row, tolerate missing optional columns, and emit telemetry-gap markers for missing mandatory fields instead of fabricating zeros.
    gen_shmem_snapshot.py builds byte-level fixtures: a valid header+readings buffer containing labels matching the wanted substrings, a DEAD-signature buffer, and a stale-poll_time pair. test_shmem_digest.py (runs on GUEST python against the pure functions): valid snapshot parses to the mandatory-field dict; DEAD detected; stale detected; manual CSV fixture parses with mojibake-free labels and gap markers where columns are absent.
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_shmem_digest.py -x -q'</automated>
  </verify>
  <acceptance_criteria>
    - Pure-function parsers round-trip byte fixtures: valid snapshot yields all nine mandatory fields, DEAD and stale states detected with distinct exit codes in CLI mode
    - Manual CSV fixture parses ISO-8859-1 correctly with gap markers, no zero fabrication
    - Daemon module imports cleanly under guest python (no accidental host-only imports at module top level)
  </acceptance_criteria>
  <done>D2-01 bridge implemented with graceful degradation and its own permanent regression tests</done>
</task>

<task type="auto">
  <name>Task 3: Thermal watchdog + toast channel (per D2-20, D2-15)</name>
  <files>benchmarks/host/thermal_watchdog.py, benchmarks/lib/toast.py</files>
  <read_first>02-CONTEXT.md decisions D2-20 and D2-15; 02-PATTERNS.md S7 cross-boundary interop safety + thermal_watchdog row; 02-RESEARCH.md Pitfall 6 and ASVS V5 threat table</read_first>
  <action>
    Create benchmarks/lib/toast.py: build_toast_xml(title, body) -> str as a PURE function applying xml.sax.saxutils.escape to both strings (raw telemetry text must never reach a shell unescaped); send(title, body) -> bool writes a temporary .ps1 taking -Title and -Body parameters and invokes powershell.exe -NoProfile -ExecutionPolicy Bypass -File script.ps1 -Title .. -Body .. (the -File parameter form mandated by S7 — never inline interpolated commands), registering the toast under PowerShell's own AUMID with raw WinRT XML (zero-install default; BurntToast deliberately NOT used); send_summary(ok_count, failed_count) formats the D2-15 end-of-session ping body N OK / M FAILED.
    Create benchmarks/host/thermal_watchdog.py: expose build_kill_command(pid:int, mode:str="wsl") -> str as a PURE function returning EXACTLY the verified command forms — wsl mode: wsl.exe -d Ubuntu-24.04 -u root -- sh -c 'kill -9 <pid>' (the live-tested CROSS_KILL_OK interop string); native mode: taskkill /PID <pid> /F. CLI --pid-file P --threshold-c 95 --poll-s 2 --kill-mode wsl|native [--dry-run] [--allow-terminate]; reads junction temperature each poll via the Task-2 snapshot reader (import from sibling module), and on reading >= threshold: reads the PID file, validates the value is a decimal integer (ASVS V5 — reject anything else with a logged error, never interpolate unvalidated text into a shell) BEFORE calling build_kill_command, executes the built command in wsl mode or native mode (for the plan-05 Windows-native arm), logs every attempt, writes verdict marker file content FAILED:thermal-abort next to the PID file, and fires send(). Last resort only, gated behind --allow-terminate: wsl.exe --terminate Ubuntu-24.04. NO software fan control anywhere — the watchdog records temperatures, it never drives fans (D2-20 record-don't-control). --dry-run prints build_kill_command output instead of executing (the unit-test seam).
    Prove mechanics via PURE-FUNCTION tests added to the guest suite (test_manifest.py — host modules are stdlib-only and import cleanly under guest python): test_build_kill_command_interop_string asserts build_kill_command(4242) equals character-for-character "wsl.exe -d Ubuntu-24.04 -u root -- sh -c 'kill -9 4242'"; a sibling test asserts build_toast_xml escapes angle brackets/ampersands; a third asserts send_summary formats the N OK / M FAILED body. The host-side dry-run CLI remains available as an owner spot-check only; the mechanical assertion lives entirely in the guest pytest suite, and the live end-to-end kill rehearsal happens at calibration (plan 02-04 rehearse-kill).
  </action>
  <verify>
    <automated>wsl.exe -d Ubuntu-24.04 -u root -- bash -lc 'cd /mnt/e/Projects/qwen_3.8_27b_optimizations && python3 -m pytest benchmarks/tests/test_manifest.py -x -q'</automated>
    <human-check>Owner spot-check (optional): run python benchmarks/host/thermal_watchdog.py --pid-file benchmarks/tests/.dryrun.pid --threshold-c 60 --dry-run on the host and eyeball that the printed command is the verified interop form.</human-check>
  </verify>
  <acceptance_criteria>
    - Guest pytest asserts build_kill_command(4242) returns exactly "wsl.exe -d Ubuntu-24.04 -u root -- sh -c 'kill -9 4242'" — integer-validated input, no other shell surface
    - build_toast_xml escaping of angle brackets/ampersands asserted by a named guest test
    - No fan-control code path exists anywhere in the module (record-only)
  </acceptance_criteria>
  <done>Host safety net (thermal abort + owner notifications) implemented and mechanically checkable before any GPU session uses it</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest -> powershell.exe | toast payloads and host fingerprint queries cross into Windows shell execution |
| host -> wsl.exe | kill commands cross into the guest with PIDs read from files |
| HWiNFO shmem -> harness | sensor data is external input consumed by parsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering/Elevation | toast + fingerprint interop calls | high | mitigate | -File script.ps1 -Param form (never inline interpolation), XML-escaped toast strings, fixed WMI query strings, integer-validated PIDs in watchdog |
| T-02-02-02 | Repudiation | telemetry authenticity/staleness | medium | mitigate | HWiS/DEAD signature checks, poll_time freshness assertion with distinct exit codes, telemetry_mode recorded in every manifest including absent |
| T-02-02-03 | Information Disclosure | manifest contents | low | accept | local single-owner tooling; manifest holds version strings and digests only, no secrets by design |
| T-02-02-SC | Tampering | dependency supply chain | low | accept | stdlib-only modules; HWiNFO64 is vendor-installed by the owner (user_setup), no registry packages introduced |
</threat_model>

<verification>
- Guest quick suite green: python3 -m pytest benchmarks/tests/test_manifest.py benchmarks/tests/test_shmem_digest.py -q
- Real-machine tracer manifest exists with all D2-10 fields non-empty
- Watchdog kill-command construction proven by guest pytest pure-function assertions against the CROSS_KILL_OK-verified interop string; toast XML escaping proven by named tests
</verification>

<success_criteria>
Any future run directory can answer "what commit, what driver, what clocks, what temps, who killed the process" entirely from files inside it.
</success_criteria>

<output>
Create `docs/phases/02-benchmark-harness-baseline-matrix/02-02-SUMMARY.md` when done
</output>
