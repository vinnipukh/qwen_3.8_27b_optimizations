### Result
"Skip to content\nNavigation Menu\nPlatform\nSolutions\nResources\nOpen Source\nEnterprise\nPricing\nSign in\nSign up\nmicrosoft\n/\nWSL\nPublic\nNotifications\nFork 1.8k\n Star 33.6k\nCode\nIssues\n925\nPull requests\n66\nDiscussions\nActions\nSecurity and quality\nInsights\n[Bug] WSL2 GPU passthrough has 16 GiB CUDA driver overhead on Blackwell sm_120 — blocks PyTorch large allocations\n #40401\nNew issue\nOpen\nDescription\nAbilay (noMugop)\nopened \nWindows Version\n\nMicrosoft Windows [Version 10.0.26100.1742]\n\nWSL Version\n\n2.6.3.0\n\nAre you using WSL 1 or WSL 2?\nWSL 2\nWSL 1\nKernel Version\n\n6.6.87.2-microsoft-standard-WSL2\n\nDistro Version\n\nUbuntu 22.04.5 LTS (Jammy Jellyfish)\n\nOther Software\nvLLM 0.20.0 (also tested 0.17.1) — PyTorch-based LLM inference engine\nSGLang 0.5.10 — alternative PyTorch-based inference engine\nPyTorch 2.10.0+cu128 (also tested 2.11.0+cu130)\nNVIDIA driver 596.36 on Windows host (also tested 581.80 RTX Enterprise)\nCUDA 12.8 / 13.0 (via pip nvidia-cuda-* wheels in venv)\nGPU: NVIDIA RTX PRO 6000 Blackwell Workstation Edition (96 GB VRAM, sm_120)\nRepro Steps\nFresh WSL2 Ubuntu 22.04 install on Windows 11 LTSC build 26100\nInstall NVIDIA driver on Windows host (596.36 or 581.80)\nInstall Python 3.10 + PyTorch + vLLM in WSL2:\npython3.10 -m venv ~/vllm-env\nsource ~/vllm-env/bin/activate\npip install vllm==0.17.1 --extra-index-url https://download.pytorch.org/whl/cu128\nTry to load any hybrid Mamba model (e.g. Qwen3.6-27B-FP8):\nObserve the error in journal/logs\nNote that torch.cuda.mem_get_info() reports free=80+ GiB at fresh init, BUT actual usable contiguous space is ~24 GiB due to hidden CUDA context overhead\nExpected Behavior\n\nWSL2 GPU passthrough should have CUDA driver context overhead comparable to native Linux (~1-2 GiB).\n\nThe same hardware on native Linux (any distro with appropriate NVIDIA driver) shows 1-2 GiB CUDA context overhead at idle, allowing PyTorch to allocate large contiguous tensors (3-18 GiB) for hybrid Mamba models without OOM.\n\nActual Behavior\n\nWSL2 GPU passthrough on NVIDIA RTX PRO 6000 Blackwell (sm_120) has ~16 GiB CUDA driver context overhead at PyTorch init — about 10x higher than native Linux on the same hardware.\n\nThis invisible overhead is consumed by:\n\nDXGI shim layer (/usr/lib/wsl/drivers/...)\nHyper-V virtualization layer\nCUDA driver context for sm_120 architecture\n\nThe overhead is NOT visible to torch.cuda.mem_get_info() so PyTorch-based frameworks (vLLM, SGLang) plan KV cache allocations based on incorrect free memory estimates → OOM at allocation time even though nvidia-smi shows 50+ GiB free.\n\nConcrete error from vLLM:\ntorch.OutOfMemoryError: CUDA out of memory.\nTried to allocate 3.48 GiB.\nGPU 0 has a total capacity of 95.59 GiB of which 50.40 GiB is free.\nIncluding non-PyTorch memory, this process has 17179869184.00 GiB memory in use.\n↑ display bug, actually = 16 GiB\nOf the allocated memory 42.49 GiB is allocated by PyTorch\n\nCritical: Tested with two NVIDIA driver versions (596.36 from Dec 2025 and 581.80 RTX Enterprise from Nov 2025) — identical 16 GiB overhead. Same drivers on native Linux show 1-2 GiB. So this is NOT a NVIDIA driver bug — it's WSL2 virtualization layer behavior.\n\nImpact\nAll hybrid Mamba/SSM models (Qwen3.6 family, Mamba-2, Jamba, etc.) cannot be used\nAll PyTorch-based inference engines affected (vLLM, SGLang, TGI, LMDeploy)\nWorkstation Blackwell cards ($5000-10000 hardware) are impacted\nWSL2 is the standard Windows AI dev environment\nCross-references\n[Bug]: Qwen3.6 hybrid Mamba models fail KV cache allocation on RTX PRO 6000 Blackwell + WSL2 — 16 GiB invisible CUDA overhead vllm-project/vllm#41619 (main bug report with full debugging context)\nsgl-project/sglang issue (TODO link if filed)\npytorch/pytorch issue (TODO link if filed)\nDiagnostic Logs\nFull system info dump: see attached system-info.txt\ncollect_env.py output: see attached collect_env_output.txt\nvLLM full error log: see attached vllm-error-full.log\nReproduction script: see attached reproduce.sh\nWSL config: .wslconfig contents:\n[wsl2]\nmemory=24GB\nprocessors=14\nswap=16GB\nswapFile=F:\\wsl-swap.vhdx\nnestedVirtualization=false\nnetworkingMode=mirrored\nvmIdleTimeout=-1\n\ncollect_env_output.txt\nreproduce.sh\nsystem-info.txt\nvllm-error-full.log\n\n👍\nReact with 👍\n1\nReacted by wzgrx\nActivity\ngithub-actions commented \ngithub-actions\nbot\n – with GitHub Actions\nContributor\nLogs are required for review from WSL team\n\nIf this a feature request, please reply with '/feature'. If this is a question, reply with '/question'.\nOtherwise, please attach logs by following the instructions below, your issue will not be reviewed unless they are added. These logs will help us understand what is going on in your machine.\n\nHow to collect WSL logs\ngithub-actions\nadded \nneeds-author-feedback\n \nAbilay (noMugop)\nmentioned this in 2 issues \n[Bug]: Qwen3.6 hybrid Mamba models fail KV cache allocation on RTX PRO 6000 Blackwell + WSL2 — 16 GiB invisible CUDA overhead vllm-project/vllm#41619\n[Bug] Qwen3.6 hybrid Mamba: Mamba pool 9-18 GiB allocation OOM on RTX PRO 6000 Blackwell + WSL2 sgl-project/sglang#24364\nnoMugop commented \nAbilay (noMugop)\nAuthor\nRelated issues — full bug surface across the stack\n\nThis problem affects multiple layers (PyTorch CUDA allocator → inference engine → WSL2 GPU passthrough). Filed in all relevant projects so each team sees the part they own:\n\nLayer\tIssue\nInference engine (vLLM)\tvllm-project/vllm#41619\nInference engine (SGLang)\tsgl-project/sglang#24364\nML framework (PyTorch CUDA allocator)\tpytorch/pytorch#182286\nPlatform (WSL2 GPU passthrough)\t#40401\nWhy all four?\nvLLM and SGLang independently reproduce the OOM with the same symptom (torch.OutOfMemoryError on Mamba state cache allocation despite 50+ GiB of free VRAM). Confirms the bug is not engine-specific.\nPyTorch is where the actual torch.OutOfMemoryError is raised — the CUDA caching allocator cannot satisfy 3-18 GiB contiguous allocations even when mem_get_info() reports plenty of free memory.\nWSL2 is where the invisible 16 GiB CUDA driver overhead comes from (DXGI shim + Hyper-V). On native Linux this overhead is 1-2 GiB on the same hardware.\nHardware/OS\n\nAll 4 issues use identical setup:\n\nGPU: NVIDIA RTX PRO 6000 Blackwell Workstation Edition (sm_120, 96 GB VRAM)\nDriver: NVIDIA 596.36 (also tested 581.80 RTX Enterprise — same overhead)\nOS: Windows 11 LTSC build 26100 → WSL 2.6.3.0 → Ubuntu 22.04.5 LTS\nModels: Qwen3.6 family (Qwen3_5ForConditionalGeneration / Qwen3_5MoeForConditionalGeneration — hybrid GDN+Mamba architecture)\n\nIf anyone investigating: please cross-link to the issues above so we can coordinate the fix across teams.\n\nAbilay (noMugop)\nmentioned this \n[Bug] CUDA caching allocator unable to satisfy 3-18 GiB contiguous allocation despite 50+ GiB free on RTX PRO 6000 Blackwell + WSL2 (16 GiB hidden CUDA overhead) pytorch/pytorch#182286\nmicrosoft-github-policy-service\nremoved \nneeds-author-feedback\n \nnoMugop commented \nAbilay (noMugop)\nAuthor\n\nWSL diagnostic logs collected via collect-wsl-logs.ps1 (per the bot request).\n\nNote: I pressed the key immediately without active repro because the bug isn't a runtime crash — it's a memory accounting issue visible at idle. The 16 GiB CUDA driver overhead is reproducible by simply loading any large CUDA tensor in PyTorch under WSL2 on this Blackwell hardware. The full vLLM error log showing the OOM is already attached to the original issue body.\n\nIf active repro trace is needed — please let me know and I'll re-run with the bug reproduction.\n\nWslLogs-2026-05-04_18-59-30.tar.gz\n\ngithub-actions commented \ngithub-actions\nbot\n – with GitHub Actions\nContributor\n\nThe log file doesn't contain any WSL traces. Please make sure that you reproduced the issue while the log collection was running.\n\nDiagnostic information\ngithub-actions\nadded \nneeds-author-feedback\n \nnoMugop commented \nAbilay (noMugop)\nAuthor\n\nRe-collected logs with active reproduction this time. The vLLM service was running during log collection, attempting to load Qwen/Qwen3.6-27B-FP8 and triggering the OOM at the Mamba state cache allocation step.\n\nAttached: WslLogs-2026-05-04_19-14-51.tar.gz (4 MB, contains active WPR trace).\n\nWslLogs-2026-05-04_19-14-51.tar.gz\n\nmicrosoft-github-policy-service\nremoved \nneeds-author-feedback\n \ngithub-actions commented \ngithub-actions\nbot\n – with GitHub Actions\nContributor\nDiagnostic information\nBen Hillis (benhillis)\nadded \nGPU\n \nwzgrx commented \nwzgrx\n\n没人管？\n\nwzgrx\nmentioned this \n⚠️ WSL2 GPU OOM leads to Hyper-V kernel panic and Windows BLUE SCREEN OF DEATH — total system crash, not a recoverable error #40732\nnoMugop commented \nAbilay (noMugop)\nAuthor\n\nI hope not ;(\n\n👀\nReact with 👀\n1\nReacted by wzgrx\nyann-amd\nmentioned this \nMisc. bug: ROCm has new DXG connector for WSL2, gives weird free memory amounts (probably not llama.cpp bug, but worth documenting) ggml-org/llama.cpp#23999\nNakanokensetsu commented \nNakanokensetsu (Nakanokensetsu)\nMinimal repro: the huge \"hidden\" reservation is triggered by bare torch.cuda.init(), independent of expandable_segments / NCCL / any inference engine\n\nCross-posting this data point because it narrows down where the fix needs to land.\n\nSetup: Windows 11 + WSL2 (kernel 6.18.33.2), 2x NVIDIA GeForce RTX 5070 (Blackwell, sm_120, 12GB each), driver 610.62 (CUDA UMD 13.3), PyTorch 2.9.1+cu128 / 2.11.0+cu128 (reproduced on both).\n\nWhat we measured: On this machine, sglang (TP=2) production traffic was correlating 1:1 with Microsoft-Windows-Resource-Exhaustion-Detector events on the Windows host — vmmemWSL's PrivateMemorySize/commit charge climbing to 68–74 GB while its WorkingSet stayed under 5–40 GB. .wslconfig caps memory=48GB, so this commit is coming from somewhere the guest-memory cap doesn't cover.\n\nTracing /proc/<pid>/maps + /proc/<pid>/smaps for the sglang TP-rank worker processes, we found a PROT_NONE, anonymous, zero-RSS mapping of ~80GB (single-GPU-context process) up to ~128GB (per NCCL rank process) — always at the same fixed address (0x207800000-0x1600000000), Rss: 0 kB / Pss: 0 kB on the Linux side (i.e. genuinely free on native Linux accounting), but apparently counted (at least partially) toward vmmemWSL's Windows-side commit charge.\n\nWe isolated the trigger with a series of tests, each removing one more layer:\n\n--disable-cuda-graph on sglang → same reservation, unchanged.\nPYTORCH_CUDA_ALLOC_CONF=expandable_segments:False → same reservation, unchanged size and address.\n--context-length 81920 → 8192 (10x smaller KV cache pool) → same reservation, unchanged.\nBare 2-process torch.distributed NCCL init (dist.init_process_group(\"nccl\"), no engine, no model) on 2 GPUs → reservation jumps from ~80GB (single GPU, no NCCL) to ~104GB — so NCCL amplifies it, but isn't required for it to appear.\nMinimal repro, single GPU, no NCCL, no distributed, nothing else:\nimport torch\ntorch.cuda.init()\nx = torch.zeros(10, device=\"cuda:0\")\nThis alone reproduces the same ~80GB PROT_NONE mapping at the identical address, immediately after torch.cuda.init(). exllamav3 (which manages multi-GPU with manual placement, no NCCL, single process) shows the same class of reservation too (~137GB observed, single process) — so this isn't specific to any inference engine's memory pool implementation, and isn't gated by expandable_segments.\n\nConclusion (tentative): this looks like it's happening at CUDA context creation itself — inside libcuda.so / the WSL2 GPU-PV (DXG) shim for Blackwell — before any PyTorch allocator code runs, since disabling expandable_segments has zero effect on it. That would point the fix at #40401 (WSL2 GPU-PV layer) rather than pytorch/pytorch's CUDA caching allocator, but flagging it here too since the allocator's OOM diagnostics are what surface it as a torch.OutOfMemoryError.\n\nConfirmed the reservation is fully released (Linux Committed_AS drops from ~180GB to ~11GB) when the process exits, but vmmemWSL's Windows-side commit only partially recovers (68GB → 48GB in one measurement) and recovers slowly — consistent with autoMemoryReclaim=dropCache not reclaiming this class of mapping quickly.\n\nHappy to share raw /proc/*/maps, /proc/*/smaps, and the Windows Resource-Exhaustion-Detector event log timestamps if useful.\n\n808_Wxyz (Hi5808)\nadded a commit that references this issue \n\nAdd paper 18: Blackwell VRAM paging fallback is a Windows behavior, n…\n\n...\n5be8b23\nHuzaifa Tariq (huzaifa-57)\nmentioned this \n[BUG] CUDA Unified Memory (nvbuf-memory-type=3) CPU-mapping segfaults in libpixman under GPU-PV on Blackwell (RTX 5070) -- reproduces on driver 596.49 AND 595.97 #41483\nSign up for free\n to join this conversation on GitHub. Already have an account? Sign in to comment\nMetadata\nAssignees\nNo one assigned\nLabels\nGPU\nType\nNo type\nProjects\nNo projects\nMilestone\nNo milestone\nRelationships\nNone yet\nDevelopment\nNo branches or pull requests\nParticipants\nIssue actions\nOpen in GitHub Copilot app\nFooter\n© 2026 GitHub, Inc.\nFooter navigation\nTerms\nPrivacy\nSecurity\nStatus\nCommunity\nDocs\nContact\nManage cookies\nDo not share my personal information"
### Ran Playwright code
```js
await page.evaluate('() => document.documentElement.innerText.slice(0,15000)');
```
### Page
- Page URL: https://github.com/microsoft/WSL/issues/40401
- Page Title: [Bug] WSL2 GPU passthrough has 16 GiB CUDA driver overhead on Blackwell sm_120 — blocks PyTorch large allocations · Issue #40401 · microsoft/WSL
- Console: 3 errors, 0 warnings
### Events
- New console entries: .playwright-cli\console-2026-08-30T17-50-39-576Z.log#L2-L3
